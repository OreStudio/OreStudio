:PROPERTIES:
:ID: BA0E3273-DC4B-7CD4-447B-06227B7D09A8
:ROAM_ALIASES: MASD
:END:
#+title: Model Assisted Software Development
#+description: The MASD methodology and how its logical/facet model maps onto ORE Studio components and codegen profiles.
#+type: knowledge
#+version: 2
#+level: cross
#+filetags: :knowledge:masd:methodology:codegen:architecture:
#+created: 2024-07-01
#+updated: 2026-05-21

MASD is the [[https://uhra.herts.ac.uk/id/eprint/17031/][Model Assisted Software Development]] methodology developed as
part of a PhD programme. Though very few concrete artefacts of MASD are
used in this project — in particular, we do not use the code generator
[[https://github.com/MASD-Project/dogen/][Dogen]] which was decommissioned — this project ports MASD's core ideas to
[[https://en.wikipedia.org/wiki/Large_language_model][LLMs]] and [[https://en.wikipedia.org/wiki/Natural_language_processing][NLP]]. The conceptual model is relevant to understanding the
organisation of the project.

* Conceptual Model

MASD starts with the premise that a software project is a collection of
/physical artefacts/ — files and directories — which live in /physical
space/; and that these artefacts can be thought of as projections from a
higher-dimension space called the /logical space/. For the purposes of
[[id:D04D3476-D7C5-3954-A33B-C641EBCB43F6][ORE Studio]]:

- /logical entities/ have well-defined properties in logical space which
  we call /facets/.
- [[https://masd-project.github.io/progen/docs/models_and_transformations.html#ID-C807836B-B1D6-1024-86E3-7D49BCF20D74][transformations]] can be defined which convert these facets into their
  physical representation.

Key terms:

- A /product/ is the overall container for the physical artefacts — the
  GitHub project.
- Products are made up of /components/ — logical groupings of physical
  artefacts, corresponding to a C++ project or any other bundle of files.
- Within each component we encounter the physical expression of different
  /facets/ of logical elements.

MASD using NLP and LLMs does not require the rigid formalism of
[[https://masd-project.github.io/progen/docs/models_and_transformations.html][meta-models]] and meta-meta-models. Instead, the underlying concepts are
ported into a set of recipes called [[id:F663DF66-4F62-0884-DB23-BB2D6A42B644][skills]] which the LLM can understand,
and these are used to generate the target physical artefacts.

The key attributes of the logical element we intend to model are kept
within the /type/ facet — regular C++ code that defines a =struct= with
the domain type. Each logical element lives on its "native" component
(e.g. =account= in =ores.iam=). They are defined via the
[[id:b1450696-8c51-4cc5-8910-84912a411ab6][Domain Type Creator]] skill, which bundles a number of facets together.
Other facets then refer to the domain type files to extract required
information.

* Facet Catalogue

The facets of a domain entity are generated by [[id:08FBD248-257A-A474-DD43-DB08949978F1][ores.codegen]] profiles.
Each profile corresponds to one or more facets:

| Facet | Codegen profile |
|---|---|
| Class definition (struct, JSON I/O, table I/O) | =--profile domain= |
| Test data generator | =--profile generator= |
| Repository entity and mapper | =--profile repository= |
| Repository CRUD operations | =--profile repository= |
| Service layer | =--profile service= |
| NATS messaging protocol | =--profile protocol= |
| SQL schema, triggers, drop | =--profile sql= |
| Qt UI (12 files) | =--profile qt= |

See [[id:08FBD248-257A-A474-DD43-DB08949978F1][ores.codegen]]§"Profile catalogue" for the full profile descriptions
and the Mustache templates each profile invokes.

* See also

- [[id:08FBD248-257A-A474-DD43-DB08949978F1][ores.codegen]] — the code generator that implements the facet
  transformations.
- [[id:b1450696-8c51-4cc5-8910-84912a411ab6][Domain Type Creator]] — skill for generating all facets of a domain
  entity.
- [[id:f27e5df7-6223-4b6f-80a5-cefbeb1bc756][Component architecture]] — how components are structured and which
  facets belong in each part (=.api= / =.core= / =.service=).
