:PROPERTIES:
:ID: B69BF02C-9E16-4DAF-BC0C-C5DBE0EC1BF4
:END:
#+title: Task: Make a nullable tenant column an optional type
#+description: nullable_tenant_id makes the SQL column nullable but codegen still emits a non-optional tenant_id, and system() is a real tenant rather than an absence, so NULL and system are conflated; synthetic's mapper throws on a NULL tenant row.
#+type: task
#+level: s1
#+filetags: :unify_llm_skills_catalogue:sprint_25:v0:
#+owner: marco
#+branch:
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-09-12
#+updated: 2026-09-12
#+environment:
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:2FCD10F0-E7B5-4CBA-A7C7-E28845DB8AAC][Unify the LLM skills catalogue]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

A nullable tenant column produces an optional domain and entity type, and a NULL tenant row round-trips instead of throwing.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DISCOVERED                              |
| Parent story | [[id:2FCD10F0-E7B5-4CBA-A7C7-E28845DB8AAC][Unify the LLM skills catalogue]] |
| Now          | Not yet started.                       |
| Waiting on   | Nothing.                               |
| Next         | Begin implementation.                  |
| Last touched | 2026-09-12                               |

* Acceptance

- nullable_tenant_id true emits an optional tenant type in the domain struct and the repository entity.
- The generated mapper maps SQL NULL to an empty optional rather than calling from_string on an empty value.
- synthetic.market_data_generation_config no longer throws on a row whose tenant_id is NULL, proved by a test that reads such a row.
- scheduler.job_definition's hand-written optional tenant_id matches what codegen emits.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result
