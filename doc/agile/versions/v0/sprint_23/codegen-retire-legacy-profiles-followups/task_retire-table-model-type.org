:PROPERTIES:
:ID: 505820E3-FCB4-4192-AA0F-BDBDC1177088
:END:
#+title: Task: Retire the table model type: remove the table branch from the generator and profile matrix
#+description: ABANDONED — absorbed into "B7: Remove split-model backward-compatibility once all models migrated" (AE754D5A), which wanted the identical table-type removal. Once no _table.org files remain, remove the table branch from get_model_type(), load_model(), and the profile matrix in the facet catalogue. Junction files remain a distinct type and are out of scope. Update the codegen input org-file schema reference to mark ores.codegen.table as removed.
#+type: task
#+level: s1
#+filetags: :codegen-retire-legacy-profiles-followups:sprint_23:v0:
#+owner: marco
#+branch:
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-06-27
#+updated: 2026-06-27
#+environment:
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:9345CBD1-2D27-4B7E-9D1F-7A25C6266F69][Codegen legacy-profile retirement follow-ups]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The ores.codegen.table model type is fully deleted from the generator; no dead code paths remain for it.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | ABANDONED                              |
| Parent story | [[id:9345CBD1-2D27-4B7E-9D1F-7A25C6266F69][Codegen legacy-profile retirement follow-ups]] |
| Now          | Nothing.                       |
| Waiting on   | Nothing.                               |
| Next         | Nothing.                       |
| Last touched | 2026-07-20                               |

* Acceptance

- get_model_type() has no table branch; the function raises on any filename that would previously have returned table.
- load_model() has no call site for load_org_table_model() (already deleted in merge-org-parsers task).
- The facet catalogue has no sql profile entry for the table type.
- The codegen input org-file schema reference doc is updated to mark ores.codegen.table as retired.
- compass codegen regenerate runs cleanly across all remaining entity files with zero errors.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Abandoned — this task's entire scope (remove the =table= model type
from =get_model_type()=/=load_model()=, update the codegen input
org-file schema reference) was independently discovered and done in
one pass under "B7: Remove split-model backward-compatibility once all
models migrated" ([[id:AE754D5A-7C07-490A-A1B4-56CD7E7A3A14][AE754D5A]]), which had substantially overlapping
acceptance criteria. See that task's Result for what shipped. The
"profile matrix in the facet catalogue" reference here is itself
stale — =facet_catalogue.org= was already deleted outright by an
earlier task in this story (B10).
