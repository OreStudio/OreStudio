:PROPERTIES:
:ID: 0761A4EC-66D1-4C3E-B46F-36D18B3D3CFA
:END:
#+title: Task: End-to-end fixes for grid work
#+description: Service dashboard offline threshold raised to 120s; ORES_VERSION macro for accurate version display; status-services.sh node vs service counts; idle heartbeat sender in wrapper for online hosts; ores.platform::datetime for cross-platform timestamps; result_handler::submit uses service context for unauthenticated wrapper requests; outcome code mapping fix; host_id persisted to result; file extension preservation in artifact URIs (.tar.gz, .csv, .xml); double-slash URL fix in AppVersionDetailDialog; SIGSEGV fix; pill badges on Result UI; main window geometry persistence; RAII thread cleanup + terminate() guard in wrapper.
#+type: task
#+version: 2
#+level: s1
#+filetags: :compute:qt:bug_fix:polish:platform:wrapper:compute_grid_polish_and_e2e:sprint_15:v0:
#+owner: marco
#+branch:
#+pr:
#+blocked_on: none
#+blocked_since:
#+created: 2026-05-20
#+updated: 2026-05-20
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:0DCEDAE5-E5F0-4AF8-AE85-37C7618237A6][Compute grid polish and E2E fixes]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Comprehensive end-to-end fix sweep on the compute grid based on real-data testing.

* Status

| Field        | Value                                                                          |
|--------------+--------------------------------------------------------------------------------|
| State        | DONE                                                                           |
| Parent story | [[id:0DCEDAE5-E5F0-4AF8-AE85-37C7618237A6][Compute grid polish and E2E fixes]] |
| Now          | Completed 2026-03-26.                                                          |
| Waiting on   | None.                                                                          |
| Next         | None.                                                                          |
| Last touched | 2026-03-26                                                                     |

* Acceptance

- Service dashboard offline threshold raised to 120s (services no longer flash Red on shutdown).
- ORES_VERSION macro replaces hardcoded '1.0' version display.
- status-services.sh distinguishes node vs service counts.
- Idle heartbeat sender in wrapper fixes 'Online hosts always 0'.
- ores.platform::datetime replaces std::format-produced nanoseconds that sqlgen::Timestamp couldn't parse.
- result_handler::submit uses service context for unauthenticated wrapper requests (was getting stuck InProgress).
- Outcome code mapping: wrapper 0/1 → domain 1=Success / 3=ClientError.
- host_id added to submit_result_request + persisted to result.
- File extension preservation (.tar.gz, .csv, .xml) in artifact URIs; HTTP routes generalised /{artifact}.
- Double-slash URL bug in AppVersionDetailDialog re-upload fixed.
- SIGSEGV in onWindowMenuAboutToShow fixed (dangling window pointer).
- ClientResultItemDelegate renders State + Outcome as pill badges.
- MainWindow geometry saved + restored across sessions.
- Compute wrapper: RAII thread cleanup + terminate() guard on startup exceptions.

* Plan

Captured during execution; cleared into the parent story on close.

* Notes

Closes the door on the compute grid as a deliverable — every issue surfaced during real-data testing has a fix.

* Result

Grid ready for use.
