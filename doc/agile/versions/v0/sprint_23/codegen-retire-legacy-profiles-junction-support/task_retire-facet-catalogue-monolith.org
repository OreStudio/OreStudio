:PROPERTIES:
:ID: E286B5ED-6B1C-4738-8E67-8F62F0C1C741
:END:
#+title: Task: B10: Retire facet_catalogue.org file + legacy literate facet/group docs
#+description: Delete facet_catalogue.org and the 18 legacy literate facet/facet_group docs (cpp_qt.org, cpp_group.org, sql_schema.org, ...), then repoint or remove the hundreds of inbound id: links (56+ to facet_catalogue alone) across the org-roam graph to the new ores.* nodes. The dead load_profiles()/_load_profiles_from_org() Python code itself was already deleted by task "Delete legacy codegen profile system" (386BED71) -- confirmed zero live callers, so that half of the original B10 scope is done; this task now covers only the file deletions and link repointing.
#+type: task
#+level: s1
#+filetags: :codegen:physical-space:cleanup:codegen-retire-legacy-profiles-junction-support:sprint_23:v0:
#+owner: marco
#+branch: feature/retire-facet-catalogue-monolith
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-06-28
#+updated: 2026-07-16
#+environment: jolly_knuth
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:9023BDE0-979F-422F-BC73-985D6A80E7CB][Retire legacy codegen profile system; add junction support to physical-space codegen]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

facet_catalogue.org and the legacy literate facet/facet_group docs are gone; every inbound id: link is repointed to the corresponding ores.* graph node (or removed); the site builds with no unresolved links and codegen still generates byte-identically.

*Narrowed from the original scope*: the dead profile-resolution Python
(=load_profiles()=, =_load_profiles_from_org()=, =_parse_facet_table()=,
plus their exclusive regex helpers) was already deleted by task "Delete
legacy codegen profile system" (id 386BED71-A76B-43EF-8A72-0D19704C5151)
-- grep confirmed zero live callers anywhere, so it was safe to remove
independently of the file/link work below. That task deliberately did
*not* delete =facet_catalogue.org= itself: a grep for inbound =id:=
links to its file id (A8145FD7-0651-40FD-857E-BC37C9320020) turned up
~80 files, matching this task's own "56 to facet_catalogue alone"
estimate -- real, separate blast radius that belongs here with its own
content-porting precondition, not folded into a "should be zero diff"
deletion task.

*Precondition — port useful content first (do NOT delete before this):* the
legacy docs carry substantive hand-written prose that the scaffolded ores.*
nodes lack (they only have the brief #+description). Before any deletion,
port each doc's real content to its corresponding new node:
- group doc (e.g. =cpp_group.org=) Summary + shared-conventions prose →
  the technical-space node (=ores.cpp.org=).
- facet doc (e.g. =cpp_qt.org=, =sql_schema.org=) Summary, "The <facet>
  facet" projection prose, and Mustache-incantations notes →
  the facet node (=ores.cpp.qt.org=, =ores.sql.schema.org=).
- =facet_catalogue.org= per-archetype descriptions → already captured as
  =#+description= on the archetype/facet nodes; verify none are lost.
Audit every legacy doc for non-placeholder prose and confirm it has a home
in the graph before removing the source.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:9023BDE0-979F-422F-BC73-985D6A80E7CB][Retire legacy codegen profile system; add junction support to physical-space codegen]] |
| Now          | Complete: content ported, links repointed, legacy docs + facet_catalogue.org + regenerate_facet_inventories.py deleted, deploy_site green, tangle drift confirmed limited to intentional edits. |
| Waiting on   | Nothing.                       |
| Next         | Close task; PR.  |
| Last touched | 2026-07-16                               |

* Acceptance

- Every legacy doc's non-placeholder prose is ported to its corresponding ores.* node BEFORE deletion (group→technical_space, facet→facet); a content audit confirms nothing useful is lost. — DONE
- facet_catalogue.org + 18 legacy docs deleted; grep finds no inbound links to the deleted IDs; old regenerate_facet_inventories.py removed; deploy_site is green; full regeneration byte-identical. — DONE. deploy_site ran clean (no warnings/broken links/unresolved links in the log); a full =tangle_codegen_templates= pass showed drift only in the two files whose .org source I intentionally edited (doc_facet.org.mustache, doc_facet_group.org.mustache — the facet_catalogue.org boilerplate removal), confirming no unintended regeneration change.
- (Already satisfied by 386BED71, verify still true at close): core.py has no load_profiles/_load_profiles_from_org/_parse_facet_table. — verified, confirmed clean.

* Plan

1. Audited all 20 legacy docs (facet_catalogue.org + 19 facet/facet_group
   docs) for non-placeholder prose.
2. Discovered two docs described stale/duplicate ground, not gaps:
   =cpp_table.org= content was already absorbed into
   =ores.cpp.domain='s table_* archetypes; the =assets= group's two
   live templates (plantuml_er, shell_service_vars) already had full
   homes at =ores.plantuml.er= / =ores.shell.service= (their archetype
   docs already carried ported content — only the facet-level Summary
   needed filling).
3. Ported Summary / facet-projection / Mustache-incantations prose from
   every legacy doc into its corresponding =ores.*= technical-space or
   facet node (committed separately from deletion).
4. Bulk-repointed ~135 internal template-library "See also"/parent-facet
   references and the boilerplate "profile membership comes from
   facet_catalogue.org" intro line to point at each doc's own
   Archetypes table instead.
5. Repointed ~33 external files (recipes, MASD =projects/modeling/=
   docs, codegen architecture/schema docs, sprint 20 agile docs) from
   the legacy IDs onto the corresponding =ores.*= nodes; rewrote the
   stale "Assets Technical Space" section in
   =technical_spaces_other.org= to describe the real post-migration
   split (=ores.cpp.qt= / =ores.plantuml= / =ores.shell=).
6. Deleted facet_catalogue.org, the 19 legacy docs, and the now-dead
   =regenerate_facet_inventories.py= (targeted the deleted group docs;
   no live caller).
7. Grep-verified zero remaining inbound =id:= links to any deleted ID
   across =doc/= and =projects/=.
8. Deleted =cmake_service_src.mustache= (confirmed dead: zero
   profile/code references, no generated output anywhere) and its
   orphaned archetype doc.
9. Migrated the 23 flat =doc_<type>_org.org= literate sources onto the
   =ores.doc.*= physical-space graph as proper archetype docs
   (address-form filename, =#+type: archetype=, =#+facet:=), wired
   into their parent facets' Archetypes tables — closing the gap this
   task's own audit surfaced. =.mustache= output filenames unchanged;
   re-tangle confirmed zero drift.
10. Ran =deploy_site= (clean, no warnings) and a full
    =tangle_codegen_templates= pass (drift limited to the two
    intentionally-edited self-hosting scaffolds) to close the task.

* Notes

- =cmake_service_src.mustache= was confirmed dead (zero profile/code
  references, no generated output anywhere) and deleted along with its
  archetype doc, superseded by =cmake_component_service_src.mustache=.
- =plantuml_er.mustache= and =shell_service_vars.mustache= were left
  alone: their output files (an =_er.puml= diagram class of artefact,
  and the very-much-live =projects/ores.sql/service_vars.sh=, read by
  =env_init.py=/=compass_db.py=) mean deleting the templates would be a
  product decision about a standalone feature, not a duplicate-cleanup
  call — out of this task's scope. Both now carry an explicit "not
  wired into a live core.py mapping" note for a future audit.
- Migrated all 23 =ores.doc.*= per-template archetype docs (was a gap
  this task's own audit surfaced, not part of the original B10 scope;
  done as a follow-up in the same session).
- ~8 prose (non-link) mentions of =regenerate_facet_inventories.py= by
  name remain in older agile/recipe docs; left as-is (dead references
  in historical prose, not broken =id:= links).

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result
