:PROPERTIES:
:ID: 3D39EF0A-585F-4C66-B84F-433CAFA4DEB2
:END:
#+title: Task: Lightweight QuantLib-free tenor type
#+description: Parse tenor labels (3M, 2Y, 1W) and support simple std::chrono-based date-window overlap checks, with no business-day calendar machinery and no QuantLib dependency.
#+type: task
#+level: s1
#+filetags: :ir-rates-synthetic-generation:sprint_23:v0:
#+owner: marco
#+branch: feature/tenor-type
#+pr: 1522
#+blocked_on:
#+blocked_since:
#+created: 2026-07-03
#+updated: 2026-07-03
#+environment: eager_maxwell
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:3ECC4BA8-6ACA-4028-9E42-AE7F25FC3B98][IR Rates synthetic data generation]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

No tenor-string parser or date-window arithmetic exists anywhere in
the codebase outside QuantLib (confirmed by investigation —
=doc/knowledge/domain/time_structures_and_tenors.org= is purely
conceptual). Add a small, QuantLib-free tenor type: parse labels like
"O/N", "1W", "3M", "2Y" into a comparable duration, and support simple
date-window overlap checks (no business-day calendar/holiday
machinery — that level of precision is only needed for actual
bootstrapping, out of scope here). This is the prerequisite for the
tenor-collision validator task.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:3ECC4BA8-6ACA-4028-9E42-AE7F25FC3B98][IR Rates synthetic data generation]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-12                               |

* Acceptance

- Parses standard tenor labels (O/N, T/N, S/N, S/W, 1W-1Y per
  [[id:0AC88EB3-DB7F-4135-9DA6-0ED4583FEC29][Tenor]]'s convention table)
  into a comparable duration type.
- Given two (start, tenor) pairs, can answer whether their date
  windows overlap, using =std::chrono= only — no QuantLib dependency.
- Lives in =ores.marketdata= (revised from the original =ores.utility=
  placement — a tenor carries financial domain semantics, not generic
  utility logic; see Notes), since vol-surface pillar work will need it
  too.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

A first C++ attempt was written directly in =ores.utility= and then
discarded once the domain-knowledge review below concluded that location
was wrong. Restart implementation in =ores.marketdata= against the
=Tenor= and =Term Structure= knowledge docs once picked back up.

* Notes

An initial =ores.utility::domain::tenor= implementation (header, source,
tests) was written, then deleted: a tenor carries genuine financial
domain semantics (settlement conventions, per-curve-type interpretation),
which does not belong in the domain-agnostic =ores.utility= alongside
things like base32 converters — it belongs in =ores.marketdata=.

Before restarting implementation, a broader discussion clarified the
epic this task sits in and produced the domain-knowledge foundation the
implementation should now be written against:

- The IR epic was scoped into phases (single-curve/single-factor MVP →
  richer single-curve engines (G2++, DNS) → multi-curve/IBOR support →
  bootstrapping → treasuries), with this story remaining Phase 1
  (single-curve, RFR-style) as originally acceptance-criteria'd.
- A new knowledge cluster was written and cross-linked via org-roam,
  replacing the previous monolithic
  [[id:1427C01C-17C0-4D7E-AE55-BF16C414FD58][Time Structures and
  Tenors]] doc with a hub plus atomic docs:
  [[id:0AC88EB3-DB7F-4135-9DA6-0ED4583FEC29][Tenor]],
  [[id:3A6CF3BC-3378-4F84-B0B6-F5D6F488AC33][Term Structure]],
  [[id:6857871D-CFDE-4D9F-A83A-4A84CD3F4448][Horizon Date]],
  [[id:DF9EFAF9-1340-48F2-BFD9-1F4ED0ADBC76][Date Rolling and
  Business-Day Calendars]],
  [[id:4DF1F49A-9AB4-4B56-B170-808490071799][Broken Dates and Turn
  Points]], [[id:013BC5B0-9461-4AD4-A55E-374BCF34D8A4][IMM Dates]].
- A second new cluster, [[id:29F0F3B1-91A6-42BD-A53F-97F1E72A4FB9][Interest
  Rate Curve Families]], documents the Funding/Projection curve bundle
  ("curve family", deliberately not "surface" — the curve-type axis is
  discrete, not continuous), multi-curve construction/build order,
  storage grouping, a deferred multicurve-management UI requirement,
  onshore/offshore namespacing, and IBOR-vs-RFR benchmark types.
- Existing duplication (e.g. scalar/term-structure-parameter examples
  restated in =pricing_configuration.org=) was replaced with links to
  the new atomic docs.

Implementation, restarted after a branch resync uncovered that the
locally cached =feature/tenor-type= branch was stale (its remote had
been deleted on merge, and =compass task start= reused the stale
local ref instead of resyncing) — =compass pr sync= brought it back
onto =origin/main= before any new code was written, confirming
=ores.synthetic='s old =process*= files and a duplicate
=ores.marketdata.api/domain/i_stochastic_process.hpp= seen earlier in
the session were artefacts of that stale branch, not real dead code
on =main=.

=code-add-domain-type='s instructions were themselves stale (hand-written
JSON models under a =projects/ores.codegen/models/{component}/= that no
longer exists — the real format is org-mode =entity_org=/etc. docs, per
=projects/ores.codegen/docs/codegen_input_org_schema.org=, generated via
=compass codegen generate --model <path> --profile ...=, not
=run_generator.sh= directly). Fixed the skill doc. Codegen was then
found not to fit =tenor= regardless: none of its entity-model types
generate a behaviour-heavy value type (parsing, comparison operators,
computed methods) with no persistence or UI — they generate flat
data-carrying structs plus persistence/UI layers. Hand-authored
instead, following the same precedent already set by
=ores.analytics.quant='s own domain types (=currency_id=, =ccy_pair=,
...), which are hand-authored for the identical reason.

Implemented =ores::marketdata::domain::tenor= in
=ores.marketdata.api= (header =include/ores.marketdata.api/domain/tenor.hpp=,
source =src/domain/tenor.cpp=): parses O/N, T/N, S/N, S/W, and the
=<n><D|W|M|Y>= pattern; =end_date()= resolves a tenor to a calendar
date off a horizon date via =std::chrono= (clamping to the last valid
day of the month for month/year arithmetic that overflows, e.g. 31 Jan
+ 1M -> 28 Feb); =resolve_window()=/=windows_overlap()= support the
tenor-collision validator's half-open date-window overlap checks.
=domain_tenor_tests.cpp=: 10 test cases / 33 assertions, covering
parsing (valid and malformed labels), ordering, date arithmetic
(including the month-clamping edge case), and window overlap
(including the half-open boundary case). Full
=ores.marketdata.api.tests= suite (110 assertions / 24 test cases)
passes with no regressions.

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1522][#1522]] | [marketdata] Add QuantLib-free tenor type |
| [[https://github.com/OreStudio/OreStudio/pull/1517][#1517]] | [doc] Establish IR curve-family and time-structure/tenor knowledge clusters |
| [[https://github.com/OreStudio/OreStudio/pull/1411][#1411]] | [agile] Raise IR Rates synthetic data generation story |

* Review

PR #1517 review round (3 automated passes, issue-level comments, no line comments):

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | =multi_curve_grid_ui.org= title "Multicurve Management" doesn't match filename | multi_curve_grid_ui.org | Declined | Intentional — the title was deliberately renamed away from the literal source-screen name per design discussion; the filename/id stay stable for URL continuity, same pattern as other docs in the cluster (e.g. =fx_currency_pairs.org= titled "Currency pairs") |
| 2 | env-format-version-12 log row bundled into the "fix env init -> configure drift" commit, whose stated purpose is a rename fix | env_format_version_log.org | Declined | Both changes are legitimate env-tooling documentation from the same session; splitting now would require a history rewrite on a branch under review, which is avoided per convention |
| 3 | story.org's =Last touched= field and top-level =#+updated= were stale (2026-07-03/2026-07-11) despite this session's edits | story.org | Fixed | Updated both to 2026-07-12 |
| 1 | ou_process / validate_process_parameters cited as existing prior art but absent from the repo (reported independently by three review passes) | story.org, task_short-rate-process.org, task_tenor-collision-validation.org, task_tick-batch-publishing.org | Fixed | bd0104de5 — added explicit "Waiting on PR #1409" note; the references are real (live on PR #1409/story/gmm-improvements, which has since merged) |
| 2 | Filename typo: gemini_ir_curve_boostrapping.org vs its correctly-spelled .png companion | doc/analysis/ | Fixed | bd0104de5 — renamed |
| 3 | Missing required #+category: field | story.org | Fixed | bd0104de5 — added "feature" |
| 4 | Committed PNGs appear unreferenced from any .org file | doc/analysis/ | Declined | Intentional — raw source material for the analysis docs, not embedded content |
| 5 | "Waiting on PR #1409" note stale — PR #1409 merged into main (4be33f2b4) before this branch's rebase, but story.org still said "still open" | story.org | Fixed | Updated to "Nothing" with a note that #1409 merged prior to this branch's rebase |

* Result

Delivered a QuantLib-free =ores::marketdata::domain::tenor= type in
=ores.marketdata.api=: parsing of standard tenor labels (short-end
O/N/T/N/S/N/S/W plus the =<n><D|W|M|Y>= period pattern), calendar-date
resolution off a horizon date via =std::chrono=, and half-open
date-window overlap checks — the prerequisite the tenor-collision
validator task needs. 10 new test cases / 33 assertions; full
=ores.marketdata.api.tests= suite (110 assertions / 24 cases) green.
Delivered across two PRs: #1517 (domain-knowledge groundwork —
Interest Rate Curve Families and Time Structures/Tenor clusters) and
this task's implementation PR (not yet raised — see PRs table once
opened). Also fixed stale =code-add-domain-type= skill instructions
(JSON model path no longer exists; real format is org-mode entity
docs generated via =compass codegen generate=) discovered while
confirming codegen wasn't actually the right tool for this value
type.
