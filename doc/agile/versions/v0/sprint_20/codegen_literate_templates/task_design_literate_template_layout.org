:PROPERTIES:
:ID: 675045C6-61AE-405D-817F-8501064C64D2
:END:
#+title: Task: Design the literate template layout and tangle workflow
#+description: Initial task for: Convert codegen mustache templates to literate org-mode documents
#+type: task
#+level: s1
#+filetags: :codegen_literate_templates:sprint_20:v0:
#+owner: marco
#+branch: feature/codegen-literate-templates
#+pr: 1123
#+blocked_on:
#+blocked_since:
#+created: 2026-06-06
#+updated: 2026-06-06
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:1B686436-FBF6-47EB-87D5-5E13A509F7D1][Convert codegen mustache templates to literate org-mode documents]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Settle the design questions before any mass migration: how the 140
templates map onto the three-level literate hierarchy (template ←
facet doc ← group doc ← groups overview) — what counts as a facet
group, e.g. =cpp_domain_type_*= — where the org sources live, how
tangling runs (org-babel via
the existing =ores-babel.el= / tangle tooling), what the generated-file
header looks like inside a =.mustache= artefact, and how the zero-diff
drift check is wired (CI target and/or compass command). Prove the
design with a small pilot batch of templates and record the decisions
in the parent story's =* Decisions= section.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:1B686436-FBF6-47EB-87D5-5E13A509F7D1][Convert codegen mustache templates to literate org-mode documents]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-06-06                               |

* Acceptance

- Layout decision recorded: org doc granularity (per template vs per
  family) and source location, with rationale.
- Tangle workflow defined and runnable end-to-end on the pilot batch.
- Pilot batch of templates migrated; codegen regeneration is
  byte-identical (zero diff) with the tangled templates in place.
- Drift-check mechanism designed (zero-diff on tangle) and its CI /
  compass wiring agreed.
- Decisions distilled into the parent story's =* Decisions=; remaining
  migration sliced into follow-up tasks by template family.

* Plan

Survey results: 140 templates in =projects/ores.codegen/library/templates/=,
prefix families cpp (58), sql (26), doc (18), cmake (8), qt (2),
plantuml (1), shell (1). Consumed by =generator.py= via *pystache*;
=profiles.json= maps 23 profiles → template/output pairs. Standalone
={{! }}= comment lines are stripped by the mustache spec (pystache
complies; several templates already use them), so a generated-file
header is output-neutral.

Proposed decisions:

1. /Granularity:/ one org file per *facet* (template family by
   filename prefix, e.g. =cmake=, =cpp_domain_type=, =cpp_qt=,
   =sql_schema=, =doc=), each template a named
   =#+begin_src mustache :tangle <name>.mustache= block with prose
   documenting role, model inputs, output artefact. Group docs
   (=cpp=, =sql=, =doc=, =cmake=) and a top-level overview complete
   the capture's three-level hierarchy in the rollout task.
2. /Location:/ org sources co-located in
   =projects/ores.codegen/library/templates/=, tangling to sibling
   =.mustache= files.
3. /Tangle tooling:/ =ores-build-codegen-templates.el= (modelled on
   =ores-build-clang-format.el=) tangles every =.org= under
   =library/templates/=; CMake target =tangle_codegen_templates=.
4. /Header:/ first line of each tangled file is
   ={{! GENERATED FILE — tangled from <facet>.org; edit the org source. }}=
   — output-neutral under pystache.
5. /Drift check:/ run the tangle then =git diff --exit-code --
   projects/ores.codegen/library/templates= (CI job / compass hook in
   the rollout task).

Pilot batch: the =cmake= facet (8 templates). Verify (a) tangled
files match originals byte-for-byte apart from the header line, and
(b) full codegen regeneration is byte-identical (header stripped in
render).

* Notes

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1123][#1123]] | [codegen] Literate templates: cmake pilot, doc types, inventories |
| [[https://github.com/OreStudio/OreStudio/pull/1115][#1115]] | [agile] Start literate-templates story |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Task filename mismatched with design-focused title | task_implement_codegen_literate_templates.org | Accepted | Renamed to task_design_literate_template_layout.org in 196861563; id-links unaffected. |

* Result

Design settled and proven with the cmake pilot (PR [[https://github.com/OreStudio/OreStudio/pull/1123][#1123]], merged).
The five decisions — facet granularity, co-location, emacs/CMake
tangle tooling, output-neutral header, zero-diff drift check — are
recorded in the story's =* Decisions=. The pilot migrated all eight
=cmake_*= templates into =cmake.org=: tangled artefacts byte-identical
to HEAD modulo the header, pystache renders byte-identical. The
three-level hierarchy is scaffolded (five group docs + overview) and
facet inventories are generated by
=regenerate_facet_inventories.py= (=--check= for CI). Remaining
migration is sliced into the rollout task; tooling support landed via
the add-facet-doc-types task in the same PR.
