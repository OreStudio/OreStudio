:PROPERTIES:
:ID: 94A50F97-A7FC-4486-BD0B-0FB305998C01
:END:
#+title: Task: Sync C++ core codegen for party_status
#+description: Re-run domain, repository, service, and generator profiles for party_status; diff against repo; fix templates to match code.
#+type: task
#+level: s1
#+filetags: :commission_party_status:sprint_22:v0:
#+owner: marco
#+branch: feature/appraise-party-status
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-06-25
#+updated: 2026-06-25
#+environment:
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:8B3D478B-DD68-44B7-A8BF-DB50F76C9506][Commission: party_status]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Ensure the C++ core codegen templates (domain, repository, service, generator profiles on the refdata-cpp component) produce output that matches the repository for party_status. Known drift categories from the currency task (include guard format, method renames, display_order default init) should be checked and resolved for this entity. Fix templates to match code — not the reverse — unless the code has a demonstrable bug.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                                   |
| Parent story | [[id:8B3D478B-DD68-44B7-A8BF-DB50F76C9506][Commission: party_status]] |
| Now          | Nothing.                               |
| Waiting on   | Nothing.                               |
| Next         | Nothing.                               |
| Last touched | 2026-06-25                             |

* Acceptance

- codegen regenerate --component refdata-cpp --profile domain produces zero diff for party_status domain header (party_status.hpp).
- Same for repository, service, and generator profiles.
- Per-change-category decision recorded for any drift found.
- No C++ build regressions after any template fixes applied.
- Findings recorded in the Result section.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Ran domain, repository, service, and generator profiles against =refdata-cpp= for =party_status=.
Only one file had drift: =party_status.hpp=. All other files (mapper, repository, service, generator,
json_io, table, table_io) produced zero diff.

Drift in =party_status.hpp=: three changes generated by domain profile.

| Change | Decision | Rationale |
|--------+----------+-----------|
| Remove =#include "ores.utility/uuid/tenant_id.hpp"= | Reject | Include is required; kept with tenant_id field |
| Remove =tenant_id= field | Reject — template bug | Mapper (party_status_mapper.cpp:34,54) directly accesses =r.tenant_id= and =v.tenant_id=; removing the field breaks compilation. Template incorrectly drops tenant_id for multi-tenant entities. |
| =int display_order;= → =int display_order = 0;= | Accept | Avoids UB on uninitialised int; matches pattern accepted for currency auxiliaries |

Spurious files written to =projects/ores.refdata.api/= and =projects/ores.refdata.core/= (wrong output path,
pre-existing template bug noted in currency task) were deleted and not committed.

Net change: one-line fix to =party_status.hpp= (=display_order = 0=). Template bug (tenant_id removal for
multi-tenant entities) noted — separate issue, not fixed here.

