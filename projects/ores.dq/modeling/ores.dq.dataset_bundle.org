:PROPERTIES:
:ID: 2DC7CB2C-EF21-4E55-BB05-07F42DF5BD8B
:profile: uuid-surrogate-lookup
:END:
#+title: ores.dq.dataset_bundle
#+description: Codegen entity model for the dq =dataset_bundle= table. Drives the C++ domain class, mapper, repository, service, generator, Qt UI, and SQL schema generation from a single literate source.
#+type: ores.codegen.entity
#+component: dq
#+filetags: :model:entity:dq:
#+brief: A named collection of datasets designed to work together.
#+entity_singular: dataset_bundle
#+entity_plural: dataset_bundles
#+entity_title: Dataset Bundle
#+created: 2026-06-01
#+updated: 2026-06-01

Installing a bundle gets the system into a ready state with a coherent set
of reference data. Bundles provide a way to group related datasets that
should be installed together.

Examples:
- "slovaris": Synthetic reference data for development and testing
- "base": Industry-standard reference data (ISO + FpML) for production
- "crypto": Base system plus cryptocurrency reference data

* Flags
:PROPERTIES:
:schema:        public
:product:       ores
:component:     dq
:END:

* Columns

** id
:PROPERTIES:
:type:      uuid
:cpp_type:  boost::uuids::uuid
:primary_key: true
:END:

UUID uniquely identifying this bundle.

This is the surrogate key for the bundle.

** code
:PROPERTIES:
:type:     text
:cpp_type: std::string
:natural_key: true
:END:

Unique code for stable referencing.

Examples: "slovaris", "base", "crypto".

#+begin_src cpp :name generator
std::string(faker::word::noun()) + "_bundle"
#+end_src

** name
:PROPERTIES:
:type:     text
:cpp_type: std::string
:natural_key: true
:END:

Human-readable name for the bundle.

#+begin_src cpp :name generator
std::string(faker::word::adjective()) + " " + std::string(faker::word::noun()) + " Bundle"
#+end_src

** description
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: false
:END:

Detailed description of the bundle's contents and purpose.

#+begin_src cpp :name generator
std::string(faker::lorem::sentence())
#+end_src

* SQL

** Flags
:PROPERTIES:
:tablename: ores_dq_dataset_bundles_tbl
:END:

* C++

** Flags
:PROPERTIES:
:subcomponent:    api
:END:

** Repository
:PROPERTIES:
:tenant_read_scope: shared
:entity_singular_short: bundle
:entity_plural_short:   bundles
:entity_singular_words: dataset bundle
:entity_plural_words:   dataset bundles
:END:

** Domain includes

#+begin_src cpp :name includes
#include <chrono>
#include <string>
#include <boost/uuid/uuid.hpp>
#+end_src

** Entity includes

#+begin_src cpp :name includes
#include <string>
#include "sqlgen/Timestamp.hpp"
#include "sqlgen/PrimaryKey.hpp"
#+end_src

** Conventions
:PROPERTIES:
:iterator_var: b
:END:

** Table display

| column      | header      |
|-------------+-------------|
| code        | Code        |
| name        | Name        |
| description | Description |
| modified_by | Modified By |
| version     | Version     |

# No "** Qt" drawer here on purpose: dataset_bundle's Qt UI
# (DatasetBundleController et al.) is hand-maintained directly in
# ores.qt.dq. See ores.dq.catalog.org's matching comment for the full
# story of why a drawer was added and reverted here.

** Custom repository methods

* See also

- [[id:AAF28605-81BE-4B7F-9B6E-7B9B1D99D7C3][ores.dq]] — component group overview.
