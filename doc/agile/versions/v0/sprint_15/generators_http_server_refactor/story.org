:PROPERTIES:
:ID: 4837265A-934C-4CFC-9F7A-EDC3F86F37C6
:END:
#+title: Story: Generators to api layer + http.server split
#+description: Move synthetic generators from core to api across IAM / refdata / trading / reporting / compute; move HTTP route handlers into domain core modules; ores.http.server retains generic server infrastructure only.
#+type: story
#+level: s2
#+filetags: :refactor:generators:http:architecture:sprint_15:v0:
#+created: 2026-05-20
#+updated: 2026-05-20
#+todo: BACKLOG STARTED | DONE ABANDONED

This page documents a [[id:699A8D45-B67E-4D3E-9206-24FA17C51ADA][story]] in [[id:03D189E0-1960-4F19-B71F-EE7C8D0941A7][Sprint 15]]. It captures the goal, current status, acceptance criteria, and the tasks that compose it.

* Goal

Close the api/core migration by relocating two cross-cutting
concerns into the right modules: synthetic generators (api) and
domain-specific HTTP route handlers (core).

* Status

| Field         | Value                                                  |
|---------------+--------------------------------------------------------|
| State         | DONE                                                   |
| Parent sprint | [[id:03D189E0-1960-4F19-B71F-EE7C8D0941A7][Sprint 15]] |
| Now           | Completed 2026-03-25.                                  |
| Waiting on    | None.                                                  |
| Next          | None.                                                  |
| Last touched  | 2026-03-25                                             |

* Acceptance

- Generators moved to api layers across trading + reporting + iam + refdata + compute.
- auth_session_service relocated from iam.core to iam.api.
- HTTP route handlers moved into domain core modules.
- ores.http.server retains generic server infrastructure only.
- Generator directory standardised to generators/ (plural).
- faker-cxx added as private dep on affected api libs.

* Tasks

| Task | State | Start | End | Description |
|------+-------+-------+-----+-------------|
| [[id:AFCD8728-5BD6-4FA5-8AA6-5EB5CD6B78F0][Move generators to api layer; split http.server]] | DONE | 2026-05-20 | 2026-03-25 | Synthetic generators relocated from core to api layers across ores.trading + ores.reporting + ores.iam + ores.refdata + ores.compute (+ tests); auth_session_service relocated from ores.iam.core to ores.iam.api (in-memory, no core deps); domain-specific HTTP route handlers (iam, refdata, variability, assets) moved into their respective domain core modules; ores.http.server now exclusively manages generic server infrastructure (compute_routes only remaining domain-specific route); generator directories standardised to generators/ (plural); faker-cxx added as private dep on affected api libs. |

* Decisions

- /Generators in api layer/ :: generated values are part of the contract surface; tests + clients can use them.
- /HTTP routes near the domain core/ :: removes the cross-component coupling that http.server-monolith created.

* Out of scope

- Splitting ores.http further (already covered by the env-isolation story).

* See also

- [[id:7631C4D3-43C5-4238-A180-337BEA863C7F][API/core architecture migration]] — sibling.
