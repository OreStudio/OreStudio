:PROPERTIES:
:ID: B4526411-7FDC-48E8-A346-DA69B5F66BDD
:END:
#+title: Task: pair_code inline flag icon renders tiny regardless of keyFlagIconSize()
#+description: The QLineEdit-hosted pair-code flag (set_line_edit_flag_icon in FlagIconHelper.hpp) stays small no matter what QSize is passed, unlike QComboBox/QTableView icons which correctly scale. Attempted fix (reaching into the internal QToolButton and forcing setFixedSize/updateGeometry) made no visible difference and was reverted -- needs proper investigation, parked for now.
#+type: task
#+level: s1
#+filetags: :sprint_24:v0:
#+owner: marco
#+branch: feature/currency-pair-code-flag-icon-size-unfixed
#+pr: 1692
#+blocked_on:
#+blocked_since:
#+created: 2026-07-09
#+updated: 2026-07-21
#+environment: jolly_knuth
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in [[id:F90CDE0A-9813-407C-A145-30BA001FAAA8][Sprint 22 leftover cleanup]]. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The pair-code inline flag on =CurrencyPairDetailDialog='s Pair Code
field (and any other =has_key_flag_icon= entity's key field) stays
visibly tiny regardless of the =QSize= passed to =keyFlagIconSize()=,
unlike the same flags rendered via =QComboBox= (Base/Quote/Settlement
Currency) or =QTableView= (list grids), which correctly respect
=single_flag_icon_size()=/=currency_pair_icon_size()=.

Root mechanism: =set_line_edit_flag_icon()=
(=projects/ores.qt/api/include/ores.qt/FlagIconHelper.hpp=) adds the
icon via =QLineEdit::addAction()=, which Qt renders through an
internal =QToolButton= — reaching into it via
=findChild<QToolButton*>()->setIconSize(...)= is the documented
approach but had no visible effect in testing. A follow-up attempt
that also forced =setFixedSize()= and =updateGeometry()= on both the
button and the line edit *also* made no difference and was reverted
(too much complexity for an unconfirmed fix) — see
=set_line_edit_flag_icon()='s current comment.

Needs proper investigation: attach a debugger or add temporary
logging to confirm whether =findChild<QToolButton*>()= is even
finding the right widget (timing — is it created synchronously by
=addAction()=, or lazily on first paint?), and whether the style in
use has its own icon-size override that's winning over the widget
property.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:F90CDE0A-9813-407C-A145-30BA001FAAA8][Sprint 22 leftover cleanup]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-09                               |

* Acceptance

-

* Plan

Confirmed via a live repro (test scenario below) that the icon
rendered by =QLineEdit::addAction()= + =findChild<QToolButton*>()=
stayed tiny no matter what =iconSize= was requested — the internal
=QLineEditIconButton='s displayed icon size is bound by layout
metrics =QLineEdit= computes once when the action is added and never
revisits, so a later =setIconSize()= on the button has no visible
effect. Rather than continue fighting that internal, undocumented
behaviour, replaced the whole action-button mechanism with an overlay
approach:

- =set_line_edit_flag_icon()= (=FlagIconHelper.hpp=/=.cpp=) now
  paints the icon on a plain =QLabel= parented to the =QLineEdit=,
  sized exactly to the requested =iconSize=, with
  =QLineEdit::setTextMargins()= reserving room for it (a call
  =QLineEdit= *does* relayout correctly).
- A small =KeyFlagLabelPositioner= event filter keeps the label
  vertically centred and left-anchored whenever the line edit
  resizes.
- =DetailDialogBase::keyFlagAction_= (=QAction*=) became
  =keyFlagIconLabel_= (=QLabel*=) — the only caller of
  =set_line_edit_flag_icon()=.

* Notes

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1692][#1692]] | [ores.qt] Fix pair_code inline flag icon rendering tiny |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Unused =<QResizeEvent>= include | FlagIconHelper.cpp | Accepted | Removed; only =event->type()= is checked, no cast to =QResizeEvent*=. |
| 2 | Centring math duplicated in =set_line_edit_flag_icon()= and =KeyFlagLabelPositioner::reposition()= | FlagIconHelper.cpp | Accepted | Exposed =reposition()= publicly and call it from both the initial-placement and resize paths; positioner instance is stashed via a =QLabel= property since it has no =Q_OBJECT= macro to support =findChild()=. |
| 3 | =setTextMargins()= unconditionally clobbers other margins | FlagIconHelper.cpp | Accepted | Added a comment documenting that this function is the sole owner of the field's text margins. |
| 4 | Initial label position could be off before the first resize event | FlagIconHelper.cpp | Declined | Theoretical edge case; the QA scenario confirms correct rendering on dialog open in practice, and =.ui=-form widgets already have a laid-out height by the time =updateFlagDisplay()= first runs. |
| 5 | Discarded =new KeyFlagLabelPositioner(...)= pointer | FlagIconHelper.cpp | Declined | Standard Qt parent/child idiom -- lifetime is owned by the label, no leak. |

* Result

Fixed by replacing the =QLineEdit::addAction()=/=QToolButton=
mechanism with an overlay =QLabel= in =set_line_edit_flag_icon()=
(see =* Plan=). Verified with
[[id:E1B1F121-ECAA-4C67-9D6E-AFBA1DA709F0][Test Scenario: pair_code inline flag icon renders at full size]]:
FAILED against the pre-fix build (icon visibly tiny, screenshot
attached to the scenario), PASSED after the fix (icon renders at the
full requested size, matching the Base/Quote currency combo flags).
