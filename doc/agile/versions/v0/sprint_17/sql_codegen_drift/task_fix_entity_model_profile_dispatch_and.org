:PROPERTIES:
:ID: dc845b65-c7c0-4156-afff-ec968079cfce
:END:
#+title: Task: Fix entity model profile dispatch and composite natural-key indexes
#+description: - Add `schema` model type to the `sql` profile in `profiles.json` so plain entity models (`*_entity.json`) are dispatche
#+type: task
#+version: 2
#+level: s1
#+filetags: :sql_codegen_drift:sprint_17:v0:
#+owner: marco
#+branch:
#+pr: https://github.com/OreStudio/OreStudio/pull/758
#+blocked_on: none
#+blocked_since:
#+created: 2026-05-22
#+updated: 2026-05-22
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31c868b9-306e-4282-ba8c-07e647021b34][task]] in the [[id:25f0a34b-c547-4550-943b-e8f517eb0f6c][SQL codegen drift remediation]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Add the schema model type to the sql profile to enable automatic dispatch for plain entity models and fix composite natural-key index generation in the Mustache template.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:25f0a34b-c547-4550-943b-e8f517eb0f6c][SQL codegen drift remediation]] |
| Now          | Completed 2026-05-17.               |
| Waiting on   | Nothing.                               |
| Next         | None.                                  |
| Last touched | 2026-05-22                               |

* Acceptance

- Add the schema model type to the sql profile to enable automatic dispatch for plain entity models and fix composite natural-key index generation in the Mustache template.
- PR #758 merged and landed on main.

* Plan

* Notes

** Summary

- Add =schema= model type to the =sql= profile in =profiles.json= so plain entity models (=*_entity.json=) are dispatched to =sql_schema_table_create.mustache= by the profile system — eliminating the need for any special-case skipping logic in the shell script
- Update =generate_refdata_schema.sh= to use =--profile sql= instead of an explicit =--template= flag, making the profile system handle model-type dispatch automatically
- Fix =sql_schema_domain_entity_create.mustache= + =generator.py=: when a domain entity declares multiple natural keys, the template was generating one unique index per key instead of a single composite unique index covering all keys together

The composite-index bug affects 10 entities (book, business_unit, party, counterparty, party_identifier, counterparty_identifier, party_contact_information, counterparty_contact_information, tenant, dataset_bundle). The fix pre-computes =has_multiple_natural_keys=, =natural_keys_composite_columns=, and =natural_keys_composite_name= in the generator and branches in the template on that flag.


* Result

Landed.
