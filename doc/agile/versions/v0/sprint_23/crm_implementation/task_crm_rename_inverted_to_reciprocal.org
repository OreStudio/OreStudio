:PROPERTIES:
:ID: A4F67A24-D6B1-42D8-A842-4A094E2C84DA
:END:
#+title: Task: Rename CRM 'inverted' concept to 'reciprocal' across code, protocol, UI, and docs
#+description: Rename the CRM 1/rate backfill concept from 'inverted' to 'reciprocal' end-to-end -- UI labels/tooltips, the inverted field on crm_rate_item/get_crm_rates_request, crm_client/crm_rate_display_service/crm_rate_formatter identifiers, Qt button/handler names, the shell --inverted flag, ores.analytics.quant's rate_inverter, and every doc/recipe/task that names it -- to match FX market convention and distinguish it from derived/triangulated (cross-rate) computation, which is a different concept entirely.
#+type: task
#+level: s1
#+filetags: :crm_implementation:sprint_23:v0:
#+owner: marco
#+branch: feature/crm-rename-inverted-to-reciprocal
#+pr: 1623
#+blocked_on:
#+blocked_since:
#+created: 2026-07-16
#+updated: 2026-07-16
#+environment: clever_dijkstra
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:B38B3869-02FD-4CC7-99BD-9A77904ACA19][Cross-rates matrix (CRM)]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

CRM's 1/rate backfill (a pair with no direct quote served from its
reverse pair's computed 1/rate) is currently named "inverted"
everywhere -- =crm_rate_item.inverted=, the Qt "Show Inverted"
toggle, the =--inverted= shell flag, =rate_inverter=, tooltip text
"Computed inverse (1/rate)". "Reciprocal" is the correct FX-market
term for this (the reciprocal of EUR/USD is USD/EUR) and cleanly
distinguishes it from *derived*/triangulated rates (this codebase's
existing term for cross-rate computation via an intermediate
currency -- see =crm_rate_item='s own doc comment) -- a different
concept "inverted" was at risk of being confused with. Rename the
concept end-to-end: user-visible strings, code identifiers, the
=get_crm_rates_request=/=crm_rate_item= protocol field, and every
doc/recipe/task that names it.

Scope note: the =inverted= field is on a NATS wire protocol
(=marketdata.v1.crm.rates=) with a boost/rfl JSON payload -- renaming
it is a breaking wire-format change for anything that has already
captured a payload sample (docs, recipes with =#+RESULTS= blocks),
but not a DB schema change (nothing persists an "inverted" column).
No external/third-party consumer of this subject exists yet, so a
straight rename is safe rather than needing a deprecation shim.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | STARTED                              |
| Parent story | [[id:B38B3869-02FD-4CC7-99BD-9A77904ACA19][Cross-rates matrix (CRM)]] |
| Now          | Rename applied across quant/marketdata/qt/shell; build+tests green. |
| Waiting on   | Manual Qt smoke, then PR.              |
| Next         | Manual smoke test, then raise PR.      |
| Last touched | 2026-07-18                               |

* Acceptance

- Protocol (=ores.marketdata.api/messaging/crm_protocol.hpp=):
  =crm_rate_item.inverted= -> =reciprocal=,
  =get_crm_rates_request.inverted= -> =reciprocal=; doc comments
  updated to say "reciprocal" throughout.
- =ores.analytics.quant=: =rate_inverter= class/file renamed to
  =rate_reciprocator= (or equivalent -- exact name decided at
  implementation time), including its test file; any comment/log text
  saying "invert"/"inverse" updated.
- =ores.marketdata.service=: =crm_ingest_bridge=, =crm_handler= and
  their tests updated wherever they reference the renamed field/type.
- =ores.marketdata.client=: =crm_client=, =crm_rate_display_service=,
  =crm_rate_formatter= (and their tests) -- any parameter/field/local
  named =inverted= renamed to =reciprocal=; tooltip text "Computed
  inverse (1/rate)" -> "Computed reciprocal (1/rate)".
- =ores.qt.marketdata=: =CrmCrossRatesMatrixMdiWindow= -- "Show
  Inverted" button label/tooltip -> "Show Reciprocal",
  =showInvertedButton_= -> =showReciprocalButton_=,
  =onShowInvertedToggled= -> =onShowReciprocalToggled=, the
  =QSettings= key (=showInverted=) migrated or renamed with care for
  existing users' saved window state (decide at implementation time
  whether a silent key-rename losing the old preference is acceptable
  given no released version depends on it yet).
- =ores.shell=: =crm rates=' =--inverted= flag -> =--reciprocal=
  (breaking flag rename, acceptable pre-1.0).
- Docs: every doc/recipe/task under
  =doc/agile/versions/v0/sprint_23/crm_implementation/= and
  =doc/recipes/shell/marketdata/crm_rates.org= that mentions
  "inverted" updated to "reciprocal", including any captured
  =#+RESULTS= example output showing the old flag/field name.
- Full build + =ores.marketdata.client.tests=/
  =ores.analytics.quant.tests=/=ores.marketdata.service.tests= green
  after the rename; a quick manual smoke (=crm rates ... --reciprocal=
  and the Qt button) confirms behaviour is unchanged, only naming.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

Mechanical rename of the CRM 1/rate-backfill concept only --
=CurrencyPairDetailDialog='s unrelated "inverted pair" duplicate-reject
feature and =rate_engine='s "sign inverts" comment (log-rate delta
math) were both left alone as distinct concepts, per the task's own
scope note.

- =ores.analytics.quant=: =rate_inverter= -> =rate_reciprocator=
  (file + class, both include/src and its test file); =allow_invert=
  -> =allow_reciprocal=; =crm_rate_view.inverted= -> =reciprocal=.
- =ores.marketdata.api=: =crm_protocol.hpp='s =crm_rate_item.inverted=
  and =get_crm_rates_request.inverted= -> =reciprocal=.
- =ores.marketdata.service=: =crm_ingest_bridge=, =crm_handler= and
  their tests updated to the renamed field/parameter.
- =ores.marketdata.client=: =crm_client=, =crm_rate_display_service=,
  =crm_rate_formatter= (+ tests) -- =inverted= parameters/fields ->
  =reciprocal=; tooltip text updated.
- =ores.qt.marketdata=: =CrmCrossRatesMatrixMdiWindow= -- "Show
  Inverted" -> "Show Reciprocal" (button label/tooltip/handler/member),
  =QSettings= key =showInverted= -> =showReciprocal= (silent rename,
  acceptable pre-1.0, no released version depends on the old key).
- =ores.shell=: =crm rates='s =--inverted= flag -> =--reciprocal=.
- Docs: the CRM shell recipes under =doc/recipes/shell/marketdata/crm/=
  were checked and contain no "inverted" references needing update.
  The sprint's own DONE task/scenario journals were left as historical
  record of what was true when that work happened, rather than rewritten.
- Full build of the affected targets (=ores.analytics.quant.lib=,
  =ores.marketdata.client.lib=, =ores.marketdata.service.lib=,
  =ores.qt.marketdata.lib=, =ores.shell.lib=) plus their =.tests=
  binaries succeeded; =[rate_reciprocator]=, =[rate_delta_tracker]=,
  =[crm_rate_formatter]=, =[crm_rate_display_service]= and =[crm]=
  (marketdata.service) test tags all green.

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
| [[id:996E0E3B-5389-4648-B153-7F8487591133][Verify CRM 'Show Reciprocal' after the inverted-to-reciprocal rename]] | PENDING | Re-run after CSV reciprocal-column fix; also covers --reciprocal shell flag and Live/Stale/Disconnected colours now. |
| [[id:D3215450-DE2A-4D5E-8DEE-25A7D78785CE][Verify Market Simulator feed-binding auto-creation and running-status badge colours]] | PENDING | Covers the two bugs found during manual smoke testing (feed_binding auto-creation, badge colours). |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1623][#1623]] | [marketdata,analytics.quant,qt,shell] Rename CRM 'inverted' concept to 'reciprocal' |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Tooltip "Computed inverse (1/rate)" not renamed per acceptance criteria | crm_rate_formatter.cpp, crm_rate_formatter_tests.cpp | Accepted | Renamed to "reciprocal"; test updated. |
| 2 | Doc-comment sweep incomplete ("inversion"/"post-inversion"/"inverse" left in several files) | crm_rate_view.hpp, crm_protocol.hpp, rate_reciprocator.hpp, rate_delta_tracker.hpp, crm_ingest_bridge.hpp, crm_commands.hpp, CrmRateCellWidget.hpp, and test names in rate_reciprocator_tests.cpp/app_crm_ingest_bridge_tests.cpp | Accepted | Full sweep completed; only the intentionally out-of-scope rate_engine.cpp "sign inverts" comment remains. |
| 3 | Grammar bug "For an reciprocal view" | crm_rate_view.hpp | Accepted | Fixed to "For a reciprocal view". |
| 4 | Qt tooltip/comment still said "inverse" | CrmCrossRatesMatrixMdiWindow.cpp | Accepted | Button tooltip and comment updated to "reciprocal". |
| 5 | Stray lambda parameter named `inv` | CrmCrossRatesMatrixMdiWindow.cpp | Accepted | Renamed to `reciprocal`. |
| 6 | `ensure_feed_binding()` (blocking NATS call) held `mu_` in the already_running branch, inconsistent with the started branch | feed_controller.hpp | Accepted | Restructured so both branches compute the outcome under the lock, release it, then call ensure_feed_binding() once outside the lock. |

* Result
