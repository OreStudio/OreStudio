:PROPERTIES:
:ID: 514472D1-ED85-447C-9DEB-F8BDE0F2FBB8
:END:
#+title: Task: Implement Hotfix: Replace setenv/unsetenv with ores.platform::environment in tests
#+description: Initial task for: Hotfix: Replace setenv/unsetenv with ores.platform::environment in tests
#+type: task
#+level: s1
#+filetags: :fix_windows_setenv_unsetenv_not_portable:sprint_25:v0:
#+owner: marco
#+branch: feature/fix-windows-setenv-unsetenv-not-portable
#+pr: 1876
#+blocked_on:
#+blocked_since:
#+created: 2026-08-06
#+updated: 2026-08-06
#+environment: eager_maxwell
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:4584CB9E-50DE-4B8F-833C-BDAE957FD944][Hotfix: Replace setenv/unsetenv with ores.platform::environment in tests]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Restore Windows build by replacing all raw setenv/unsetenv calls in tests with the portable ores.platform::environment wrapper.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:4584CB9E-50DE-4B8F-833C-BDAE957FD944][Hotfix: Replace setenv/unsetenv with ores.platform::environment in tests]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-08-06                               |

* Acceptance

- Windows (MSVC + Clang) CI goes green on all 12 affected translation units
- Linux (Clang/GCC) build remains green
- All affected test suites pass on Linux

* Plan

Root cause: =setenv=/=unsetenv= are POSIX-only functions (<cstdlib>).
On Windows (both MSVC and Clang), they are undeclared — the platform
uses =_putenv_s= instead. The 12 test files (1 nats, 11 service
config_parser) call =setenv=/=unsetenv= directly instead of using the
portable =ores::platform::environment= wrapper.

Fix: replace all raw calls with =environment::set_value= /
=environment::unset_value= from ores.platform, which already has
=#ifdef _WIN32= guards using =_putenv_s=. Drop =<cstdlib>= — no other
stdlib symbols are used in these files.

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1876][#1876]] | [ores.nats] Replace setenv/unsetenv with portable ores.platform::environment |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Include ordering: new include must sort among quoted includes per .clang-format | all 12 files | Accepted | Ran clang-format -i on all 12 files |

* Result

12 test files patched:
- =ores.nats/tests/config_nats_configuration_tests.cpp=: 3 =setenv= +
  3 =unsetenv= → =environment::set_value= / =environment::unset_value=
- 11× =*/service/tests/config_parser_tests.cpp=: 3 =unsetenv= each
  → =environment::unset_value=

=Dropped =<cstdlib>= from all 12 files — no other stdlib symbols were
in use. The =ores.platform::environment= wrapper (=environment.cpp:=61-75)
already has =#ifdef _WIN32= guards using =_putenv_s=.

Include ordering fixed per review via =clang-format -i=.

Verified: Linux CI all green (8/8 pass). Windows verification is on the
PR's CI — the fix resolves the =use of undeclared identifier 'setenv'=
and ='unsetenv'= errors that both MSVC and Windows Clang emit for these
POSIX-only functions.
