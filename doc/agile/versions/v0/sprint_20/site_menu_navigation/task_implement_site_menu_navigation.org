:PROPERTIES:
:ID: 42B291C9-55B2-4B3B-9FFC-DECB2E8AB82A
:END:
#+title: Task: Design and implement the site navigation menu
#+description: Initial task for: Replace site navbar with a structured menu
#+type: task
#+level: s1
#+filetags: :site_menu_navigation:sprint_20:v0:
#+owner: marco
#+branch: feature/site-menu-navigation
#+pr: 1164
#+blocked_on:
#+blocked_since:
#+created: 2026-06-07
#+updated: 2026-06-07
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:84CB6F89-8847-47B0-B844-3D754BF00276][Replace site navbar with a structured menu]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Replace the flat =site-html-preamble= string in =ores-build-site.el= with a
hierarchical dropdown menu; update =assets/style.css= with the required
CSS; and keep the graph-page nav injection in =ores-inject-site-nav= in
sync with the new markup.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                                   |
| Parent story | [[id:84CB6F89-8847-47B0-B844-3D754BF00276][Replace site navbar with a structured menu]] |
| Now          | Nothing.                               |
| Waiting on   | Nothing.                               |
| Next         | Nothing.                               |
| Last touched | 2026-06-07                               |

* Acceptance

- [x] =site-html-preamble= in =ores-build-site.el= uses semantic dropdown
  markup (=<ul>= / =<li>= with nested =<ul>= for submenus).
- [x] All existing nav items plus new additions (Compass, Board, Recipes,
  Knowledge) are present under the new structure.
- [x] Dropdown behaviour is CSS-only (=:hover= / =:focus-within=); no JS.
- [x] New CSS added to =assets/style.css=; no inline styles.
- [x] =ores-inject-site-nav= in =ores-build-site.el= updated to inject the
  same markup into the graph page.
- [x] =cmake ... --target deploy_site= completes without errors.
- [x] Spot-checked in a browser: dropdowns open, links resolve correctly.

* Plan

** Wireframe

Top bar (all pages):

#+begin_example
+--------------------------------------------------------------------------------+
|  Home  Project v  Architecture v  Developer v  Agile v  Resources v  [github] |
+--------------------------------------------------------------------------------+
#+end_example

Dropdown panels (visible on hover / focus-within):

#+begin_example
Project v        Architecture v    Developer v    Agile v      Resources v
+-------------+  +--------------+  +----------+  +----------+  +---------------+
| Orientation |  | System Model |  | Guide    |  | Overview |  | Manuals       |
| Compass     |  | MASD         |  | Recipes  |  | Board    |  | Downloads     |
| Product     |  +--------------+  | LLMs     |  +----------+  | Knowledge     |
| Roadmap     |                    +----------+                 | Knowledge     |
+-------------+                                                 |   Graph       |
                                                                +---------------+
#+end_example

** Implementation

1. Replace flat =site-html-preamble= in =ores-build-site.el= with =<div
   class='nav-group'>= / =<ul class='nav-dropdown'>= structure (done).
2. Add =.nav-group=, =.nav-group-label=, =.nav-dropdown= CSS to
   =assets/style.css=; add =position:relative= and =align-items:center=
   to =#site-nav= (done).
3. Expand menu with new pages: Compass (Project), Recipes (Developer),
   Agile group (Overview + Board dashboard), Knowledge (Resources) (done).
4. Build and spot-check in browser (=compass services start= then
   =cmake ... --target deploy_site= + local preview).
5. Commit and raise PR.

* Notes

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1164][#1164]] | [site] Replace flat navbar with hierarchical dropdown menu |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| R1 | =outline: none= removes keyboard focus ring (WCAG 2.4.7) | =assets/style.css= | Fixed | Replaced with =:focus-visible= rule; fixed in 7a4b1878d |
| R2 | Dropdown can overflow viewport at nav edges | =assets/style.css= | Fixed | Added =first-of-type= / =last-of-type= overrides; fixed in 7a4b1878d |
| R3 | No opacity transition on open/close | =assets/style.css= | Fixed | Switched to opacity + pointer-events approach; fixed in 7a4b1878d |
| R4 | =cursor: default= on interactive label | =assets/style.css= | Fixed | Changed to =cursor: pointer=; fixed in 7a4b1878d |
| R5 | Home link → =index.html= may not exist | =ores-build-site.el= | Accepted | =index.org= exists at repo root; confirmed by reviewer |
| R6 | Two "Recipes" labels in different groups | =ores-build-site.el= | Accepted | Unambiguous in context; confirmed acceptable by reviewer |

* Result

Replaced the flat 12-item =site-html-preamble= in =ores-build-site.el= with
a CSS-only hierarchical dropdown menu grouping items into five logical
sections: Project, Architecture, Developer, Agile, and Resources.  Four new
pages added (Compass, Board, Agile Recipes, Knowledge). Home link corrected
to =index.html=. Dropdown CSS added to =assets/style.css= (position,
opacity-based transition, =:focus-visible= ring, edge-group overflow fix).
=ores-inject-site-nav= inherits the new markup automatically. PR #1164
merged.
