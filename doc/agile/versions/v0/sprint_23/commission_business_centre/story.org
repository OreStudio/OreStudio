:PROPERTIES:
:ID: 2406AA9C-6B67-4B74-954E-2C04AE833FB9
:END:
#+title: Story: Commission: business_centre
#+description: Commission business_centre: verify Qt UI post-NATS, fix regressions, add manual chapter, capture shell/CLI/Wt/HTTP backlog.
#+type: story
#+level: s2
#+filetags: :product:refdata:commissioning:business_centre:sprint_23:v0:
#+created: 2026-05-29
#+updated: 2026-07-12
#+environment: jolly_knuth
#+todo: BACKLOG STARTED | DONE ABANDONED

This page documents a [[id:699A8D45-B67E-4D3E-9206-24FA17C51ADA][story]] in [[id:341C071C-3042-4386-BC14-0D52CE8E6250][Sprint 23]]. It captures the goal, current status, acceptance criteria, and the tasks that compose it.

* Goal

Commission =business_centre= across all access layers: verify the Qt UI end-to-end post-NATS
migration, fix any regressions found, and add the entity chapter to the user manual
(Qt windows only — shell and CLI commands are a horizontal layer handled by the
top-level Shell entity commands and CLI commands commissioning stories, not per-entity).
File backlog captures for Wt and HTTP support.

* Status

| Field         | Value                                  |
|---------------+----------------------------------------|
| State         | STARTED                              |
| Parent sprint | [[id:341C071C-3042-4386-BC14-0D52CE8E6250][Sprint 23]] |
| Now           | Not yet started.                       |
| Waiting on    | Nothing.                               |
| Next          | Break the story into tasks.            |
| Last touched  | 2026-07-12                               |

* Acceptance

- Qt list window (MDI): loads and displays records correctly.
- Qt detail window: edit and save round-trips without error.
- Qt history window: shows change history for the entity.
- Qt delete command: removes the record; history is preserved.
- Qt eventing: a change made in one connected session appears in a second session without manual refresh.
- Manual: entity chapter documents all Qt windows (shell and CLI sections added
  later, once the top-level Shell entity commands / CLI commands stories reach
  this entity).
- Backlog captures filed for shell, CLI, Wt, and HTTP support.
- All regressions found are fixed inline or filed as captures.
- Site builds cleanly.
- No cross-component leakage: business_centre's Qt CRUD code and plugin
  wiring live entirely in ores.qt/refdata under RefdataPlugin, not
  ores.qt/party under PartyPlugin.
- business_centre's insert and validate SQL functions carry
  =security definer= + =set search_path = public, pg_temp=, and the
  validate function's bootstrap guard filters to active rows only.

* Tasks

| Task | State | Start | End | Description |
|------+-------+-------+-----+-------------|
| [[id:183B2813-A7A8-4EC1-A43A-E37CCFF83C21][Appraise business_centre against the entity evaluation checklist]] | DONE | 2026-07-12 | 2026-07-12 | Score business_centre across all layers (SQL, domain, repository, service, generator, protocol, NATS eventing/handler, Qt) using the entity evaluation checklist; findings drive the rest of the story's tasks. |
| [[id:34E655ED-E560-4C87-A0D4-524B904B5C26][Fix business_centre SQL security-definer and bootstrap-guard gaps]] | BACKLOG | | | ores_refdata_business_centres_insert_fn() and ores_refdata_validate_business_centre_fn() are missing security definer + set search_path; the validate function's bootstrap pass-through doesn't filter by valid_to=infinity. Same bug class fixed in Commission: country. |
| [[id:0FCD3745-61CD-4644-BAE2-CBCD994448FE][Move business_centre Qt CRUD to RefdataPlugin]] | BACKLOG | | | business_centre's Qt CRUD code and plugin wiring live in ores.qt/party under PartyPlugin even though the C++ backend is already in ores.refdata. Relocate to ores.qt/refdata/RefdataPlugin, mirroring the Move book to refdata story. |
| [[id:49403E16-E495-436D-8EE9-9739F638D534][Sync codegen for business_centre across all profiles]] | BACKLOG | | | No live codegen model exists despite the SQL file's AUTO-GENERATED banner (appraisal finding 3) -- downgraded from "regenerate" to a by-hand drift/consistency check against the checklist criteria; authoring a fresh model is out of scope. |
| [[id:15F0D21E-EDE0-4834-9963-D32953CCF9A1][Verify business_centre Qt UI end-to-end post-NATS]] | BACKLOG | | | Exercise the business_centre Qt UI end-to-end (from its new RefdataPlugin location): MDI list window, detail window (edit/save round-trip), history window, delete command, and cross-session eventing. Fix any regressions found. |
| [[id:996277E2-DBF4-4FF6-9AB3-E8534F675238][Write the business_centre manual chapter]] | BACKLOG | | | Add the business_centre entity chapter to the user manual, documenting the Qt windows (list, detail, history, delete). |
| [[id:404A83BE-21BE-4AE5-9A98-A70346D26EFC][File Wt and HTTP backlog captures for business_centre]] | BACKLOG | | | File backlog captures for business_centre's Wt web UI and HTTP REST API support, since both are out of scope for this story (handled by the top-level Wt/HTTP commissioning stories). |

* Decisions

- Rescoped after the appraisal task surfaced four findings (SQL
  security/bootstrap gaps, wrong Qt plugin, no live codegen model): added
  two new tasks (SQL security fix, Qt-to-RefdataPlugin move) ahead of Qt
  verification, so verification happens once against the final location
  rather than twice. Mirrors the sequencing rationale in [[id:0E63013B-3B02-4968-A9EA-ED85C00EC7AF][Move book to
  refdata]].
- Authoring a fresh codegen model for business_centre is out of scope --
  materially bigger than "sync existing drift" and risks destabilising a
  currently-working entity mid-story. The sync-codegen task is downgraded
  to a by-hand consistency check instead; a proper model can be authored
  in a follow-up story if desired.
- business_unit and contact_type show the identical wrong-plugin pattern
  (also under PartyPlugin) but are out of scope here -- their own
  Commission stories should pick this up, or a shared capture filed.

* Out of scope

- Authoring a codegen entity model for business_centre (see Decisions).
- Relocating business_unit or contact_type's Qt code (separate stories).
- Portfolio's equivalent Qt-plugin relocation, if it has the same issue
  (not checked as part of this appraisal).
