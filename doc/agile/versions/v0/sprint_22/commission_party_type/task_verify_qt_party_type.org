:PROPERTIES:
:ID: 21E5D45B-9CAD-4146-8ECF-1ACDB6398508
:END:
#+title: Task: Verify party_type Qt UI end-to-end post-NATS
#+description: Verify the Qt list window, detail dialog, history dialog, delete, and eventing for party_type against a live environment post-NATS migration; fix any regressions found.
#+type: task
#+level: s1
#+filetags: :commission_party_type:sprint_22:v0:
#+owner: marco
#+branch: feature/verify-qt-party-type
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-06-25
#+updated: 2026-07-03
#+environment: solid_dirac
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:59583AFF-7657-4F79-BB7E-2086BCA04A91][Commission: party_type]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Verify the =party_type= Qt UI end-to-end against live services
post-NATS: list window, detail dialog (fields and validation), history
dialog, delete, and the =party_type_changed= eventing. Fix any
regressions found.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | STARTED                              |
| Parent story | [[id:59583AFF-7657-4F79-BB7E-2086BCA04A91][Commission: party_type]] |
| Now          | Unblocked: root cause was a legacy env's NATS server squatting on solid_dirac's ports, not a controller bug (see PR #1415). Service fleet is up clean (24/24). Resuming Qt UI verification. |
| Waiting on   | Nothing.                               |
| Next         | Launch the Qt client and drive party_type list/detail/history/delete/eventing. |
| Last touched | 2026-07-03                               |

* Acceptance

- List window loads party types from live services; paging works.
- Detail dialog round-trips create and edit with validation.
- History dialog shows correct versions and timestamps.
- Delete soft-deletes and the list reacts via eventing.
- A change made in one connected session appears in a second session without manual refresh.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

Attempted to start the service fleet twice (=compass services start=,
after =compass db recreate -y -k= against a freshly rebuilt
=linux-clang-debug-make= preset) to drive the Qt client against live
services. Both attempts: NATS starts fine, controller starts, launches
=ores.wt.service[0]=, then crashes moments later writing the phase
update — =ERROR: invalid byte sequence for encoding "UTF8": 0x99= on
the first attempt, =0x8f= on the second (different byte, same call
site) — see [[id:D48BEA76-F879-4C16-8310-933B7EAEBCDF][bug capture]] for
full analysis. Filed as a separate bug rather than debugged in-line
since it looks like memory corruption (ASAN/valgrind territory), not a
party_type-related regression. Blocking further progress on this task
until fixed.

*Update:* not memory corruption. A legacy, untracked =nats-server= from
the deleted =OreStudio.local2= checkout was squatting on solid_dirac's
NATS ports (42222/8222); solid_dirac's own NATS fatal-crashed on the
port conflict every time, but =compass services start='s readiness
probe was fooled because something was answering on 42222. Controller
and wt.service connected to the wrong broker, producing the corrupted
reads. Fixed in [[id:05243CFE-F838-4759-9956-9400D6C90FDC][a separate
infra story]] (PR #1415): killed the stray process and migrated NATS
ports to derive from =ORES_BASE_PORT=. Full 24-service fleet now starts
clean.

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result
