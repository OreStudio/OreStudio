:PROPERTIES:
:ID: 88E4746E-DB9A-4BDB-8C7E-5F4571D3024D
:END:
#+title: ores.cmake.service
#+description: Service-component src CMakeLists variant.
#+type: facet
#+model_types: component
#+level: cross
#+facet_group: ores.cmake
#+filetags: :codegen:physical-space:cmake:
#+created: 2026-06-28
#+updated: 2026-07-16

This page is the literate source for the =ores.cmake.service= facet of the
codegen template library. Each section documents one template and
holds its source in a =mustache= block tangled to the sibling
=.mustache= file consumed by =generator.py=. The =.mustache= files are
generated artefacts — edit this document, then run the tangle
(=compass build --direct tangle_codegen_templates= or
=projects/ores.lisp/src/ores-build-codegen-templates.el= directly).
Output paths and profile membership are documented in each template's row
of the Archetypes table below.

* Summary

One template: =src/CMakeLists.txt= for a =.service= component
(=component-service= profile). Splits sources into a library
(everything except =main.cpp=, linking the sibling core via
={{core_full_name}}.lib=) and the service executable (=main.cpp= only).

* The ores.cmake.service facet

A sibling variant of [[id:4A3D3B2C-B4EC-46D7-BFF1-816A41896199][ores.cmake.component]]'s =src= archetype, split out
because the service flavour has a two-target shape (library + exe)
the others don't. An earlier, standalone-service =src= variant with
no sibling-core dependency (=cmake_service_src.mustache=, the
pre-regroup shape) was confirmed dead — zero profile or code
references, no generated output anywhere — and deleted.

* Mustache incantations

No constructs beyond the group's shared conventions and the
={{core_full_name}}= interpolation for the sibling-core link.

* Templates

(One subheading per template: prose covering role, model inputs,
output artefact and profiles, followed by the source block. The first
line of every block is the output-neutral GENERATED FILE header:)

#+begin_example
,#+begin_src mustache :tangle <name>.mustache
{{! GENERATED FILE — tangled from <path to this doc>. Edit the org source. }}
...template source...
,#+end_src
#+end_example

* Archetypes

| Archetype | Description |
|-----------+-------------|
| [[id:CB65F43C-1504-454E-BC1B-968814AF7667][ores.cmake.service.src]] | =src/CMakeLists.txt= for a =.service= component (=component-service= profile): splits the sources into a library (everything except =main.cpp=, linking the sibling core via ={{core_full_name}}.lib=) and the service executable (=main.cpp= only). Output =projects/{component_full}/src/CMakeLists.txt=. |

* See also

- (Parent template group doc: =<group>_group.org=.)
- [[id:3B6B9F75-BDC9-4AE9-B16E-5A05FFC3A4B5][Codegen template library]] — the groups overview.
