This file contains notes on code review for code generated by claude.

* Analytics

- why does pricing engine type have a tenant but no party?
- is it a good idea to default tenants to system? maybe we should default to a
  well known invalid tenant and force users to setup tenant?

#+begin_src c++
    utility::uuid::tenant_id tenant_id = utility::uuid::tenant_id::system();
#+end_src

- not obvious what pricing_model_config is modeling. Seems to model the notion
  of the file for pricingengine.xml. We then say:
  "Natural key within a tenant (e.g. "Base SIMM", "AMC Monte Carlo")."
  Where is this name coming from?
- what is the config variant?
- we should have a table listing valid pricing model parameters and their types.
  We should check the parameters have valid values on insertion. UI should give
  you the available parameters for a model when you are configuring it, and have
  fields with the expected type.
- no tests for types (basic serialisation).
- why are log names so verbose? e.g. =pricing_engine_type_handler_lg()= replace
  with just =lg()=. Check templates.
- permissions are hard-coded. Each component should have a permission list with
  all constants:

#+begin_src c++
if (!has_permission(req_ctx, "analytics::pricing_engine_types:write")) {
#+end_src

- this seems like boilerplate for every messaging handler, can we factor it out:

#+begin_src c++
        BOOST_LOG_SEV(pricing_engine_type_handler_lg(), debug)
            << "Handling " << msg.subject;
        auto req_ctx_expected = ores::service::service::make_request_context(
            ctx_, msg, verifier_);
        if (!req_ctx_expected) {
            error_reply(nats_, msg, req_ctx_expected.error());
            return;
        }
        const auto& req_ctx = *req_ctx_expected;
        if (!has_permission(req_ctx, "analytics::pricing_engine_types:write")) {
            error_reply(nats_, msg, ores::service::error_code::forbidden);
            return;
        }
#+end_src

- why is the jwt verifier optional? would it not make more sense for it to have
  "modes"? allow all, strict.
- should we not have a "base" handler that has the typical members:

#+begin_src c++
    pricing_model_config_handler(ores::nats::service::client& nats,
        ores::database::context ctx,
        std::optional<ores::security::jwt::jwt_authenticator> verifier)
        : nats_(nats), ctx_(std::move(ctx)), verifier_(std::move(verifier)) {}
#+end_src

  would mean less boilerplate in each handler.
- each handler seems to have a series of types that we need to know of:

#+begin_src c++
        service::pricing_model_config_service svc(req_ctx);
        get_pricing_model_configs_response resp;
#+end_src
  and so on. it would be nice to have "typedefs" for these (e.g. service_type,
  response_type, etc). The code then just talks in terms of typedefs (whatever
  the modern c++ version is).

* global

- update UML diagrams.
- do a find grep for all stubs (=stub.hpp=) and remove them all. For tests,
  check to see if we have minimal testing as well (=stub_tests.cpp= can only be
  replaced once we have tests in place.
- it would be nice to be able to run the wizards to bootstrap system, tenant,
  party from ores.cli and ores.shell. Maybe we don't need both but at least one.

* DB

- we do not seem to drop the roles before recreating the database.
- missing pgtab extension.
- long identifiers:

psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/create/dq/dq_lei_relationships_artefact_create.sql:55: NOTICE:  identifier "dq_lei_relationships_artefact_relationship_start_node_node_id_idx" will be truncated to "dq_lei_relationships_artefact_relationship_start_node_node_id_i"
psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/create/dq/dq_lei_relationships_artefact_create.sql:61: NOTICE:  identifier "dq_lei_relationships_artefact_relationship_relationship_type_idx" will be truncated to "dq_lei_relationships_artefact_relationship_relationship_type_id"
psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/create/refdata/refdata_party_contact_informations_create.sql:77: NOTICE:  identifier "ores_refdata_party_contact_informations_party_contact_type_uniq_idx" will be truncated to "ores_refdata_party_contact_informations_party_contact_type_uniq"
psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/create/refdata/refdata_counterparty_contact_informations_create.sql:77: NOTICE:  identifier "ores_refdata_counterparty_contact_informations_counterparty_contact_type_uniq_idx" will be truncated to "ores_refdata_counterparty_contact_informations_counterparty_con"
psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/create/iam/iam_session_stats_create.sql:143: WARNING:  using non-immutable functions in continuous aggregate view may lead to inconsistent results on rematerialization

- can we have just a single notice informing detection of timescale db and no
  others. log very noisy.

- what is this:

psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/create/utility/utility_summary_functions_create.sql:39: NOTICE:  type "ores_utility_layer_summary_row" does not exist, skipping

- too many notices on create change reason categories.

--- Change Reason Categories ---
psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/populate/dq/dq_change_reasons_populate.sql:58: NOTICE:  Created change reason category: system

- same with permissions

--- IAM Permissions ---
psql:/home/marco/Development/OreStudio/OreStudio.local1/projects/ores.sql/populate/iam/iam_permissions_populate.sql:43: NOTICE:  Created permission: iam::accounts:create
