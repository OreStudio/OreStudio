# Validation Ignore Rules
#
# Format: WARNING_CODE entity_pattern
# - Lines starting with # are comments
# - Empty lines are skipped
# - Patterns support * wildcard (e.g., dq_*_artefact_tbl)
#
# Warning codes:
# - DROP_001: Table created but no drop script
# - DROP_002: Function created but no drop script
# - TEMPORAL_004: Missing version column
# - TEMPORAL_005: Missing modified_by column
# - TEMPORAL_006: Missing change_reason_code column
# - TENANT_001: Missing tenant_id column
# - TENANT_002: Missing tenant_id in primary key
# - TENANT_003: Missing tenant_id in EXCLUDE constraint
# - RLS_001: Table has tenant_id but no ENABLE ROW LEVEL SECURITY found
# - RLS_002: Table has party_id but no AS RESTRICTIVE party isolation policy
# - RLS_003: *_rls_policies_create.sql file not reachable from rls/rls_create.sql

# =============================================================================
# Change reason tables - intentionally missing change_reason_code
# These ARE the change reason tables, they can't reference themselves
# =============================================================================
TEMPORAL_006 ores_dq_change_reason_categories_tbl
TEMPORAL_006 ores_dq_change_reasons_tbl

# =============================================================================
# Lightweight junction tables - intentionally omit version
# These are simple relationship tables without full audit trail
# =============================================================================
TEMPORAL_004 ores_dq_dataset_dependencies_tbl

# =============================================================================
# System-managed tables - no user audit trail needed
# =============================================================================
TEMPORAL_004 ores_iam_permissions_tbl
TEMPORAL_005 ores_iam_permissions_tbl

# =============================================================================
# Artefact/staging tables - tenant_id not in PK by design
# These are temporary import tables where dataset_id is the primary identifier.
# tenant_id is carried for data flow to target tables, not for uniqueness.
# =============================================================================
TENANT_002 ores_dq_*_artefact_tbl

# =============================================================================
# Audit/publication tables - tenant_id not in PK by design
# These are append-only logs with UUID primary keys.
# tenant_id is present for filtering but not for uniqueness.
# =============================================================================
TENANT_002 ores_dq_dataset_publications_tbl
TENANT_002 ores_dq_bundle_publications_tbl

# =============================================================================
# Tenants table - tenant_id is always system_tenant_id by design
# All tenant records are owned by the system tenant. The tenant's own identity
# is stored in 'id', not 'tenant_id'. Including tenant_id in PK or EXCLUDE
# would be redundant since it's always the same value (system tenant).
# =============================================================================
TENANT_002 ores_iam_tenants_tbl
TENANT_003 ores_iam_tenants_tbl

# =============================================================================
# Tags table - tag_id UUID is globally unique by design
# tenant_id is used for filtering and name uniqueness within tenant,
# but not needed in PK/EXCLUDE since tag_id is already globally unique.
# =============================================================================
TENANT_002 ores_assets_tags_tbl
TENANT_003 ores_assets_tags_tbl

# =============================================================================
# Image tags junction table - temporal, but tag UUIDs are globally unique.
# tenant_id is present for RLS and filtering; the PK covers the essential
# uniqueness. EXCLUDE constraint tracks the tag/image pair over time.
# =============================================================================
TENANT_002 ores_assets_image_tags_tbl
TENANT_003 ores_assets_image_tags_tbl

# =============================================================================
# Geolocation reference table - tenant_id used for RLS but not a primary key
# column. The table has no conventional PK (it uses a GiST range index); the
# tenant_id + ip_range combination is sufficient for lookup queries.
# =============================================================================
TENANT_002 ores_geo_ip2country_tbl

# =============================================================================
# IAM temporal junction tables - tenant_id present for RLS but not in PK.
# The FK-linked UUIDs (account_id, role_id, permission_id) are globally unique,
# so tenant isolation is enforced via RLS rather than PK membership. The EXCLUDE
# constraint covers the entity pair + valid period; tenant_id is not needed
# there because the entity IDs already imply a single tenant.
# =============================================================================
TENANT_002 ores_iam_account_roles_tbl
TENANT_003 ores_iam_account_roles_tbl
TENANT_002 ores_iam_role_permissions_tbl
TENANT_003 ores_iam_role_permissions_tbl

# =============================================================================
# Login info table - single row per account_id; account_id is globally unique
# so tenant_id is not needed in the primary key.
# =============================================================================
TENANT_002 ores_iam_login_info_tbl

# =============================================================================
# TimescaleDB hypertable tables - tenant_id is present for RLS but excluded
# from the primary key by design. Hypertable PKs must include the partition
# column (a timestamp) and typically a UUID; adding tenant_id would add
# unnecessary width to the partition index without benefit.
# =============================================================================
TENANT_002 ores_iam_sessions_tbl
TENANT_002 ores_iam_session_samples_tbl
TENANT_002 ores_iam_auth_events_tbl
TENANT_002 ores_telemetry_logs_tbl

# =============================================================================
# Database info table - single-row system metadata, not tenant-specific.
# Records schema version, git commit and build environment at creation time.
# There is exactly one row per database instance; tenant_id has no meaning here.
# =============================================================================
TENANT_001 ores_database_info_tbl

# =============================================================================
# MQ metrics samples table - system-level time-series, not tenant-specific.
# pgmq queues are global (shared infrastructure); metrics_all() returns no
# tenant context. tenant_id has no meaning in this monitoring table.
# =============================================================================
TENANT_001 ores_mq_metrics_samples_tbl

# =============================================================================
# MQ queues table - supports party/tenant/system scopes with nullable tenant_id.
# For system-scope queues tenant_id is intentionally NULL; a non-null tenant_id
# column check is not applicable.
# =============================================================================
TENANT_001 ores_mq_queues_tbl

# =============================================================================
# MQ messages, archive, and stats tables - tenant_id not in PK by design.
# PKs are bigserial (messages) or (queue_id, timestamp) for hypertables.
# Isolation is enforced via RLS policies on the parent queue.
# =============================================================================
TENANT_002 ores_mq_messages_tbl
TENANT_002 ores_mq_message_archive_tbl
TENANT_002 ores_mq_queue_stats_tbl

# =============================================================================
# Scheduler job definitions table - tenant_id nullable for system-level jobs.
# System-scope jobs have tenant_id = NULL; EXCLUDE constraint uses nullable
# (tenant_id, party_id) scope which cannot include tenant_id in the
# standard non-null PK/EXCLUDE form.
# =============================================================================
TENANT_002 ores_scheduler_job_definitions_tbl
TENANT_003 ores_scheduler_job_definitions_tbl

# =============================================================================
# Scheduler job instances table - TimescaleDB hypertable on triggered_at.
# tenant_id present for RLS but excluded from PK by the same rationale as
# other hypertables; the serial PK is globally unique.
# =============================================================================
TENANT_002 ores_scheduler_job_instances_tbl

# =============================================================================
# Reporting junction tables - tenant_id not in PK/EXCLUDE by design.
# risk_report_config_id is a globally unique UUID that already implies a single
# tenant; tenant isolation is enforced via RLS rather than PK membership.
# The EXCLUDE constraint covers the config+scope pair over time; adding tenant_id
# would be redundant since the config UUID is globally unique.
# =============================================================================
TENANT_002 ores_reporting_risk_report_config_portfolios_tbl
TENANT_003 ores_reporting_risk_report_config_portfolios_tbl
TENANT_002 ores_reporting_risk_report_config_books_tbl
TENANT_003 ores_reporting_risk_report_config_books_tbl

# =============================================================================
# Compute platform reference and junction tables - global reference data.
# ores_compute_platforms_tbl is a tenant-independent code table (platform
# identifiers are universal, not per-tenant). ores_compute_app_version_platforms_tbl
# is a lightweight junction table that inherits tenant context from the parent
# app_version; tenant_id would be redundant here.
# =============================================================================
TENANT_001 ores_compute_platforms_tbl
TENANT_001 ores_compute_app_version_platforms_tbl

# =============================================================================
# Service samples table - system-level time-series, not tenant-specific.
# Heartbeats originate from internal services (not user requests); there is no
# tenant context. tenant_id has no meaning in this monitoring table.
# =============================================================================
TENANT_001 ores_telemetry_service_samples_tbl

# =============================================================================
# Controller tables - system-level service lifecycle management.
# These tables track the desired state and operational state of ORE Studio
# backend services. They are not tenant-specific; controller is a system-level
# component that runs outside the tenant isolation model.
# =============================================================================
TENANT_001 ores_controller_service_definitions_tbl
TENANT_001 ores_controller_service_instances_tbl
TENANT_001 ores_controller_service_events_tbl
TENANT_001 ores_controller_service_dependencies_tbl

# =============================================================================
# Market data TimescaleDB hypertables — observations and fixings.
# These tables use (id, date) as the primary key because TimescaleDB requires
# the partition column in the PK. GIST exclusion is incompatible with
# hypertables; bitemporality is enforced via partial unique index + trigger
# instead. version/modified_by/change_reason_code are intentionally absent:
# these are time-series ingest tables, not user-managed entities.
# =============================================================================
TEMPORAL_003 ores_marketdata_observations_tbl
TEMPORAL_003 ores_marketdata_fixings_tbl
TEMPORAL_004 ores_marketdata_observations_tbl
TEMPORAL_004 ores_marketdata_fixings_tbl
TEMPORAL_005 ores_marketdata_observations_tbl
TEMPORAL_005 ores_marketdata_fixings_tbl
TEMPORAL_006 ores_marketdata_observations_tbl
TEMPORAL_006 ores_marketdata_fixings_tbl
TENANT_002 ores_marketdata_observations_tbl
TENANT_002 ores_marketdata_fixings_tbl

# =============================================================================
# Workflow instances table - tenant_id present but not in PK by design.
# Uses a UUID primary key for global uniqueness. tenant_id is carried for RLS
# and filtering, not for uniqueness. Same pattern as other append-only tables.
# =============================================================================
TENANT_002 ores_workflow_workflow_instances_tbl

# =============================================================================
# Workflow steps table - tenant isolation via FK to parent instance.
# Steps have no direct tenant_id; they inherit tenant context from the parent
# ores_workflow_workflow_instances_tbl row via the workflow_id FK. RLS on the
# steps table uses a subquery join to enforce tenant isolation.
# =============================================================================
TENANT_001 ores_workflow_workflow_steps_tbl

# =============================================================================
# RLS_001 ignores — tables with tenant_id that intentionally skip RLS
# =============================================================================

# DQ artefact/staging tables — pipeline-internal staging, populated by DDL user.
# Not accessed by application users directly; no RLS needed.
RLS_001 ores_dq_*_artefact_tbl
RLS_001 ores_dq_artefact_types_tbl

# DQ pipeline and publication log tables — append-only, populated by the
# data quality pipeline. Tenant context is carried as a column for query
# filtering only; RLS would add overhead with no security benefit.
RLS_001 ores_dq_bundle_publications_tbl
RLS_001 ores_dq_dataset_publications_tbl

# DQ system tables — badge metadata and code domain lookups are managed by
# the DQ pipeline, not by application users.
RLS_001 ores_dq_badge_definitions_tbl
RLS_001 ores_dq_badge_mappings_tbl
RLS_001 ores_dq_badge_severities_tbl
RLS_001 ores_dq_code_domains_tbl

# IAM system enum/lookup tables — system-wide reference data, not user-created.
# Account types, tenant statuses, and tenant types are seeded at startup and
# never vary per tenant; RLS would add overhead with no benefit.
RLS_001 ores_iam_account_types_tbl
RLS_001 ores_iam_tenant_statuses_tbl
RLS_001 ores_iam_tenant_types_tbl

# IAM auth events — append-only audit log. The entire log is accessible to
# tenant admins; party-level isolation is not applied to audit trails.
RLS_001 ores_iam_auth_events_tbl

# Refdata lookup/enum tables — system-wide or tenant-seeded reference data.
# These are code tables (statuses, types, categories) with no per-party data.
RLS_001 ores_refdata_book_statuses_tbl
RLS_001 ores_refdata_business_unit_types_tbl
RLS_001 ores_refdata_contact_types_tbl
RLS_001 ores_refdata_party_categories_tbl
RLS_001 ores_refdata_party_id_schemes_tbl
RLS_001 ores_refdata_party_statuses_tbl
RLS_001 ores_refdata_party_types_tbl
RLS_001 ores_refdata_purpose_types_tbl
RLS_001 ores_refdata_rounding_types_tbl

# Reporting lookup tables — concurrency policy and report type codes are
# system-wide enumerations seeded at startup.
RLS_001 ores_reporting_concurrency_policies_tbl
RLS_001 ores_reporting_report_types_tbl

# MQ queue stats — aggregate metrics table, no user-created rows.
# Isolation is enforced on the parent queue via its own policies.
RLS_001 ores_mq_queue_stats_tbl

# Telemetry NATS sample tables — high-volume time-series from infrastructure
# monitoring. Not accessed by application users; no RLS needed.
RLS_001 ores_telemetry_nats_server_samples_tbl
RLS_001 ores_telemetry_nats_stream_samples_tbl

# Compute sample tables — time-series metrics from the BOINC compute grid.
# Not accessed by application users; no RLS needed.
RLS_001 ores_compute_grid_samples_tbl
RLS_001 ores_compute_node_samples_tbl

# Trading type/enum tables — lookup codes seeded at startup; system-wide.
RLS_001 ores_trading_business_day_convention_types_tbl
RLS_001 ores_trading_day_count_fraction_types_tbl
RLS_001 ores_trading_floating_index_types_tbl
RLS_001 ores_trading_leg_types_tbl
RLS_001 ores_trading_payment_frequency_types_tbl

# =============================================================================
# RLS_002 ignores — tables with party_id that intentionally skip party isolation
# =============================================================================

# IAM auth events — auth log; party_id records the session context for
# debugging, not for data isolation. The whole log is tenant-scoped.
RLS_002 ores_iam_auth_events_tbl

# MQ messages and archive — access is controlled by queue ownership (which
# queue a message belongs to), not by party_id. Custom queue-based policies
# are applied inline in the create file.
RLS_002 ores_mq_messages_tbl
RLS_002 ores_mq_message_archive_tbl

# MQ queue stats — aggregate metrics; no per-party row isolation needed.
RLS_002 ores_mq_queue_stats_tbl

# IAM account parties — this IS the party membership table. It must be
# readable before a party context is established (e.g. during login), so a
# party isolation policy would cause a chicken-and-egg deadlock. Tenant
# isolation (applied above in the create file) is the correct boundary here.
RLS_002 ores_iam_account_parties_tbl

