:PROPERTIES:
:ID: 7C44751E-54C0-4CB7-B489-1D3EC609A4E3
:END:
#+title: How do I configure the project?
#+description: Pick a CMake preset and configure — this is the once-per-checkout step before building.
#+type: recipe
#+level: cross
#+filetags: :cmake:build:configure:presets:recipe:
#+created: 2026-05-18
#+updated: 2026-05-18

For the preset menu and what each axis means, see [[id:5AAE6900-8488-4E22-9F31-A1B9D5320EFB][CMake setup]].

* Question

How do I configure ORE Studio with CMake?

* Answer

From the project root, pick a preset and configure with it:

#+begin_src sh :results verbatim
cmake --preset linux-clang-debug-ninja
#+end_src

For a release build:

#+begin_src sh :results verbatim
cmake --preset linux-clang-release-ninja
#+end_src

Configure once per preset; the preset's build directory is created at
=build/output/<preset>/=. To list available presets see
[[id:AADBEF95-AFD8-4DC0-A4EC-505E47B07F86][How do I list available presets?]].

** Re-configuring with options

To enable test logging or any other option, pass =-D<NAME>=<VALUE>= to
the same command. The build directory is reused; CMake re-configures
in place:

#+begin_src sh :results verbatim
cmake --preset linux-clang-debug-ninja -DORES_TEST_LOG_LEVEL=debug
#+end_src

* Script

The commands above. No wrapper.

* Tested by

CI workflows configure every preset on every push.

* See also

- [[id:5AAE6900-8488-4E22-9F31-A1B9D5320EFB][CMake setup]] — preset naming, build types.
- [[id:AADBEF95-AFD8-4DC0-A4EC-505E47B07F86][How do I list available presets?]]
- [[id:F176FCA1-68D5-420D-B076-75A65FEE5EBB][How do I build the system?]] — next step after configure.
