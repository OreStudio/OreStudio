:PROPERTIES:
:ID: 4E613004-5331-42C2-A654-56292F763095
:END:
#+title: Task: Support multiple named .env files, e.g. per remote host
#+description: Every compass command that needs environment or DB config (db recreate, services start, etc.) reads a single hardcoded .env at the repo root, with no way to point at a different target. Surfaced while deploying the containerized service runtime to Newton: compass db recreate had to be run against Newton's Postgres by temporarily editing the repo's own .env in place (ORES_DB_HOST, PGPORT), then restoring it afterward -- fragile and easy to forget to revert. Add support for named environment files, e.g. .env.newton alongside the default .env, and a way to select one per invocation (an --env-file flag or similar), so targeting a remote host's DB or services never requires mutating the working .env.
#+type: task
#+level: s1
#+filetags: :compass-quality-of-life-improvements-followups:sprint_25:v0:
#+owner: marco
#+branch: feature/support-multiple-env-files
#+pr: 1877
#+blocked_on:
#+blocked_since:
#+created: 2026-07-29
#+updated: 2026-07-29
#+environment: merry_newton
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:E599E5A0-84C9-4636-B478-5D1478688B9B][Compass quality-of-life improvements: remaining follow-ups]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Let compass commands that read environment/DB config (~db recreate~,
~services start/stop~, ~nats init~, etc.) target a named environment
file other than the repo-root ~.env~, e.g. ~.env.newton~ for a remote
deploy host. Today every one of these hardcodes ~project_root / ".env"~
(~compass_db.py load_env()~ and equivalents elsewhere), so the only way
to point at a different Postgres/NATS/host is to edit the working
~.env~ in place and remember to revert it -- exactly what this
session's Newton deploy-and-verify task had to do by hand.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:E599E5A0-84C9-4636-B478-5D1478688B9B][Compass quality-of-life improvements: remaining follow-ups]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-29                               |

* Acceptance

- Environment-reading compass commands accept a way to select a named
  env file (e.g. ~--env-file .env.newton~ or ~--env newton~ resolving
  to ~.env.newton~), defaulting to the current ~.env~ when omitted --
  fully backwards compatible.
- At minimum ~compass db recreate~ and ~compass services
  start/stop/status~ support it, since those are the two operations
  this session needed against Newton.
- Documented via a recipe under ~doc/recipes/~.
- The working ~.env~ is never mutated as a side effect of targeting a
  different host.

* Plan

Flag parsed early in =compass.py main()= via =_pop_env_file_arg()=,
before command dispatch, removing =--env=/=--env-file= from sys.argv
so sub-command parsers never see them. =_resolve_env_file()= converts
=--env newton= shorthand to =.env.newton=. Resolved path stored as
module-level =_ACTIVE_ENV_FILE=.

Two functions that read .env modified:
- =_read_env_map()= (compass.py) — consulted by every internal =cmd_*=
- =load_env()= (compass_db.py) — consulted by =db=, =services=, =systemd=, =client=, =claude= sub-commands

=env_file= parameter threaded through =compass_db.run()=,
=compass_services.run()=, =run_client()=, =systemd_generate.run()=,
=compass_claude.run()= — all defaulting to =None= (use default =.env=).
Works for any flag position (before or after sub-command).

* Notes

Recipe: doc/recipes/compass/how-do-i-use-named-env-files.org

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1877][#1877]] | [compass] Support multiple named .env files via --env / --env-file |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

=--env <name>= (resolves to =.env.<name>=) and =--env-file <path>= flags
added to every compass command. Flag works before or after the
sub-command. Parsed early in =main()= before command dispatch.

Modified two env-reading functions: =_read_env_map()= (compass.py,
for internal =cmd_*= commands) and =load_env()= (compass_db.py, for
=db=/=services=/=systemd=/=client=/=claude= sub-commands). Threaded
=env_file= parameter through five =run()= functions.

Acceptance met:
- =--env= and =--env-file= on all env-reading commands
- =db= and =services= supported (primary use case)
- Recipe at =doc/recipes/compass/how-do-i-use-named-env-files.org=
- Default =.env= never mutated; fully backwards compatible
