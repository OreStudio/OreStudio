:PROPERTIES:
:ID: D566131C-D08C-4AFE-950E-B3DD26EB2C24
:END:
#+title: Story: Extend oresmd to full ORE quote-type coverage
#+description: Pulled into sprint 25 from the product backlog: extend oresmd from ~7 of 49 ORE quote types to full coverage across all asset classes -- new families (inflation, correlation), new metric/dimension fields on existing families (IR, credit, equity, commodity), and a new volatility-surface sub-schema.
#+type: story
#+level: s2
#+filetags: :market-data:identifiers:oresmd:sprint_25:v0:
#+created: 2026-07-30
#+updated: 2026-08-10
#+environment: merry_newton
#+todo: BACKLOG STARTED | DONE ABANDONED

This page documents a [[id:699A8D45-B67E-4D3E-9206-24FA17C51ADA][story]] in [[id:0E7321A7-A66D-4CE6-B3E7-89F2E35CA48B][Sprint 25]], carried unfinished from [[id:D269862E-2035-4857-A0D0-A6E93B0E24C9][Sprint 24]] at close. It captures the goal, current status, acceptance criteria, and the tasks that compose it.

* Goal

oresmd should be able to *express* an identifier for every one of the
49 ORE quote types [[id:F9AE7F54-D995-476A-BA24-93698BF5029D][the market
data catalogue]] documents -- not just project a URI into a working ORE
key string, which is a separate, narrower concern this story does not
require of every family (see =* Out of scope=). Representation
completeness is the goal; XML/conversion coverage is tracked
separately (e.g. the [[id:A202712C-9CFD-48DC-9014-40BA5A9408C6][ORE
curve-config/market-data XML round-trip task]] this story was split off
from, which stays scoped to =YieldCurve= plus whatever asset classes
oresmd already represents; that task has since been =ABANDONED=, its
=* Result= recording why).

* Status

| Field         | Value                                  |
|---------------+----------------------------------------|
| State         | DONE                              |
| Carried from  | [[id:D269862E-2035-4857-A0D0-A6E93B0E24C9][Sprint 24]] (unfinished at close) |
| Now           | Nothing. |
| Waiting on    | Nothing.                               |
| Next          | Nothing. |
| Last touched  | 2026-08-10                               |

* Acceptance

- [X] Every catalogued quote type has a corresponding, constructible
  oresmd identifier shape (a variant member, or new fields/enum values
  on an existing one) with unit tests confirming it can be built and,
  where the catalogue gives a worked key example, projects to the
  documented ORE key string. -- with two documented caveats:
  =RATING/TRANSITION_PROBABILITY= is descoped (the credit identifier
  has no =provider=/=from_rating=/=to_rating= fields to carry it --
  recorded in the credit task's =* Result= and the credit spec's enum
  footer, tracked for its own task); and the shared
  =volatility_surface_point= sub-schema lands on every identifier
  struct, but only swaption vol projects a quote key today (see the vol
  task's =* Result=) -- every non-swaption vol shape is constructible
  and unit-tested, without a projection.
- [X] =market_data_urn.org= (id:C3E053CA-0D4B-480B-9119-E11530160EC1)
  is updated per family as its task lands -- new asset classes get new
  "Worked examples" sections in the same style as the existing five;
  extended asset classes get their new fields documented in "Grammar"
  and "Field justification" the same way existing fields are. (All
  seven families now have worked-example sections -- inflation and
  correlation added, the five originals extended; "Grammar"/"Field
  justification" gained the =quote=/=model= fields; "Data model" now
  covers all seven structs.)
- [X] No existing asset class's current fields/behaviour regress --
  every extension is additive (new optional fields, new enum values),
  matching this story's own established "resolution is strictly
  additive" principle. (Verified mechanically: regeneration of every
  generated oresmd file is byte-identical to the pre-story hand-crafted
  originals -- the =codegen-drift= gate's zero-diff check.)

* Tasks

| Task | State | Start | End | Description |
|------+-------+-------+-----+-------------|
| [[id:C9C888AB-473D-4C91-BC30-9470E4080C5A][Extend oresmd IR coverage: MM, FRA, basis swaps, zero curves, futures]] | DONE | 2026-08-07 | 2026-08-07 | Represent the 10 of 12 catalogued IR quote types not yet covered: MM/RATE, FRA/RATE, IMM_FRA/RATE, BASIS_SWAP/BASIS_SPREAD, CC_BASIS_SWAP/BASIS_SPREAD, CC_FIX_FLOAT_SWAP/RATE, BMA_SWAP/RATIO, ZERO/RATE, ZERO/YIELD_SPREAD, MM_FUTURE/PRICE, OI_FUTURE/PRICE. |
| [[id:37E0F26A-7702-4379-8D18-C58DC701C790][Extend oresmd credit coverage: hazard rate, recovery rate, base correlation, rating transition]] | DONE | 2026-08-08 | 2026-08-08 | Represent the 4 of 5 catalogued credit quote types not yet covered: HAZARD_RATE/RATE, RECOVERY_RATE/RATE, CDS_INDEX/BASE_CORRELATION and INDEX_CDS_TRANCHE/BASE_CORRELATION (legacy and modern base-correlation formats), RATING/TRANSITION_PROBABILITY. |
| [[id:D7FC7525-5532-4498-8B7B-5F14215C99FD][Extend oresmd equity coverage: dividend rate, equity forward price]] | DONE | 2026-08-08 | 2026-08-08 | Represent the 2 of 3 catalogued equity quote types not yet covered: EQUITY_DIVIDEND/RATE, EQUITY_FWD/PRICE. |
| [[id:9E8804D0-16BA-45D6-BCE8-CBCA60219DE2][Extend oresmd commodity coverage: commodity forward price, CPR]] | DONE | 2026-08-08 | 2026-08-08 | Represent the 2 of 3 catalogued commodity quote types not yet covered: COMMODITY_FWD/PRICE, CPR/RATE. |
| [[id:B87C97B8-415B-4C7D-9ACB-2B58EEFCB26E][Extend oresmd FX coverage: FX forward points]] | DONE | 2026-08-08 | 2026-08-08 | Represent the 1 of 2 catalogued FX quote types not yet covered: FXFWD/RATE. |
| [[id:DD35387F-D84B-4392-9C07-8F6F7F2FFF8F][Add oresmd inflation asset class: ZC/YoY swap rate, seasonality]] | DONE | 2026-08-08 | 2026-08-08 | oresmd has no inflation asset class at all today. Add inflation_market_data_identifier covering the 3 catalogued inflation quote types: ZC_INFLATIONSWAP/RATE, YY_INFLATIONSWAP/RATE, SEASONALITY/RATE. |
| [[id:B91F6F7D-5C8A-4DB0-A5C6-827EEC43BE41][Design and add oresmd volatility-surface sub-schema]] | DONE | 2026-08-08 | 2026-08-08 | oresmd only represents swaption RATE_LNVOL today. Design one shared strike/expiry/model-subtype sub-schema and apply it across all 8 catalogued vol sub-families: swaption, cap/floor, FX option, equity option, commodity option, bond option, inflation cap/floor, credit index option (12 quote types total). |
| [[id:6D1CA504-D70A-4ACB-9560-3C124162FC80][Add oresmd correlation asset class: pairwise factor correlation]] | DONE | 2026-08-08 | 2026-08-08 | oresmd has no correlation asset class at all today. Add correlation_market_data_identifier covering CORRELATION/RATE (pairwise correlation between two risk factors, e.g. CCY-CMS-TENOR, FX-FIX-CCY1-CCY2, EQ-TICKER). |
| [[id:602454B1-2D6E-4E40-BBD0-DA3F6446A569][Codegen oresmd quote types]] | DONE | 2026-08-10 | 2026-08-10 | Add codegen support for oresmd quote types: define a new entity type that generates the enum, struct fields, parser branches, projection functions, resolver wiring, and tests from a compact specification, producing zero diffs against the hand-crafted implementation. |

* Decisions

- *Representation and conversion are separate concerns*: this story's
  acceptance is "oresmd can express an identifier for every catalogued
  type", not "every type has working XML import/export" -- the latter
  is per-domain-model conversion work tracked wherever that domain
  model actually lives (e.g. =ir_curve_generation_config=/=ir_curve_feed=
  for IR yield curves), and most catalogued types have no ORES domain
  model behind them at all yet. Confusing the two led to this story's
  own split-off from an XML-round-trip task that was never meant to
  carry this scope.
- *Volatility surfaces get one shared sub-schema, not eight
  independent ones*: every vol sub-family (swaption, cap/floor, FX/equity/commodity/bond
  option, inflation cap/floor, credit index option) shares the same
  underlying shape -- a strike/moneyness dimension, an expiry/tenor
  dimension, and a model-subtype (=RATE_LNVOL=/=RATE_NVOL=/=RATE_SLNVOL=/=SHIFT=/=PRICE=)
  -- so the schema design happens once, in its own task, rather than
  being rediscovered independently per sub-family.
- *oresmd codegen replaces the hand-crafted files wholesale, and
  byte-identical output is the acceptance gate*: the org spec files are
  the canonical source for the enum, structs, parser, projections,
  resolver, and tests; codegen generates complete replacement files
  (framework code included), and adoption required each generated file
  to match the hand-crafted original exactly (=git diff= empty after
  regeneration), enforced thereafter by the CI =codegen-drift= gate
  now covering =ores.marketdata=.

* Out of scope

- Full =curveconfig.xml=/=todaysmarket.xml=/market-data-quote-string
  *conversion* code for every family -- tracked, where it belongs, in
  the [[id:A202712C-9CFD-48DC-9014-40BA5A9408C6][ORE curve-config/market-data
  XML round-trip task]] (scoped to =YieldCurve= for now; that task is
  since =ABANDONED=, its =* Result= recording why) and future tasks
  once each family's own ORES domain model exists.
- Building the ORES domain models each new asset class's real data
  would live in (e.g. an actual inflation curve entity, a correlation
  matrix entity) -- this story only extends oresmd's *identifier*
  representation, not the persisted domain models those identifiers
  would eventually back.
