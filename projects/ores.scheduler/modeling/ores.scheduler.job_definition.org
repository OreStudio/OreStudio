:PROPERTIES:
:ID: BD750888-6B72-48C1-AE74-DDC535705022
:END:
#+title: ores.scheduler.job_definition
#+description: Codegen entity model for the scheduler job_definition table. Drives the C++ domain class, mapper, repository, service, protocol, eventing, generator, history provider and SQL schema generation from a single literate source.
#+type: ores.codegen.entity
#+component: scheduler
#+filetags: :model:entity:scheduler:
#+brief: Persistent plan for a recurring or one-off scheduled job.
#+entity_singular: job_definition
#+entity_plural: job_definitions
#+entity_title: Job Definition
#+created: 2026-06-01
#+updated: 2026-09-26

A job the scheduler fires on a cron expression. The row carries the schedule
(=schedule_expression=), what to run when it fires, and whether it is active.
=action_type= selects the behaviour: =execute_sql= runs the SQL in =command=,
and =nats_publish= publishes the subject and body carried in =action_payload=.

The table is bi-temporal and audited (see
=projects/ores.sql/create/scheduler/scheduler_job_definitions_create.sql=): it
carries =version=, the four audit columns and the =valid_from=/=valid_to= pair
with the GIST exclusion and the delete rule, so the model takes the ordinary
audited shape and needs no shape flag.

=tenant_id= is nullable. A job may belong to no tenant, because the scheduler
fires system jobs from a NULL-tenant row: the MQ statistics scrape and the
compute stale-result reaper are both such rows. The model binds
=uuid-identified-lookup= for its UUID surrogate key, its tenant scope and its
standard presentation tier, and states =nullable_tenant_id= itself, which that
profile leaves to the model.

=job_name= is the natural key and is unique within its tenant; =id= is the
surrogate. Uniqueness is what the component's upsert path relies on: a job that
arrives under an existing name updates that row in place instead of adding a
second one.

The component's operational views — the global job-instance list and the live
scheduler status — are operations rather than entity verbs, and are modelled
in =ores.scheduler.scheduling_operations=.

* Flags
:PROPERTIES:
:schema:    public
:product:   ores
:component: scheduler
:profile:   uuid-identified-lookup
:END:

* Natural keys

* Columns

** job_name
:PROPERTIES:
:type:        text
:cpp_type:    std::string
:natural_key: true
:END:

Unique name for the job within its tenant. The scheduler seed writes
=ores.mq.metrics_scrape= and the compute seed writes
=compute.v1.reap.stale_results=; both are system jobs, so both carry a NULL
tenant and neither collides with a tenant's own job of the same name.

** id
:PROPERTIES:
:type:          uuid
:cpp_type:      boost::uuids::uuid
:primary_key:   true
:uuid_check_fn: ores_utility_nil_uuid_fn()
:END:

UUID primary key for the job definition.

** party_id
:PROPERTIES:
:type:     uuid
:cpp_type: std::optional<boost::uuids::uuid>
:nullable: true
:END:

Optional party scope for this job. NULL for a tenant-scoped job and for a
system job.

** description
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: false
:default:  ''
:END:

Human-readable description of the job.

** command
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: false
:default:  ''
:END:

SQL command to execute when =action_type= is =execute_sql=.

** schedule_expression
:PROPERTIES:
:type:          text
:cpp_type:      domain::cron_expression
:is_value_type: true
:nullable:      false
:END:

Cron expression defining the schedule. It is the component's own validated
=cron_expression= type, so an invalid expression cannot enter the domain.

** action_type
:PROPERTIES:
:type:          text
:cpp_type:      std::string
:nullable:      false
:default:       'execute_sql'
:default_value: "execute_sql"
:END:

Execution mode: =execute_sql= or =nats_publish=.

** action_payload
:PROPERTIES:
:type:          jsonb
:cpp_type:      std::string
:nullable:      false
:default:       '{}'::jsonb
:default_value: "{}"
:END:

Payload for the =nats_publish= action type. The scheduler seed writes an empty
object; the compute seed writes ={"subject":"compute.v1.work.reap"}=.

** is_active
:PROPERTIES:
:type:          boolean
:cpp_type:      bool
:nullable:      false
:default:       true
:default_value: "true"
:END:

Whether the job fires. A paused job keeps its row and its history.

* SQL

** Flags
:PROPERTIES:
:tablename:          ores_scheduler_job_definitions_tbl
:nullable_tenant_id: true
:END:

* Physical space

The shell commands for this entity are switched off. The =ores.shell= composite
does not carry a scheduler part yet, and the shell command registry is
hand-written, so enabling the facet here would generate command units and a
recipe for a module no build reaches. Adding the part to the shell component,
registering its commands, and then enabling this facet is the next unit of the
scheduler clean-up; the profile enables the facet, so this table states the
reason it is overridden.

| Address                | Enabled |
|------------------------+---------|
| ores.cpp.shell-command | false   |

* C++

** Flags
:PROPERTIES:
:subcomponent:        api
:generator_facet_name: generators
:END:

** Repository
:PROPERTIES:
:entity_singular_short: definition
:entity_plural_short:   definitions
:entity_singular_words: job definition
:entity_plural_words:   job definitions
:END:

** Domain includes

#+begin_src cpp :name includes
#include <chrono>
#include <optional>
#include <string>
#include <boost/uuid/uuid.hpp>
#+end_src

** Conventions
:PROPERTIES:
:iterator_var: jd
:END:

** Table display

| column              | header        |
|---------------------+---------------|
| id                  | ID            |
| job_name            | Job Name      |
| description         | Description   |
| schedule_expression | Schedule      |
| is_active           | Active        |
| modified_by         | Modified By   |
| version             | Version       |

** Custom repository methods

The generated CRUD set reads by primary key and by the natural key. The
scheduler loop needs a third read: every active job, across every tenant,
because it fires system jobs as well as tenant ones. That read is the one
method beyond the generated set, and it cannot be expressed by the generated
by-column finder because it deliberately drops the tenant filter.

*** Repository declarations

#+begin_src cpp :name declaration :implements DCA78C69-E508-48D9-9972-A9B8094D91FB
    /**
     * @brief Reads every active job definition, across all tenants.
     *
     * The scheduler loop fires system jobs as well as tenant ones, so it
     * cannot read through the tenant-scoped CRUD set.
     */
    std::vector<domain::job_definition> read_all_active(context ctx);
#+end_src

*** Repository implementations

#+begin_src cpp :name implementation :implements F2EB1914-5E94-42CA-9C77-46BDB364BF9E
std::vector<domain::job_definition>
job_definition_repository::read_all_active(context ctx) {
    BOOST_LOG_SEV(lg(), debug) << "Reading all active job definitions.";
    static const auto max(make_timestamp(MAX_TIMESTAMP, lg()));
    const auto query = sqlgen::read<std::vector<job_definition_entity>> |
                       where("is_active"_c == true && "valid_to"_c == max.value());

    return execute_read_query<job_definition_entity, domain::job_definition>(
        ctx,
        query,
        [](const auto& entities) { return job_definition_mapper::map(entities); },
        lg(),
        "Reading all active job definitions.");
}
#+end_src

* See also

- [[id:B788F24E-2E3F-432A-BD4F-CA8D6EBB2C9D][ores.scheduler]] — component group overview.
