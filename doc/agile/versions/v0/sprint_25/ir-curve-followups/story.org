:PROPERTIES:
:ID: 80F1638E-D04A-4618-ABD6-7A853F31FCD0
:END:
#+title: Story: IR curve follow-ups
#+description: Three IR curve follow-up items: expand stochastic IR process types beyond Vasicek/CIR/Hull-White, fix IR curve seed data and Qt dialog combo gaps, and investigate tenant_type has_tenant_id model/table drift.
#+type: story
#+level: s2
#+filetags: :sprint_25:v0:
#+created: 2026-08-06
#+updated: 2026-08-06
#+environment: eager_maxwell
#+todo: BACKLOG STARTED | DONE ABANDONED

This page documents a [[id:699A8D45-B67E-4D3E-9206-24FA17C51ADA][story]] in [[id:0E7321A7-A66D-4CE6-B3E7-89F2E35CA48B][Sprint 25]]. It captures the goal, current status, acceptance criteria, and the tasks that compose it.

* Goal

(Describe the user-visible outcome this story delivers.)

* Status

| Field         | Value                                  |
|---------------+----------------------------------------|
| State         | STARTED                              |
| Parent sprint | [[id:0E7321A7-A66D-4CE6-B3E7-89F2E35CA48B][Sprint 25]] |
| Now           | Not yet started.                       |
| Waiting on    | Nothing.                               |
| Next          | Break the story into tasks.            |
| Last touched  | 2026-08-06                               |

* Acceptance

-

* Tasks

| Task | State | Start | End | Description |
|------+-------+-------+-----+-------------|
| [[id:003B3504-8F6F-406F-A94B-A3119DE51962][IR curve follow-ups]] | DONE | 2026-08-10 | 2026-08-10 | Two follow-ups surfaced during IR curve work: realistic-2026 seed data incompatible with index_family enum (provision party fails), and IrCurveGenerationConfigDetailDialog's hand-patched index family combo doesn't list the 16 new RFR/IBOR families. |
| [[id:8656F506-A3A8-4498-9F34-4A47F32F78A6][Expand synthetic IR stochastic process types beyond Vasicek/CIR/Hull-White]] | DONE | 2026-08-07 | 2026-08-09 | Add support for further stochastic interest-rate models (BDT, Black-Karasinski, HJM, LMM, SABR, G2++, affine/QG term-structure) to ores.synthetic's yield curve process types, currently limited to Vasicek, CIR, and Hull-White. |
| [[id:2F793500-FBD1-4E4F-8B6E-15D03337FBD2][Investigate tenant_type model/table drift on has_tenant_id]] | BACKLOG | | | tenant_type's codegen model has no :has_tenant_id: flag, meaning current codegen would drop the tenant_id column entirely, yet the live table still has it -- likely stale drift, not a clean pattern to copy. |
| [[id:0A9B3E56-231D-467C-9146-14386A9DA9FF][Implement G2++ two-factor Gaussian short-rate process]] | DONE | | 2026-08-07 | Implement a two-factor Gaussian short-rate engine following the data-oriented pattern of the existing hull_white_process. The second factor adds kappa2, sigma2, and correlation rho. discount_factor() has a closed-form two-factor affine bond price. Reference: QuantLib G2 class. |
| [[id:CDB12DB0-B0FF-4507-B94F-17540F6792B3][Implement Black-Karasinski log-normal short-rate process]] | DONE | | 2026-08-09 | Implement a log-normal short-rate engine via Euler discretization of d(ln r) = kappa(theta - ln r)dt + sigma dW, then r = exp(ln r). This ensures positivity without a tree — a simple SDE approach, data-oriented with plain struct parameters and free functions. Reference: QuantLib BlackKarasinski class. |
| [[id:DF2BC7E3-8530-4A9C-9071-E48C2BDF44A5][Implement Black-Derman-Toy binomial tree short-rate process]] | DONE | | 2026-08-09 | Implement a BDT recombining binomial tree engine. The tree simultaneously matches the initial term structure and a time-varying volatility sigma(t). Data-oriented design: a const tree built once at construction, next() walks the tree, discount_factor() sums over the tree-implied bond prices. Extract a reusable binomial_tree utility for future tree models. Reference: QuantLib BlackDermanToy class. |
| [[id:0F75021C-E1AF-452B-813C-DA43F37A7C4E][Implement multi-factor Affine Term Structure process framework]] | DONE | | 2026-08-09 | Implement a general multi-factor affine term-structure engine: r_t = delta_0 + delta^T X_t, where X_t follows a multi-factor affine diffusion. Data-oriented: the factor dynamics are a struct of (kappa vector, sigma matrix, theta vector, delta_0, delta vector) with free-function evolvers. The existing 1-factor Vasicek/CIR/Hull-White become special cases of this framework. Reference: Duffie-Kan theory, QuantLib affine model fragments. |
| [[id:65233C15-4ABD-4457-B238-0FD994028978][Implement Heath-Jarrow-Morton forward-curve process]] | DONE | | 2026-08-09 | Implement an HJM forward-rate engine where the state is a discretized forward curve f(t,T_i) at N tenor points. Data-oriented: the forward curve is a vector of doubles, evolved by a free function that applies volatility structure sigma(t,T_i) and the HJM no-arbitrage drift condition. discount_factor() integrates the forward curve from the current discretization. Reference: QuantLib HJM class. |
| [[id:48D2AA4A-6253-4B09-A78F-21C9B950F68C][Implement LIBOR Market Model forward-rate process]] | DONE | | 2026-08-09 | Implement an LMM/BGM engine where the state is a vector of forward LIBOR rates L_i(t) for N accrual periods. Data-oriented: the forward-rate vector, tenor structure, and correlation matrix are plain structs. The drift for each L_i under the forward measure depends on the correlation structure across all forward rates — the most complex drift calculation in the set. Reference: QuantLib LiborMarketModel classes. |
| [[id:F1825CC4-6CB7-43F3-9184-0AFE22C15D34][Implement Quadratic Gaussian term-structure process]] | DONE | | 2026-08-09 | Implement a Quadratic Gaussian engine extending the affine framework with quadratic yield terms: r_t = delta_0 + delta^T X_t + X_t^T Gamma X_t. Data-oriented: builds on the affine framework's structs, adding a Gamma matrix. The quadratic term captures richer yield-curve dynamics (curvature effects) that affine models miss. This is the most original implementation — less direct QuantLib reference, more theory-driven. Reference: Ahn-Dittmar-Gallant QG papers. |
| [[id:8B09FCC2-0D30-4AF2-A205-01FC0A41ED3D][Wire G2 (Two-Factor Gaussian) into the system: factory, seed data, config storage, Qt UI]] | STARTED | 2026-08-07 | | PR 2 for the G2 model: expose the raw two_factor_gaussian_process engine to the system. Strongly-typed parameter structs, factory method, child DB table for G2-specific params, and dynamic Qt UI that switches the parameter form based on process_type. |
| [[id:718B2D01-3383-4DEA-80F5-5F30F300DB74][Fix realistic_2026 theme provisioning: cross-tenant parameter_definition_id validation]] | DONE | 2026-08-10 | 2026-08-11 | The realistic_2026 synthetic theme fails to publish in every Acme entity: the generated insert trigger on ores_synthetic_config_process_parameter_values_tbl validates parameter_definition_id under NEW.tenant_id, but the definitions are system-tenant reference data, so the cross-tenant reference from ores_synthetic_publish_theme_from_dq_fn is rejected (Invalid parameter_definition_id). Fix: :use_system_tenant: true on the FK; regenerate, rebuild, recreate DB, re-provision. |

* Decisions

- Matrix math for the multi-factor processes (affine, HJM, LMM, QG) uses
  Eigen (vcpkg port =eigen3=, header-only). Prior-art analysis rejected
  =std::mdspan= (absent from this toolchain), Boost uBLAS (legacy API, no
  Cholesky), glm (graphics-only) and NVIDIA matx (CUDA-first). Eigen is the
  modern CPU dense-algebra standard: value semantics, expression templates,
  LLT/LLT-with-eigen-fallback Cholesky for correlated shocks.
- Exact one-step Gaussian transitions everywhere one exists (the codebase
  convention, and QuantLib's own OU evolve): BK on =ln r=, affine/QG on the
  factors. Euler is used only where no exact transition exists (LMM's
  rate dynamics), with its O(dt) bias measured and documented.
- One generic =rate_tree= lattice in =math/= serves every tree model: BDT
  fills a binomial (2 children, p = 0.5), BK a centered adaptive trinomial
  (Hull-White moment-matching probabilities, valid for any horizon --
  no clamping). The processes build the tree; the utility walks it and
  propagates state prices. BDT extends flat beyond the input curve rather
  than bounding the horizon (no new limitations at the start).
- QG bond pricing uses the exponential-quadratic recursion (completion of
  squares over the per-tick shock, Eigen inverse + determinant). At
  Gamma == 0 it reduces to the affine recursion bit-for-bit -- the
  discrete-time design anchors the two models together.
- =affine_term_structure_process= covers Gaussian factors only. CIR stays
  its own process: the square-root factor's exact transition does not fit
  Gaussian OU dynamics, and forcing it through would break its exact
  transition. Documented as a future extension.
- Engine work only: none of the six processes is wired into
  =process_factory= yet. System-infra wiring is PR 2 (the G2 precedent).
- =oresmd::index_family= mirrors the SQL CHECK constraint on
  =ores_synthetic_ir_curve_generation_configs_tbl= (22 families) as the
  single source of truth, and =is_overnight()= encodes its tenor rule
  (only libor/euribor are term). The seed/config side stays a plain
  lower-case token; the enum mismatch that broke party provisioning from
  realistic-2026 seed data is resolved on the oresmd side.
- The dialog's index family combo stays hand-patched (all 22 families,
  b36839fd0) rather than enum-driven: ores.qt/synthetic already links
  ores.marketdata.api, but the combo lists user-facing labels that would
  not gain from magic_enum round-trips; both sides cite the same CHECK.

* Out of scope

-
