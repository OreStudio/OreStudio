:PROPERTIES:
:ID: CA6A6FA0-0DA7-4FF6-8F66-F51FAF620A94
:END:
#+title: Migrate the remaining temp path call sites to the shared scoped temp types
#+description: Six components still build their own temporary paths, two of them in production code that never cleans up after a failed run; migrate them to ores.platform::filesystem::scoped_temp_file and scoped_temp_directory.
#+type: capture
#+level: cross
#+filetags: :tech_debt:filesystem:testing:cpp:
#+created: 2026-09-26
#+updated: 2026-09-26

This page is a [[id:671F18E4-E09C-4B3B-BD24-D33DF8AE38A6][capture]] in the *next* bucket of the product backlog — a pre-sprint idea, not yet pulled into a sprint as a story.

* What

The shared temp types land in =ores.platform::filesystem= with the platform
clean-up: =scoped_temp_file= and =scoped_temp_directory=. Six call sites
still build their own temporary paths and should migrate to them.

| Call site | Kind | Today |
|-----------+------+-------|
| =ores.storage/src/net/storage_transfer.cpp= | production | a local =make_temp_archive()= plus a second inline path, UUID named, never removed |
| =ores.ore/service/src/messaging/report_package_handler.cpp= | production | a staging directory created inline, UUID named, never removed |
| =ores.compute/wrapper/tests/filesystem_archiver_tests.cpp= | test | its own variant |
| =ores.ore/core/tests/planner_import_planner_tests.cpp= | test | its own variant |
| =ores.orgmode/tests/resolver_tests.cpp= | test | its own variant |
| =ores.utility/tests/crypto_sha256_tests.cpp= | test | its own variant |

* Why

One behaviour had seven implementations and three naming strategies, which
is the duplicate case that B06 and H02 exist for. The two production sites
leak their temp file or directory whenever a run fails between creation and
cleanup, and every test site repeats a manual =std::filesystem::remove=
that a destructor should own.

Each call site belongs to a component with its own clean-up story, so this
is a separate pass rather than part of the platform story. The migration is
mechanical at every site and changes no behaviour beyond the cleanup.

* References

- =projects/ores.platform/include/ores.platform/filesystem/= — the shared types.
- =projects/ores.platform/tests/filesystem_file_tests.cpp= — the migrated reference call site.

* See also

- [[id:DE3200A3-7E68-40DE-B2C8-53FAF985B27D][Bring ores.platform to the clean standard]] — where the shared types landed.
- [[id:7B477025-132E-4B20-80B4-D8C9420880B8][Component Clean Standard]] — B06 and H02, the duplicate rules this serves.
