:PROPERTIES:
:ID: 00B62E70-D961-4DFA-BAB4-BB3488B31C86
:END:
#+title: Task: Sync C++ messaging codegen for currency auxiliaries
#+description: Re-run protocol, nats-eventing, and nats-handler profiles for rounding_type, monetary_nature, currency_market_tier; diff against repo; fix templates to match code.
#+type: task
#+level: s1
#+filetags: :commission_currency:sprint_22:v0:codegen:cpp:nats:
#+owner: marco
#+branch: feature/sync-cpp-messaging-codegen-currency-auxiliaries
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-06-07
#+updated: 2026-06-12
#+environment: brave_hopper
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:7F9C0F29-4268-4B62-A1BB-2153ECF0A858][Commission: currency]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Ensure the C++ messaging codegen templates (=protocol=, =nats-eventing=,
=nats-handler= profiles) produce output that matches the repository for
=rounding_type=, =monetary_nature=, and =currency_market_tier=.

These profiles generate:
- =protocol=: NATS request/response header structs (=*_protocol.hpp=)
- =nats-eventing=: domain changed-event struct with =event_traits= (=*_changed_event.hpp=)
- =nats-handler=: NATS message handler class (=*_handler.hpp=)

The previous verify-codegen task did not explicitly run these profiles; drift status
is unknown. Run each, diff against the repo, and fix templates to match code — not the
reverse — unless the code has a demonstrable bug.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                                 |
| Parent story | [[id:7F9C0F29-4268-4B62-A1BB-2153ECF0A858][Commission: currency]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-06-25                             |

* Acceptance

- =codegen regenerate --component refdata-cpp --profile protocol= produces zero diff
  for =rounding_type=, =monetary_nature=, =currency_market_tier= files.
- Same for =nats-eventing= and =nats-handler= profiles.
- Per-change-category decision recorded for any drift found.
- Findings recorded in =* Result=.

* Plan

1. Run =protocol=, =nats-eventing=, =nats-handler= profiles on =refdata-cpp=;
   scope diff to the three auxiliary entities.
2. For each changed file decide direction (fix template or fix code with justification).
3. Fix templates; re-run until zero diff.
4. Commit and raise PR.

* Notes

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | _STATE_RE / read_state duplicated in compass.py vs ui.py | compass.py | Fixed in c69ceec38 | Third _STATE_RE variant at line 1393 intentionally left (looser tolerance, private) |
| 1 | _STATE_ICONS["story"] missing BLOCKED | ui.py | No change | Stories have no BLOCKED state (todo: BACKLOG STARTED \| DONE ABANDONED) |
| 1 | get_*_response.success=true default for list responses | *_protocol.hpp | Fixed in 978bafa87 | Changed to false in template; added explicit resp.success=true in handler success path |
| 1 | Header guards missing CORE_ segment | *_handler.hpp | Fixed in 978bafa87 | Changed component_upper → component_core_upper in cpp_nats_handler_hpp.org |
| 2 | DISCOVERED state in _STATE_ICONS — vestigial check | ui.py | No change | DISCOVERED is a real task state in this project's #+todo sequence |
| 2 | delete handler loops in single try — pre-existing | *_handler.hpp | No change | Consistent with existing handler convention; out of scope |

* Result

Zero drift across all nine files (three profiles × three auxiliary entities).

| Profile | Entity | File | Result |
|---------+--------+------+--------|
| =protocol= | =rounding_type= | =rounding_type_protocol.hpp= | zero diff |
| =protocol= | =monetary_nature= | =monetary_nature_protocol.hpp= | zero diff |
| =protocol= | =currency_market_tier= | =currency_market_tier_protocol.hpp= | zero diff |
| =nats-eventing= | =rounding_type= | =rounding_type_changed_event.hpp= | zero diff |
| =nats-eventing= | =monetary_nature= | =monetary_nature_changed_event.hpp= | zero diff |
| =nats-eventing= | =currency_market_tier= | =currency_market_tier_changed_event.hpp= | zero diff |
| =nats-handler= | =rounding_type= | =rounding_type_handler.hpp= | zero diff |
| =nats-handler= | =monetary_nature= | =monetary_nature_handler.hpp= | zero diff |
| =nats-handler= | =currency_market_tier= | =currency_market_tier_handler.hpp= | zero diff |

The component-wide run (=--component refdata-cpp=) emits 13 spurious ERRORs for
=table= and =junction= models that do not support messaging profiles; these are
pre-existing and do not affect the auxiliary entity output. Captured as
=codegen_profile_model_type_incompatibility_errors= in the product backlog inbox.
