:PROPERTIES:
:ores.cpp.nats-handler.enabled: nil
:ores.cpp.nats-sub-registrar.enabled: nil
:ores.cpp.service.enabled: nil
:ores.cpp.shell-command.enabled: true
:ores.ts.web.enabled: true
:ID: D96E6889-A53B-41AF-A7F5-A3497A188BA0
:END:
#+title: ores.iam.account_party
#+description: Codegen junction model for account_parties. Renders the generic junction protocol itself.
#+type: ores.codegen.junction
#+component: iam
#+filetags: :model:junction:iam:
#+name: account_parties
#+name_singular: account_party
#+name_title: Account Party
#+name_singular_words: account party association
#+brief: Links an IAM account to a party.
#+product: ores
#+schema: public
#+has_tenant_id: true
#+created: 2026-06-02
#+updated: 2026-09-20

Junction table linking IAM accounts to parties. Each account can be
associated with one or more parties, controlling which parties a user
can act on behalf of.

* Flags
:PROPERTIES:
:profile: tenant-scoped-junction
:END:

* Left
:PROPERTIES:
:column:        account_id
:column_short:  account
:column_title:  Account
:type:          uuid
:cpp_type:      boost::uuids::uuid
:index_comment: Index for looking up parties for an account
:list_by:       true
:END:

ID of the IAM account.

References ores_iam_accounts_tbl.id (soft FK).

#+begin_src cpp :name generator
ctx.generate_uuid()
#+end_src

* Right
:PROPERTIES:
:column:        party_id
:column_short:  party
:column_title:  Party
:type:          uuid
:cpp_type:      boost::uuids::uuid
:index_comment: Index for finding accounts associated with a party
:END:

ID of the party this account is associated with.

References ores_refdata_parties_tbl.id (soft FK).

#+begin_src cpp :name generator
ctx.generate_uuid()
#+end_src

* Columns

* SQL

** Flags
:PROPERTIES:
:tablename: ores_iam_account_parties_tbl
:END:

* Foreign keys

** account_id
:PROPERTIES:
:table:         ores_iam_accounts_tbl
:error_message: Invalid account_id: %. No account found with this id.
:END:

ID of the IAM account; must exist in ores_iam_accounts_tbl.

** party_id
:PROPERTIES:
:table:         ores_refdata_parties_tbl
:error_message: Invalid party_id: %. No active party found with this id.
:END:

ID of the party this account is associated with; must exist in
ores_refdata_parties_tbl.

* Repository
:PROPERTIES:
:name_singular_short: account_party
:name_short:          account_parties
:name_singular_words: account party
:name_words:          account parties
:order_column:        party_id
:END:

* C++

** Flags
:PROPERTIES:
:subcomponent: api
:END:

** Domain includes

#+begin_src cpp :name includes
#include <chrono>
#include <string>
#include <boost/uuid/uuid.hpp>
#+end_src

** Entity includes

#+begin_src cpp :name includes
#include <string>
#include "sqlgen/Timestamp.hpp"
#include "sqlgen/PrimaryKey.hpp"
#+end_src

** Conventions
:PROPERTIES:
:iterator_var: ap
:END:

** Table display

| column      | header      |
|-------------+-------------|
| account_id  | Account     |
| party_id    | Party       |
| modified_by | Modified By |
| version     | Version     |

** Presentation
:PROPERTIES:
:collection_name: account_parties
:END:

The screen's declaration. An association has no key field to name: the model
declares none, because an association is identified by the pair it links and a
single field cannot state half of a pair. The view addresses it by the pair,
one path segment each, and the key record the descriptor states carries both.
That is why =:key_field:= is absent here and present on every entity that has
one.

*** Detail fields

Both members name a row in another collection, so each is a combo that lists
that collection rather than a field a person types. A screen that asked for two
UUIDs would be a screen nobody uses: nobody knows them, and the ones that do
did not need the screen.

| field      | label   | widget         | type          | is_key | is_required | placeholder | combo_domain_type      | combo_code_field | combo_display_field |
|------------+---------+----------------+---------------+--------+-------------+-------------+------------------------+------------------+---------------------|
| account_id | Account | accountIdCombo | dynamic_combo | true   | true        |             | iam::domain::account   | id               | username            |
| party_id   | Party   | partyIdCombo   | dynamic_combo | true   | true        |             | refdata::domain::party | id               | name                |

*** Columns

| enum_name | field      | header  | type   | width |
|-----------+------------+---------+--------+-------|
| AccountId | account_id | Account | string | 320   |
| PartyId   | party_id   | Party   | string | 320   |

* See also

- [[id:BD9653B7-C92B-4A71-9B5D-7ADB4EBCB94F][ores.iam]] — component group overview.
