:PROPERTIES:
:ID: 79F3E3F0-1F11-4A1D-AFB0-8CF9DD6C9972
:END:
#+title: Task: Merge service deployment config into service_registry.org as a new aspect
#+description: service_registry.org currently models only DB-access aspects per service (psql_var, env_key, iam_role, DML/select/execute grant prefixes), keyed by short names (iam, refdata, ...). Extend it in place to also carry a deployment aspect per service (replicas, enabled, description, depends_on, and an optional Extra args list of genuinely per-service CLI flags), one heading per fleet process the systemd generator needs to know about. Rename headings to full binary names (ores.iam.service, not iam) since that is the join key compass systemd generate actually needs (TLS cert/key filenames, unit basenames). Add the 3 processes that have no DB role (ores.http.server, ores.wt.service, ores.compute.wrapper) as deployment-only entries (no psql_var/iam_role/etc.), and drop the stale controller entry (no binary exists for it anymore). Extend org_loader.py's load_org_service_registry_model to parse and return the new deployment fields alongside the existing DB-grant fields. This is a pure model+parser change -- no consumer (systemd_generate.py, the SQL grant generator) is touched in this task, so it is safely reviewable in isolation as long as both aspects round-trip correctly.
#+type: task
#+level: s1
#+filetags: :codegen:modeling:tech-debt:process-supervisor-config-passthrough-gap:sprint_25:v0:
#+owner: marco
#+branch: feature/merge-service-registry-deployment-model
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-08-05
#+updated: 2026-08-05
#+environment: bright_faraday
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:9E3EC9E2-742F-4830-839D-99EF2C2BC626][Collapse NATS config passthrough to a single environment-mapper fallback tier]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

One org file models each service's full identity (DB access + how to launch it) as separate optional aspects on the same entity, instead of two separately-keyed files describing overlapping sets of the same services.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:9E3EC9E2-742F-4830-839D-99EF2C2BC626][Collapse NATS config passthrough to a single environment-mapper fallback tier]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-08-05                               |

* Acceptance

- service_registry.org has one heading per fleet process (all 19: the 16 existing DB-role services plus ores.http.server/ores.wt.service/ores.compute.wrapper), keyed by full binary name; DB-grant aspect fields are present only for the 16 services with their own NATS-domain-service DB role; deployment aspect fields (replicas/enabled/description/depends_on/extra args) are present for all 19; the stale controller entry is removed; load_org_service_registry_model returns both aspects correctly (unit-tested or verified via a scratch script); no consumer code changed in this task.

* Plan

- Rewrote service_registry.org: renamed all 16 existing headings from
  short names (iam, refdata, ...) to full binary names (ores.iam.service,
  ...) -- env_key/psql_var scalars unchanged, so DB-grant generation is
  unaffected by the rename. Added :replicas:/:enabled:/:depends_on:
  properties and an optional =** Extra args= sub-heading to every entry.
  Added ores.http.server, ores.wt.service, ores.compute.wrapper as new
  deployment-only entries (no psql_var/iam_role/etc., since they have no
  NATS-domain-service DB role). Dropped the stale controller entry.
  Every non-iam entry depends_on ores.iam.service, matching the old
  controller_service_dependencies_populate.sql seed data.
- Extended load_org_service_registry_model in org_loader.py: the
  discriminator for "is this a real service entry" moved from
  :psql_var: (now optional -- the DB-access aspect) to :replicas: (now
  mandatory -- the deployment aspect, since every entry in the fleet
  needs it). Added replicas (int), enabled (bool), depends_on (list,
  comma-split), and extra_args (list, one per == Extra args== bullet)
  to the returned dict, alongside the untouched DB-grant fields.
- Added projects/ores.codegen/tests/test_org_loader_service_registry.py:
  9 tests covering both aspects' presence/absence, replica counts,
  enabled defaults, dependency chains, extra-args round-tripping, the
  dropped controller entry, the total count (19), and that the
  pre-existing DB-grant-prefix parsing still round-trips correctly.
- Deliberately did not touch systemd_generate.py or any other consumer
  in this task -- it still queries postgres as before. That migration
  is the next task in the chain (wire_systemd_generate_to_codegen_model).

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

service_registry.org now models each of the fleet's 19 processes as one
entity with two independent optional aspects: DB access (present for the
16 with their own NATS-domain-service role) and deployment (present for
all 19). Headings renamed to full binary names, the stale controller
entry dropped. load_org_service_registry_model extended to parse and
return both aspects; verified via a scratch script and 9 new pytest
cases (all pass), plus the full existing ores.codegen suite (117/117
green) confirming no regression to DB-grant generation. No consumer
code touched -- systemd_generate.py still queries postgres, migrating
it is the next task in the chain.
