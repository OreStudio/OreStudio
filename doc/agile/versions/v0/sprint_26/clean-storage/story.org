:PROPERTIES:
:ID: 1AF1672B-C21F-4337-A9F3-4253008DB80D
:END:
#+title: Story: Clean ores.storage to the component clean standard
#+description: Bring ores.storage to the Component Clean Standard (All items): tier 1, foundation infrastructure.
#+type: story
#+level: s2
#+filetags: :sprint_26:codegen:clean_standard:v0:
#+created: 2026-09-23
#+updated: 2026-09-23
#+environment: solid_dirac
#+todo: BACKLOG STARTED | DONE ABANDONED

This page documents a [[id:699A8D45-B67E-4D3E-9206-24FA17C51ADA][story]] in [[id:2B39B97B-89F9-4320-8BAC-A603D805EBF3][Sprint 26]]. It captures the goal, current status, acceptance criteria, and the tasks that compose it.

* Goal

ores.storage meets the [[id:7B477025-132E-4B20-80B4-D8C9420880B8][Component Clean Standard]]: the items marked All pass, each item that does not apply is recorded with its reason, and ores.storage joins =COMPONENTS_UNDER_TEST=. Order 6 of 35 in the sprint 26 component clean-up, tier 1 (foundation infrastructure).

* Status

| Field         | Value                                  |
|---------------+----------------------------------------|
| State         | DONE                              |
| Parent sprint | [[id:2B39B97B-89F9-4320-8BAC-A603D805EBF3][Sprint 26]] |
| Now           | Nothing. The component is clean on =feature/clean-storage=; no PR raised. |
| Waiting on    | Nothing.                               |
| Next          | Nothing. |
| Last touched  | 2026-09-26                               |

* Acceptance

- Every checklist item that applies to a component of kind *All* passes, with its evidence recorded on the task.
- Every item that does not apply is recorded as not applicable, with the reason.
- Every generator defect found is fixed at its source, with a regression test.
- ores.storage is listed in =COMPONENTS_UNDER_TEST=, and =check_component_drift.py --all= passes.

* Tasks

| Task | State | Start | End | Description |
|------+-------+-------+-----+-------------|
| [[id:023DDD49-944D-45B2-88E1-080BCE4741EB][Bring ores.storage to the clean standard]] | DONE | 2026-09-26 | 2026-09-26 | Work ores.storage through the Component Clean Standard in order and record each item. |

* Notes

- Survey on 2026-09-23: 0 entity, junction or operation models; 9 C++ files outside tests, 0 of them generated (0%); 0 messaging headers, 0 of them hand-written.

* Decisions

- The archiver stays in =ores.storage= for this pass. It carries the
  component's only platform conditional and it duplicates
  =ores.compute.wrapper='s own copy, so the audit's platform-placement rule
  and the duplicate rule point at the same fix: one archiver, in
  =ores.platform::filesystem=, where the platform concern belongs. It is not
  done here because that would add libarchive to a foundation component and
  rewrite the tree of an already-closed clean pass. The capture
  [[id:43217FF8-2359-4C99-8E1D-6344E50C47EE][Consolidate the duplicated tarball archivers into one shared utility]]
  owns the move.
- =net::http_client= and =filesystem::archiver= stay public even though
  nothing outside the component includes them. Their intended consumer is
  =storage_transfer=, which composes both, and the cross-component captures
  want them as the survivor of the consolidation rather than as a private
  detail to be hidden first.
- V06 is an accepted exception rather than a pass. =COMPONENTS_UNDER_TEST=
  lists catalogue components, and a component with no entity model has no
  catalogue row, so the drift gate cannot name =ores.storage= at all. The
  standard records the same gap for the whole infrastructure tier.
- The temporary-file migration from
  [[id:CA6A6FA0-0DA7-4FF6-8F66-F51FAF620A94][Migrate the remaining temp path call sites to the shared scoped temp types]]
  lands here rather than in a pass of its own, because that capture assigns
  each call site to the clean-up story of the component that holds it.

* Out of scope

-
