:PROPERTIES:
:ID: A441DA2E-4B74-49B3-97AD-1861647B7D94
:END:
#+title: User Journey: Rescue access
#+description: A tenant administrator resets a password and locks or unlocks an account.
#+type: user_journey
#+level: cross
#+filetags: :user_journey:security:
#+created: 2026-09-23
#+updated: 2026-09-23
#+startup: inlineimages

This page documents a [[id:F44766FA-1E9C-416F-849A-E131101B2ECA][user journey]]: A tenant administrator resets a password and locks or unlocks an account.

A user journey describes what one person is trying to do, before anything is
built. It is a design document that outlives any sprint, so it names the
screens, the entity fields each screen shows, and the operations and messages
each step needs.

* Goal

"I want to get my colleague back into the system, or shut the account down."
The administrator sets a new password for one colleague, then locks or
unlocks the account. The journey is done when the colleague can sign in again,
or when the account is shut and the administrator has said why.

* Actor and trigger

The tenant administrator: an account inside the tenant that holds
=iam::accounts:reset_password=, =iam::accounts:lock= and
=iam::accounts:unlock=. The trigger is a support call, because a colleague is
locked out, or because a compromise is suspected. It starts when the
administrator opens *Rescue access* for one account, and ends when the account
is usable or shut.

This is the administrative half of the credentials topic. The member's own
half is [[id:5FB73D18-4734-485B-B414-9EBD1B76A239][Protect my account]], and
the record of what happened is
[[id:22AC8DD8-A440-4330-9992-47A5E9985473][Audit sign-ins]].

* Steps

1. *Find the colleague.* The administrator opens the screen from the account
   list and reads the account: username, full name, type and email. The system
   resolves the account and shows its security state beside it.
2. *Read the security state.* The administrator reads whether the account is
   locked, the failed attempt count, the last sign-in and whether a password
   change is required. A run of failed attempts tells the administrator which
   story this is.
3. *Set a new password.* The administrator types a new password twice and
   resets it. The system checks the password against the policy, and stores the
   new hash. The member can sign in with it at once.
4. *Force a change on the next sign-in.* The administrator asks the member to
   choose their own password at the next sign-in. No operation carries this
   over NATS, so the screen cannot offer it.
5. *Lock or unlock.* The administrator locks an account that must be shut, or
   unlocks one that was locked by failed attempts. The system writes the lock
   flag on the login record, and clears the failed attempt count on unlock.
6. *Leave.* The administrator goes back. The account list shows the new state.

* Screens and wireframes

One screen. The account being rescued, its security state, the password form
and the actions are panels of that screen, because the administrator thinks of
them as one decision.

#+caption: Rescue access: the account, its security state, the reset form and the actions available today.
#+attr_html: :width 100%
[[proj:doc/knowledge/journeys/credentials/wireframe_rescue_access.png]]

* Entities composed

#+ATTR_HTML: :class hug-leading
| Entity | What it contributes | Model |
|--------+---------------------+-------|
| =account= | Username, full name, type and email; the account being rescued | [[id:81D4D50F-A8D4-47F8-9500-89F6AC460469][ores.iam.account]] |
| =login_info= | Locked, failed attempts, last sign-in, reset required | [[id:8A507DA9-1FFE-4DD3-992F-7359F5531253][ores.iam.login_info]] |

The screen writes a password hash and never reads one back, so =password_hash=,
=password_salt= and =totp_secret= stay off it.

* Operations and messages

#+ATTR_HTML: :class hug-leading
| Step | Operation | Subject | Status |
|------+-----------+---------+--------|
| Find the colleague | List the accounts | =iam.v1.accounts.list= | exists |
| Find the colleague | Read one account | =iam.v1.accounts.get= | exists |
| Read the security state | Read the login record | =iam.v1.login_info.get= | exists |
| Set a new password | Reset the password | =iam.v1.accounts.reset-password= | exists |
| Force a change on the next sign-in | Set password reset required | none | missing |
| Lock the account | Lock the account | =iam.v1.accounts.lock= | exists |
| Unlock the account | Unlock the account | =iam.v1.accounts.unlock= | exists |
| Activate or deactivate the account | Change the account state | none | missing |
| Send a recovery link | Start self-service recovery | none | missing |

All three write subjects are declared in
[[id:082C5D76-93C2-41E6-8193-9598B79DD07A][ores.iam.account_messages]].
=iam.v1.accounts.reset-password= needs =iam::accounts:reset_password=,
=iam.v1.accounts.lock= needs =iam::accounts:lock=, and
=iam.v1.accounts.unlock= needs =iam::accounts:unlock=. A tenant administrator
who holds the role has all three, so each is =exists=.

* What is missing

1. *No forgot-password or self-service recovery.* Nothing lets a member who
   cannot sign in start recovery. The only password subjects are
   =iam.v1.accounts.change-password= (signed in, own account) and
   =iam.v1.accounts.reset-password= (administrator only). There is no request
   subject, no recovery token and no message path. The candidates are
   =iam.v1.accounts.request-password-reset= and
   =iam.v1.accounts.complete-password-reset=.
2. *No activate or deactivate.* An account has no active flag. The only state
   an administrator can change is =login_info.locked=. A lock can be lifted,
   but an account cannot be retired and later restored.
3. *The reset flag cannot be set over NATS.* =password_reset_required= exists on
   =login_info=, =account_operations_service::set_password_reset_required=
   implements it, and the HTTP route uses it. No NATS subject calls it, and
   =iam.v1.accounts.reset-password= writes a new password directly. The two
   surfaces disagree: over HTTP the member must choose a password at the next
   sign-in, over NATS the administrator has already chosen it.
4. *Locking does not end sessions.* =iam.v1.accounts.lock= writes the login
   record only. A session that is already signed in stays open until it
   expires. The candidate is the same session-end operation that
   [[id:22AC8DD8-A440-4330-9992-47A5E9985473][Audit sign-ins]] needs.

* Related journeys

- [[id:5FB73D18-4734-485B-B414-9EBD1B76A239][Protect my account]] — the member's own password change, when they can still sign in
- [[id:22AC8DD8-A440-4330-9992-47A5E9985473][Audit sign-ins]] — the failed attempts and sessions that explain why this account is locked
- [[id:2FB693D1-ACC7-4112-924F-56732FF9DE0B][Bring someone in]] — create the account when the colleague in the support call has none
- [[id:858C3FAA-C87A-4099-8893-582F9500681D][Sign in]] — where the password you set is first used, and where the forced change happens

* Related knowledge

- [[id:8AEB87D6-0CE2-1434-E71B-60BEAE20ADF0][Identity and Access Management]]
- [[id:EADD51F7-9F3E-4B03-8750-D7ABC995FE46][How do I add a PlantUML diagram?]]
- [[id:8C1DC5DE-F480-49F4-AA63-77A397D0BC03][Credentials journeys]]

* See also

- [[id:D602E114-F896-42CA-893A-841AD6FC3221][User Journeys]]
