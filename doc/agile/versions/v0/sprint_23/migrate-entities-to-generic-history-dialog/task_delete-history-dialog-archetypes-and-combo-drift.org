:PROPERTIES:
:ID: AA97E310-F293-46AD-9BE7-C13AD376346C
:END:
#+title: Task: Delete per-entity history-dialog codegen archetypes and reconcile dynamic_combo drift
#+description: Delete outright (not just disable) the deprecated per-entity history-dialog codegen archetypes, and resolve the dynamic_combo detail-dialog codegen drift found while regenerating currency for task 257F85B2.
#+type: task
#+level: s1
#+filetags: :migrate-entities-to-generic-history-dialog:sprint_23:v0:
#+owner: marco
#+branch: feature/delete-history-dialog-archetypes-and-combo-drift
#+pr: 1590
#+blocked_on:
#+blocked_since:
#+created: 2026-07-14
#+updated: 2026-07-15
#+environment: brave_hopper
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:DBDFF0B1-E358-4300-B361-644A89FBD169][Migrate all entities onto the generic HistoryDialog]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Two related codegen cleanup items surfaced while closing out task
257F85B2 ("Update ores.cpp.qt codegen templates for the generic
HistoryDialog"):

1. *Delete the per-entity history-dialog archetypes outright.*
   257F85B2 marked =ores.cpp.qt.history_dialog_impl.org=/
   =history_dialog_header.org=/=history_dialog_ui.org= =#+default:
   disabled= — "retained for reference" — but the per-entity
   =*HistoryDialog= class they generate is deprecated and being
   actively removed, not a pattern anyone should reference or
   accidentally re-enable. Delete the three =.org= archetypes and
   their tangled =cpp_qt_history_dialog.{hpp,cpp}.mustache=/
   =qt_history_dialog_ui.mustache= outputs entirely, and any
   =facet_catalogue.org=/=template_variables.org= references to them,
   so the pattern cannot be generated by anyone, full stop.
2. *Reconcile the dynamic_combo detail-dialog codegen drift* captured
   in [[id:BE3DEB46-1022-4A68-A1CD-7575CF6CDC33][Reconcile dynamic_combo detail-dialog codegen drift]]: several
   entities' detail dialogs (e.g. currency) still use the old
   =setCurrentText()=-based combo pattern instead of the current
   template's =findData()=/=setCurrentIndex()= + loading/error
   placeholder text — found while regenerating currency for 257F85B2,
   deliberately left out of that diff to keep it scoped.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                                   |
| Parent story | [[id:DBDFF0B1-E358-4300-B361-644A89FBD169][Migrate all entities onto the generic HistoryDialog]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-15                               |

* Acceptance

- =ores.cpp.qt.history_dialog_impl.org=/=history_dialog_header.org=/
  =history_dialog_ui.org= and their tangled =.mustache= outputs no
  longer exist in =projects/ores.codegen/library/templates/=.
- No remaining reference to them in =facet_catalogue.org=,
  =template_variables.org=, or =cpp_qt.org=.
- =compass codegen entity generate <entity> --address ores.cpp.qt=
  for any entity produces no per-entity =*HistoryDialog=
  class/header/.ui output — confirmed the generator itself no longer
  knows how to emit one (not just that nobody currently calls it).
- Every =dynamic_combo= entity's detail dialog is reviewed against
  the current template; diffs that are genuinely additive/correct
  (loading/error placeholder text, =findData()=-based value matching)
  are applied and committed per entity, with each diff inspected
  before applying — not a blind bulk regenerate, since this changes
  combo value-matching semantics and a wrong diff could silently break
  a working dialog.
- [[id:BE3DEB46-1022-4A68-A1CD-7575CF6CDC33][Reconcile dynamic_combo detail-dialog codegen drift]] closed
  (discarded/resolved) once addressed.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

Item 1 (archetype deletion): deleted
=ores.cpp.qt.history_dialog_impl/header/ui.org= and their tangled
=.mustache= outputs; removed every reference from
=facet_catalogue.org=, =cpp_qt.org=, =ores.cpp.qt.org=, =assets.org=,
and the now-dead static template-mapping entries in
=codegen/core.py=. Verified via re-tangle (facet doc count dropped by
3) and =compass codegen entity generate <entity> --diff= producing no
history-dialog output/error for any entity.

Item 2 (dynamic_combo drift): per the story owner's direction, no
selective/hand-patched regeneration — for each entity, ran the full
=compass codegen entity generate <entity> --address ores.cpp.qt= and
took the complete output (including the HistoryDialog controller
migration that comes with it, not just the DetailDialog combo fix),
reviewing the diff before applying; anything genuinely unexpected
gets flagged rather than silently forced through. Caught and fixed
one real template bug this surfaced: UUID-keyed entities'
=showHistoryWindow= passed the raw =boost::uuids::uuid= straight to
=QString::fromStdString=, which needs a =std::string= — only visible
once an actual UUID-keyed entity (=book=) was regenerated through
this path; fixed with =boost::uuids::to_string()=.

Completed for: country, currency, tenor_convention, instrument_code,
tenor, business_centre, book (7, in PR #1571). Rebasing onto latest
main surfaced one further trivial drift line on =book=
(=idEdit->setReadOnly=), reconciled the same way.

Follow-on batch (this branch, post-#1571): book_purpose_type,
book_status, business_day_convention_type, calendar, contact_type,
cds_convention, crm_driver_pair, crm_enabled_derived_pair,
crm_topology_config, currency_group, currency_pair,
currency_pair_classification, deposit_convention, fra_convention,
ibor_index_convention, ois_convention, overnight_index_convention,
swap_convention, zero_convention (19 more, 26 total). Full,
unmodified generator output per entity throughout, per the story
owner's explicit "no selective regeneration" direction.

Full regeneration across this batch surfaced two further genuine
codegen/model bugs (beyond the UUID-string-conversion one from
PR #1571), both pre-existing and unrelated to this task's own changes:

- =history_response_data_field= mismatch: 8 =*_convention= entities'
  actual protocol field is the pluralized collection name (e.g.
  =cds_conventions=), not the codegen default of =history=. Fixed via
  explicit per-entity model overrides.
- =delete_request_id_field= mismatch: same 8 entities' actual
  =delete_<entity>_request= field is a natural/display-key name (e.g.
  =codes=), not the codegen default of =ids=. Fixed the same way for
  these 8; a broader ~22-entity instance of the same mismatch exists
  elsewhere in the codebase but is out of this task's scope.

Manual QA testing (scenario [[id:FFFC881E-D3CD-4FDE-8366-9FF7979DA376][FFFC881E]]) surfaced two more real bugs,
also fixed on this branch:

- All 8 =*_convention= entities above were additionally missing
  =:has_change_reason_cache: true=, despite their schema requiring
  =change_reason_code= — saves failed with an empty change-reason
  dropdown. Fixed by adding the model flag and regenerating.
- =currency_group= and =calendar= had controllers but were never
  wired into =RefdataPlugin='s menu, and neither had its NATS CRUD
  subject registration wired into =registrar.cpp= server-side
  (=currency_group= didn't even have its =*_registrar.{hpp,cpp}=
  generated yet). Fixed by generating the missing registrar,
  instantiating both controllers, adding menu entries, and wiring
  both into =registrar.cpp='s =append(register_..._handlers(...))=
  list.

Also discovered and fixed (mid-migration, server-side): the generic
=history.v1.get= NATS subject requires a *separate*,
hand-maintained server-side history-provider registration in
=registrar.cpp= (independent of the client-side controller wiring
above) — without it, the History dialog compiles and opens but the
server returns "No history provider registered for entity_type:
...". Registered all 23 mapper-ready migrated entities (excluding
=instrument_code=, which has no history field mapper).

* Notes

*Scope note*: this task's scope is the 26 =ores.refdata= entities
listed above (=counterparty=, =counterparty_contact_information= and
=counterparty_identifier= deliberately excluded — active work in
another environment). The remaining =ores.refdata= entities and every
other component (=party=, =admin=, =compute=, =analytics=,
=marketdata=, =synthetic=, =trading=, =workspace=) are tracked by the
parent story's own per-component tasks, not by this one — closing
this task does not imply the parent story is done.

=registrar.cpp='s growth from adding 24 history-provider registrations
by hand (100+ includes, dozens of near-identical blocks) was captured
separately as a codegen candidate:
[[id:2DFEF8F6-DA10-407B-8A8A-95B938CD673D][Codegen the history-provider registrar wiring]].

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
| [[id:FFFC881E-D3CD-4FDE-8366-9FF7979DA376][Verify migrated entity history dialogs (book, cds_convention, tenor, currency_group, calendar)]] | PASSED | First run FAILED (schema drift, missing change-reason wiring, missing menu entries); all fixed and re-run PASSED. |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1590][#1590]] | [qt,refdata] Migrate 19 more entities onto generic HistoryDialog; fix change-reason/menu/registrar gaps |
| [[https://github.com/OreStudio/OreStudio/pull/1571][#1571]] | [qt,codegen] Delete legacy history-dialog archetypes; reconcile dynamic_combo drift (7 entities) |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Template drift CI failure: cpp_group.org stale after hand-editing cpp_qt.org's description | cpp_group.org | Accepted | Fixed in 221c426b via regenerate_facet_inventories.py, confirmed resolved by re-review. |
| 2 | Dangling id: links to the deleted ores.cpp.qt.history_dialog_impl archetype | doc/knowledge/architecture/history_diff_architecture.org | Accepted | Reworded both references to point at ores.cpp.qt.controller_impl (the archetype that now wires HistoryDialog) and explain the retirement, flagged independently by two review passes. |
| 3 | Hand-written validateInput() rationale comment dropped by full regen (currency) | CurrencyDetailDialog.cpp | Declined | Explicit, accepted tradeoff of the "no selective/hand-patched regeneration" policy this task follows; multiple reviewers confirmed this is intentional, not a regression. |
| 4 | Stale PlantUML diagrams still declare deleted BookHistoryDialog/BusinessCentreHistoryDialog classes | ores.qt.trading.puml / ores.qt.party.puml | Declined | Generated artifacts from a separate, out-of-scope archetype; reviewer flagged as informational only ("not a blocker"), and the diagrams already have pre-existing structural staleness (mis-scoped component placement) unrelated to this PR. |

* Result

Both goal items shipped for this task's scope (26 =ores.refdata=
entities): the deprecated per-entity history-dialog archetypes are
deleted outright (PR #1571), and the =dynamic_combo= drift is
reconciled for all 26 entities via full, unmodified codegen
regeneration (7 in #1571, 19 in this follow-on batch). Manual QA
scenario [[id:FFFC881E-D3CD-4FDE-8366-9FF7979DA376][FFFC881E]] PASSED after fixing everything it surfaced:
missing server-side history-provider registrations (23 entities),
=history_response_data_field=/=delete_request_id_field= model
mismatches (8 =*_convention= entities), missing
=has_change_reason_cache= wiring (same 8), and missing
Currency Group/Calendar menu + NATS CRUD wiring.

Remaining =ores.refdata= entities and every other component remain
out of scope, tracked by the parent story's per-component tasks.