namespace OresCodegenCppGenerator

// UVL model of entity_meta_model_cpp_generator.org: the standard
// single-record and N-record generators are mandatory; custom generator
// functions (declaration + implementation) are an independent optional
// pair.

features
    CppGenerator {abstract}
        mandatory
            StandardGenerators
        optional
            HasCustomDeclarations
            HasCustomImplementations
