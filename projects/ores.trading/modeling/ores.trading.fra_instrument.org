:PROPERTIES:
:ID: 104DDBF1-12E8-435F-90F0-A701AAD9078F
:END:
#+title: ores.trading.fra_instrument
#+description: Codegen entity model for the trading =fra_instrument= table. Drives the C++ domain class, mapper, repository, service, generator, Qt UI, and SQL schema generation from a single literate source.
#+type: ores.codegen.entity
#+component: trading
#+filetags: :model:entity:trading:
#+brief: Forward Rate Agreement (FRA) instrument.
#+entity_singular: fra_instrument
#+entity_plural: fra_instruments
#+entity_title: FRA Instrument
#+created: 2026-06-01
#+updated: 2026-06-01

Represents a Forward Rate Agreement instrument that fixes a future
interest rate for a notional principal amount over a specified period.

* Flags
:PROPERTIES:
:schema:           public
:product:          ores
:component:        trading
:has_tenant_id:    true
:has_workspace_id: true
:END:

* Primary key
:PROPERTIES:
:column:   instrument_id
:type:     uuid
:cpp_type: boost::uuids::uuid
:END:

UUID uniquely identifying this FRA instrument.

Surrogate key for the instrument record.

* Natural keys

* Columns

** trade_type_code
:PROPERTIES:
:group:    identity
:type:     text
:cpp_type: std::string
:nullable: false
:END:

Trade type code (soft FK to ores_trading_trade_types_tbl).

#+begin_src cpp :name generator
std::string("FRA")
#+end_src

** party_id
:PROPERTIES:
:group:    identity
:type:     uuid
:cpp_type: boost::uuids::uuid
:nullable: false
:END:

Party that owns this instrument.

Set from session variable app.current_party_id.

** trade_id
:PROPERTIES:
:group:    identity
:type:     uuid
:cpp_type: std::optional<boost::uuids::uuid>
:nullable: true
:END:

Optional soft FK to the parent trade.

Links instrument to a trade if applicable.

** start_date
:PROPERTIES:
:type:     date
:cpp_type: std::string
:nullable: false
:END:

FRA start date.

ISO 8601 date string (YYYY-MM-DD).

#+begin_src cpp :name generator
std::string("2024-01-15")
#+end_src

** end_date
:PROPERTIES:
:type:     date
:cpp_type: std::string
:nullable: false
:END:

FRA end date.

Must be after start_date.

#+begin_src cpp :name generator
std::string("2025-01-15")
#+end_src

** currency
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: false
:END:

ISO 4217 currency code.

e.g., USD, EUR, GBP.

#+begin_src cpp :name generator
std::string("USD")
#+end_src

** rate_index
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: false
:END:

Floating rate index code.

e.g., LIBOR, EURIBOR, SOFR.

#+begin_src cpp :name generator
std::string("SOFR")
#+end_src

** long_short
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: false
:END:

Position direction: Long or Short.

Indicates whether the party is a buyer (Long) or seller (Short).

#+begin_src cpp :name generator
std::string("Long")
#+end_src

** strike
:PROPERTIES:
:type:          numeric(18, 10)
:cpp_type:      double
:nullable:      false
:default_value: 0.0
:END:

Fixed contract rate.

Expressed as a decimal fraction.

#+begin_src cpp :name generator
0.05
#+end_src

** notional
:PROPERTIES:
:type:          numeric(28, 10)
:cpp_type:      double
:nullable:      false
:default_value: 0.0
:END:

Notional principal amount.

Must be positive.

#+begin_src cpp :name generator
1000000.0
#+end_src

** description
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: true
:END:

Optional free-text description.

Human-readable notes about this instrument.

* SQL

** Flags
:PROPERTIES:
:tablename: ores_trading_fra_instruments_tbl
:END:

* C++

The C++ domain class is decomposed into nested sub-structs (the C1202
pattern from PRs #1047/#1071/#1075/#1083/#1085): the implicit scaffolding
columns (version, tenant_id, workspace_id, the primary key) and the
columns marked =:group: identity= below fold into an
=instrument_identity identity;= member (see the
ores.trading.instrument_identity field-group model), the implicit audit
columns fold into an =ores::dq::domain::audit_record audit;= member (see
the ores.dq.audit_record field-group model), and the remaining columns
stay flat in between. The SQL schema, DB entity and column lists are
unaffected — only the domain class and the mapper's domain-side accesses
nest. The entity templates consume these annotations; domain and
repository profiles regenerate correctly.

** Flags
:PROPERTIES:
:subcomponent:    api
:domain_identity_group: ores.trading.instrument_identity
:domain_audit_group:    ores.dq.audit_record
:has_batch_read:       true
:generator_facet_name: generators
:END:

** Repository
:PROPERTIES:
:entity_singular_short: fra_instrument
:entity_plural_short:   fra_instruments
:entity_singular_words: FRA instrument
:entity_plural_words:   FRA instruments
:END:

** Domain includes

#+begin_src cpp :name includes
#include <chrono>
#include <string>
#+end_src

** Conventions
:PROPERTIES:
:iterator_var: fi
:END:

** Custom repository methods

* See also

- [[id:5C7A961E-D834-4B2F-AE15-F049B27361D8][ores.trading]] — component group overview.
