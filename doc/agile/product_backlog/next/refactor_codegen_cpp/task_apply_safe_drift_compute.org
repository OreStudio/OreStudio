:PROPERTIES:
:ID: 51E65D05-7F22-4DE1-9509-D50A9D98A503
:END:
#+title: Task: Apply safe drift to compute-cpp
#+description: Apply safe drift to the compute-cpp component using the pattern established by the refdata-cpp pilot. Heaviest exclusion catalogue: restore all 6 service files (production uses minimal list/find/save/history API), result_repository (read_by_workunit), workunit_repository (read_by_batch), 3 mappers (nullable vs non-optional), 2 repositories (system_tenant union filter). Build and test green, raise PR.
#+type: task
#+level: s1
#+filetags: :refactor_codegen_cpp:sprint_22:v0:
#+owner: marco
#+branch: feature/refactor-codegen-cpp
#+pr:
#+blocked_on: C8A4D7F1-2E6B-4C3A-8F9D-5B1E3C8D2F7A
#+blocked_since:
#+created: 2026-05-31
#+updated: 2026-06-12
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:3B8F2A1D-7C4E-4B6A-9E5D-2F1A8C3B7D9E][Refactor ores.codegen C++ generation]] story.

* Goal

Apply safe drift to =compute-cpp= following the [[id:C8A4D7F1-2E6B-4C3A-8F9D-5B1E3C8D2F7A][refdata-cpp pilot]] pattern.
Compute has the heaviest exclusion catalogue: all 6 services use a custom
minimal API (=list()=, =find()=, =save()=, =history()=) that does not match
the template; several repositories and mappers have semantic differences from
production that the template cannot capture.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | BACKLOG                                |
| Parent story | [[id:3B8F2A1D-7C4E-4B6A-9E5D-2F1A8C3B7D9E][Refactor ores.codegen C++ generation]] |
| Now          | Not yet started.                       |
| Waiting on   | [[id:C8A4D7F1-2E6B-4C3A-8F9D-5B1E3C8D2F7A][Apply safe drift to refdata-cpp (pilot)]] |
| Next         | Start once refdata pattern is merged.  |
| Last touched | 2026-05-31                             |

* Acceptance

- =cmake --build --preset linux-clang-debug-make= passes.
- =ctest -R "ores\.compute"= passes.
- Final zero-diff invariant holds after regenerate + restore exclusions.
- All 12 compute exclusions documented in commit message.
- PR raised, reviewed, merged.

* Plan

** 1. Rebase onto =origin/main=
** 2. Regenerate

#+begin_src sh
projects/ores.codegen/codegen.sh regenerate --component compute-cpp --profile all-cpp
#+end_src

** 3. Restore exclusions from HEAD (12 files)

| File | Reason |
|------+--------|
| 6 service =.hpp= files | Custom minimal =list()=, =find()=, =save()=, =history()= API |
| 6 service =.cpp= files | Same |
| =result_repository.{hpp,cpp}= | Custom =read_by_workunit= method |
| =workunit_repository.{hpp,cpp}= | Custom =read_by_batch= method |
| =result_mapper.cpp= | Nullable columns mapped as non-optional with sentinel values |
| =workunit_mapper.cpp= | Same |
| =host_mapper.cpp= | Same |
| =app_repository.cpp= | Reads union =tenant_id == tid \vert\vert tenant_id == system= |
| =app_version_repository.cpp= | Same |

** 4. Build with =make=

Many callers in messaging handlers reference the custom service API. If any
break, additional handler restores may be needed.

** 5. Run compute tests
** 6. Final zero-diff check
** 7. Commit + push + raise PR

* Notes

This task is expected to be the largest exclusion list of any component. If
the exclusion list grows beyond what's reasonable to maintain (>15 files),
consider deferring compute-cpp to a separate cleanup story.

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result
