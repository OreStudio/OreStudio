:PROPERTIES:
:ID: F560ADDF-260A-4ECC-81EB-1C83A32CD5AA
:END:
#+title: Task: Convert remaining ~19 hand-crafted trading instrument sub-types to codegen models
#+description: Bring the remaining ~19 hand-crafted trading instrument sub-types (bond, most equity_*/fx_* variants, commodity, composite + composite_legs, credit, scripted, swap_legs) under codegen, following the equity_position_instrument pilot's proven pattern. Most of these use the legacy Primary key heading format and need task 836BB4CE resolved or worked around per-entity first.
#+type: task
#+level: s1
#+filetags: :port-trading-instruments-to-codegen:sprint_25:v0:
#+owner: marco
#+branch: feature/convert-remaining-trading-instrument-subtypes
#+pr: 1874
#+blocked_on:
#+blocked_since:
#+created: 2026-08-05
#+updated: 2026-08-08
#+environment: clever_dijkstra
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:FBBC0235-C506-440B-A849-4DC96B5921C4][Port trading instruments to codegen]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Bring the remaining ~19 hand-crafted trading instrument sub-types
under codegen, matching what the
[[id:91ED2388-6CC8-433B-95E5-795C8D0D6346][equity_position_instrument
pilot]] already proved: author a modern-format model for each,
regenerate every facet (SQL/domain/entity/mapper/repository/service/
generator) zero-diff against the hand-crafted original, and land the
=has_workspace_id= inheritance query (already fixed generically in
PR #1846) for every one of them. Most of these entities carry the
legacy top-level =* Primary key= heading, so task 836BB4CE (or a
per-entity rewrite into the modern column-property format, as the
pilot did) is a prerequisite for each one individually.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | STARTED                              |
| Parent story | [[id:FBBC0235-C506-440B-A849-4DC96B5921C4][Port trading instruments to codegen]] |
| Now          | 10/19 converted: equity_variance_swap_instrument, fx_forward_instrument, equity_forward_instrument, fx_accumulator_instrument, fx_vanilla_option_instrument, fx_variance_swap_instrument, equity_swap_instrument, fx_barrier_option_instrument, fx_digital_option_instrument, fx_asian_forward_instrument. |
| Waiting on   | Nothing.                               |
| Next         | Convert the next flat instrument (e.g. equity_option). |
| Last touched | 2026-08-08                               |

* Acceptance

- Every remaining hand-crafted trading instrument sub-type (bond,
  remaining equity_*/fx_* variants, commodity, composite +
  composite_legs, credit, scripted, swap_legs) has a codegen model
  authored in the modern format.
- Each regenerates zero-diff against its hand-crafted original
  (beyond genuine, systemic template improvements already proven safe
  by the pilot).
- Full project build green and full test suite passing after each
  batch of conversions (not just at the very end).
- Workspace inheritance (PR #1846) verified working for every
  converted entity.
- Add a repository test proving batch remove() doesn't cross
  workspace boundaries (flagged during PR #1857 review as missing --
  the fix itself was verified correct by inspection across 11
  entities, but no test exercises it).

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

Converting entities one at a time (or in small batches), each fully
verified (build + tests + zero-diff on a second regen pass) before
moving to the next, following the equity_position_instrument pilot's
approach exactly. Picking flat, leg-free instruments first (simplest,
matches the pilot's shape 1:1); composite_instrument/composite_leg
and any swap-leg-bearing entities (which carry genuine custom
transactional service logic beyond plain CRUD) are deferred until the
codegen service template gains proper support for hand-written
extension points -- see Notes.

* Notes

** Follow-up: entire equity_* instrument family missing from drop_trading.sql

Flagged during PR #1874 review: =projects/ores.sql/drop/trading/drop_trading.sql=
has zero =\ir= references to any =trading_equity_*_instruments_drop.sql=
file -- not =equity_position= (the PR #1857 pilot), nor
=equity_forward=/=equity_variance_swap= (this task's entities 3-4), nor
any of the still-hand-crafted equity variants. These =_drop.sql= files
exist on disk but are unreachable dead code: a full =drop.sql= run
leaves every equity instrument table, trigger, and function fully
intact. Pre-existing (predates the pilot), not something to patch
piecemeal per converted entity -- needs a dedicated small cleanup task
wiring up the whole equity_* family in =drop_trading.sql= at once,
mirroring the FX/rates/bond blocks' pattern.

** Composite/leg-bearing entities need a template feature, not just a model

Attempted =composite_instrument= first (after the pilot) since it's
the smallest remaining domain struct (49 lines). Regeneration failed
to build: the hand-crafted =composite_instrument_handler.hpp=
messaging dispatcher calls custom service methods
(=save_composite_instrument(data, legs)=,
=remove_composite_instrument=, =get_legs=/=get_legs_batch=) that
manage the parent + =composite_leg= child relationship
transactionally -- genuine custom logic no codegen service template
currently reproduces. The =Custom repository methods= org heading
exists in the model schema but isn't actually consumed by any
template today (confirmed by grep), so this isn't a quick fix.

=fra_instrument= has the identical pattern with =swap_leg= (custom
=get_swap_legs=/=get_swap_legs_batch=/save-with-legs methods hand-
patched onto its "generated" service file) -- and because
=fra_instrument.org= is permanently blocked from regeneration by task
836BB4CE, this drift has never actually been caught or verified. Any
entity with a *_leg child table (composite_instrument, and
presumably every swap-leg-bearing swap/swaption entity) shares this
gap.

Decision: defer composite_instrument/composite_leg and any other
leg-bearing entities to a follow-up once the service template gains
a genuine paste-block/custom-method extension mechanism (or the
custom methods are otherwise formalised) -- don't force a hand-patch
that would just recreate the same silent-drift problem fra_instrument
already has. Picking flat, single-table instruments first instead.

Also discovered along the way: the shared
=ores.trading.instrument_identity= field-group struct hardcodes the
=instrument_id= field name, so it only fits entities whose primary key
column is literally named =instrument_id= (as in the pilot). Entities
like =composite_instrument= whose PK column is =id= don't fit that
group and would need either a rename (bigger blast radius, changes
the DB column) or to stay flat with no identity grouping (fine --
small entities don't need the C1202 nesting anyway).

** equity_variance_swap_instrument (converted 2026-08-05)

Flat instrument, no leg children, matches the pilot's shape exactly
(instrument_identity + audit_record groups, has_batch_read,
has_workspace_id). Two =date=-typed columns (=start_date=/
=maturity_date=) modelled as =std::string= with =:cpp_type: std::string=,
following the precedent already used by =cap_floor_instrument.org=
and others (ISO 8601 date strings, not =std::chrono::year_month_day=).

Zero-diff regeneration across all facets (SQL, domain, entity,
mapper, repository, service, generator) beyond the same systemic
template improvements already proven safe by the pilot and the
follow-up workspace_id/batch-remove fixes (PR #1857). Verified with a
second regeneration pass producing byte-identical output. Full
project build green; =ores.trading.*= tests pass; full repo =ctest=
71/71 test binaries pass (the one pre-existing, unrelated
=ores.marketdata.core.tests= failure from live DB schema drift
persists, unrelated to this work -- confirmed zero diff in any
marketdata file).

** fx_forward_instrument (converted 2026-08-05)

Flat instrument, no leg children, matches the pilot's shape exactly.
Same zero-diff outcome, verified with a second regeneration pass
(byte-identical output) and =ores.trading.*= tests green.

** equity_forward_instrument (converted 2026-08-05)

Flat instrument, no leg children. Same zero-diff outcome, verified
with a second regeneration pass (byte-identical output) and
=ores.trading.*= tests green.

** fx_accumulator_instrument (converted 2026-08-05)

Flat instrument, no leg children. Its =trade_type_code= check is a
single-value equality (== 'FxAccumulator'=), not an =in (...)= list --
modelled accordingly in =* SQL / ** Checks=. Same zero-diff outcome,
verified with a second regeneration pass (byte-identical output) and
=ores.trading.*= tests green.

** fx_vanilla_option_instrument (converted 2026-08-05)

Flat instrument, no leg children. Same zero-diff outcome, verified
with a second regeneration pass (byte-identical output) and
=ores.trading.*= tests green.

** fx_variance_swap_instrument (converted 2026-08-06)

Flat instrument, no leg children. Has a cross-column check
(=end_date > start_date=) -- modelled directly in =* SQL / ** Checks=
and preserved verbatim by regeneration. Every generated file diffed
by hand before building (per explicit review requirement): all
changes additive/cosmetic or the same systemic template improvements
already proven across the prior 5 entities: SQL header/hardening,
domain doc-comments + =entity_type_of()=, entity field reorder +
=db_timestamp= alias, mapper field reorder, repository
workspace-scoping (including the batch-remove =workspace_id= fix)
plus =read_at_version=/pagination/count, service list/count/batch-
save/delete/delete-batch/history, generator field values matching
the model exactly. No renamed/orphaned files -- every file with a
hand-crafted predecessor was modified in place at its original path.
Zero further drift confirmed via a second regeneration pass;
=ores.trading.*= tests green.

** equity_swap_instrument (converted 2026-08-08)

Flat instrument, no leg children. Carries the
=:ores.cpp.eventing-integration-test.enabled: true= property (per
explicit direction: every entity converted going forward gets the
eventing facet), so this conversion additionally generates the
changed-event, messaging protocol/handler/registrar, presentation
history_field_mapper, service event registrar, and the eventing
integration test -- the full per-entity eventing stack.

Two generator-value subtleties surfaced during verification (both
caught by the eventing integration test hitting the live DB):
- =payment_frequency= generated ="3M"=, which
  =ores_refdata_validate_payment_frequency_fn= rejects (only Once,
  Annual, Semiannual, Quarterly, Bimonthly, Monthly, Lunarmonth,
  Weekly, Daily). Changed the generator block to ="Quarterly"=
  (semantically 3M).
- =basket_json= generated ="[]"=, which maps to a non-NULL DB value
  and violates the XOR check
  (=EquitySwap= requires basket_json NULL). The mapper maps empty
  strings to SQL NULL, so the generator block now emits
  =std::string("")=, keeping basket_json NULL for the default
  EquitySwap row.

Also carries the hand-crafted trade_type index (the only equity
variant with one; fx_variance_swap never had one) -- preserved
verbatim in the model. XOR check, workspace validation, and the
batch-remove workspace fix all verified present after regeneration.
Zero further drift via a second regeneration pass; build green;
=ores.trading.{api,core,service}.tests= all pass via the
=test_ores.trading.*.tests= targets (core: 69 cases incl. the eventing
integration test).

** fx_variance_swap_instrument: eventing facet added (2026-08-08)

Per the same direction, the already-converted
=fx_variance_swap_instrument= gained the
=:ores.cpp.eventing-integration-test.enabled: true= property and was
regenerated. Purely additive: 10 new files (changed-event,
protocol/handler/registrar, history_field_mapper, event registrar,
eventing integration test), zero modifications to any existing
generated file, zero further drift on a second pass. Build green;
=ores.trading.core.tests= passes 70 cases incl. the new
=write_fx_variance_swap_instrument_publishes_nats_changed_event=.

** fx_variance_swap_instrument: messaging path reconciled (2026-08-08)

Caught while converting fx_barrier_option_instrument: the variance-swap
conversion (which landed after the eventing task closed) never adopted
the generated messaging path. Its hand-crafted
=save_fx_variance_swap_instrument_request/=_response= struct still
lived in =instrument_protocol.hpp=, its =save_variance_swap()= branch
still served the save subject from =typed_fx_instrument_handler.hpp=,
and its generated registrar was never wired -- a latent duplicate-
struct-definition collision with the generated
=fx_variance_swap_instrument_protocol.hpp= (same names, same
namespace), plus a dead generated registrar. Applied the standard
reconciliation: deleted the hand-crafted struct from
=instrument_protocol.hpp= (and its now-unused domain include), deleted
=save_variance_swap()= and its service include from the typed handler,
removed the typed subscription from =registrar_fx.cpp= and wired
=register_fx_variance_swap_instrument_handlers= in its place. Its four
consumers (=ore_import_execute_handler=, =OreImporter=,
=ImportTradeDialog=, =FxVarianceSwapInstrumentForm=) now include the
generated protocol; the generated struct is shape-identical (same
=data= member, same =nats_subject=), so no call-site changes were
needed. Subject unchanged -- subscribers are unaware.

** fx_digital_option_instrument (converted 2026-08-08)

Flat instrument, no leg children, eventing facet enabled. Richest
optional-numeric column set in the family: =strike=,
=lower_barrier=, =upper_barrier= all =std::optional<double>=. =strike=
is the first optional-double in the converted family to carry a
generator block -- emitted verbatim as =r.strike = 1.1;=, which
compiles because =std::optional<double>= is implicitly constructible
from =double=. The two barrier levels have no generator blocks
(default =std::nullopt=), matching the plain-FxDigitalOption test data
(barrier fields absent). =option_type= (Call), =barrier_type=,
=description= nullable with empty-string defaults per the empty-to-NULL
mapper convention. =long_short= carries the in-check ('Long',
'Short'); the =trade_type_code= check is the 4-value in-list of the
digital/touch family.

As with fx_barrier: the notify-trigger drop file did not exist before
this conversion, so the missing =\\ir= line was added to
=drop_trading.sql=. Also wired the *pre-existing but unwired*
=trading_fx_variance_swap_instruments_notify_trigger_drop.sql= into
=drop_trading.sql= -- the variance-swap conversion regenerated its
drop.sql without the inline notify drops but never added the =\\ir=
line, leaving its notify trigger/function un-droppable via a full
=drop.sql= run since the conversion.

Messaging path reconciled in the same commit (hand-crafted save struct
deleted from =instrument_protocol.hpp=, =save_digital_option()= removed
from =typed_fx_instrument_handler.hpp=, typed subscription replaced
with =register_fx_digital_option_instrument_handlers= in
=registrar_fx.cpp=; consumers migrated to the generated protocol).

Zero-diff verified via a second regeneration pass. Build green;
=ores.trading.{api,core,service}.tests= all pass (api: 98 cases; core:
72 cases incl. the new
=write_fx_digital_option_instrument_publishes_nats_changed_event=;
service: 10 cases).

** fx_barrier_option_instrument (converted 2026-08-08)

Flat instrument, no leg children, eventing facet enabled per standing
direction. Carries the only =trade_type_code= check in the family that
is an =in (...)= list of 5 barrier product types; modelled verbatim.
=upper_barrier= is the first =std::optional<double>= column in the
converted family: nullable numeric(28,10) with =cpp_type:
std::optional<double>=, no generator block, so the default synthetic
row leaves it =std::nullopt= (mapper pass-through to NULL) -- matching
the single-barrier test data. =settlement=,
=underlying_code=, =description= are nullable with no generator blocks
(empty string, mapped to SQL NULL per the empty-to-NULL convention).

All existing files modified in place at their original paths; new
files are the standard generated stack (table/table_io, generator,
changed-event, protocol/handler/registrar, history_field_mapper, event
registrar, eventing integration test). The
=trading_fx_barrier_option_instruments_notify_trigger_drop.sql= file
did *not* exist before this conversion -- the hand-crafted
=trading_fx_barrier_option_instruments_drop.sql= carried the notify
trigger/function drops inline. The regenerated pair split them, so the
missing =\\ir= line was added to =drop_trading.sql= (same fix the PR
#1874 review applied to fx_accumulator/fx_vanilla_option/fx_forward).

Messaging path reconciled in the same commit: hand-crafted
=save_fx_barrier_option_instrument_request/=_response= deleted from
=instrument_protocol.hpp=, =save_barrier_option()= deleted from
=typed_fx_instrument_handler.hpp=, typed subscription removed from
=registrar_fx.cpp= and replaced with
=register_fx_barrier_option_instrument_handlers=; the four consumers
(=ore_import_execute_handler=, =OreImporter=, =ImportTradeDialog=,
=FxBarrierOptionInstrumentForm=) include the generated protocol.

Zero-diff verified via a second regeneration pass. Build green;
=ores.trading.{api,core,service}.tests= all pass (api: 98 cases; core:
71 cases incl. the new
=write_fx_barrier_option_instrument_publishes_nats_changed_event=;
service: 10 cases).

** All converted instruments bound to the trading-instrument variability profile (2026-08-08)

Per review direction, every converted instrument model must follow the
standard model shape and bind to a variability profile instead of
carrying scope knobs inline. Created
=projects/modeling/variability_trading_instrument.org= (scaffolded via
=compass add profile=) assigning =has_tenant_id=, =has_workspace_id=
and =party_id_from_session=, and bound all 10 converted instrument
models to it: root Flags now carry only
=schema/product/component/:profile:=, and the SQL Flags drawer carries
only the =tablename=. All 10 verified zero-diff after the retrofit
(via =compass codegen entity generate <entity> --diff=).

** fx_asian_forward_instrument (converted 2026-08-08)

Flat instrument, no leg children, eventing facet enabled. Routes two
ORE product types (FxAverageForward, FxTaRF) -- the =trade_type_code=
check is the 2-value in-list =('FxAverageForward', 'FxTaRF')=.
=reference_currency/reference_notional/settlement_currency/
settlement_notional/payment_date/long_short= are
FxAverageForward-specific; =currency/fixing_amount/target_amount/
strike= are FxTaRF-specific (nullable with no generator blocks, left
empty). Carries the hand-crafted unique index on =trade_id= (with
=where_extra: trade_id is not null=) -- preserved verbatim in the
model. =payment_date= is a =date=-typed column modelled as
=std::string= (ISO 8601), per family precedent.

Messaging path reconciled in the same commit -- and this time the
typed-FX pattern is *gone outright*: deleting the asian_forward save
struct left =instrument_protocol.hpp= with no typed section at all and
=typed_fx_instrument_handler.hpp= with no methods left, so the whole
file was deleted (=git rm=). The typed subscription in
=registrar_fx.cpp= is replaced with
=register_fx_asian_forward_instrument_handlers=; the four consumers
(=ore_import_execute_handler=, =OreImporter=, =ImportTradeDialog=,
=FxAsianForwardInstrumentForm=) include the generated
=fx_asian_forward_instrument_protocol.hpp=; the generated struct is
shape-identical (same =data= member, same
=trading.v1.fx_asian_forward_instruments.save= subject), so no call-site
changes were needed.

The =trading_fx_asian_forward_instruments_notify_trigger_drop.sql= file
did not exist before this conversion (the hand-crafted drop.sql carried
the notify drops inline), so the missing =\\ir= line was added to
=drop_trading.sql=, per the established pattern.

Zero-diff verified via a second regeneration pass. Build green;
=ores.trading.{api,core,service}.tests= all pass (api: 98 cases; core:
73 cases incl. the new
=write_fx_asian_forward_instrument_publishes_nats_changed_event=;
service: 10 cases).

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1874][#1874]] | [codegen,trading] Convert 5 flat trading instrument sub-types to codegen |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | drop_trading.sql not updated with \ir lines for the 3 new fx_*_instruments_notify_trigger_drop.sql files this PR extracted (flagged independently by 2 of 3 reviewers) | drop_trading.sql | Fixed | Added the 3 missing \ir lines (fx_accumulator, fx_vanilla_option, fx_forward). Verified for real: ran the actual modular drop.sql against the live dev DB -- it walked through the entire trading section (including the newly-fixed lines) with zero errors before failing much later on an unrelated, pre-existing controller-component dependency issue. Recreated the DB afterward and re-ran ores.trading.* tests, all green. |
| 2 | Entire equity_* instrument family (equity_position, equity_forward, equity_variance_swap, equity_option, equity_swap, equity_accumulator, ...) is absent from drop_trading.sql -- a pre-existing gap this PR's 2 new equity entities fall into | drop_trading.sql | Declined (this PR) | Confirmed pre-existing (predates PR #1857's pilot too), and larger than this PR's scope per the reviewer's own suggestion to flag rather than block. Filed as a follow-up note below; a dedicated cleanup task should wire up the whole equity_* family at once rather than patching it piecemeal per PR. |
| 3 | read_at_version() doesn't filter by workspace_id, unlike read_latest/remove | *_repository.cpp (all 5 new entities) | Declined (this PR) | Inherited unchanged from the pilot (PR #1857) -- not a regression introduced here. Whether version history should be workspace-scoped is a design question for the workspace story, not something to silently change per-entity as it gets copied forward. |
| 4 | Trailing double blank line in regenerated repository .cpp files | *_repository.cpp (all 5 new entities) | Declined | Cosmetic template artifact, not worth a template change for. |

* Result
