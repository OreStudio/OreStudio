:PROPERTIES:
:ID: 13255AAF-C142-4A65-A56A-065D456BA952
:END:
#+title: Task: Import/export cleanup: composite legs, pure parser, batch fetches, trade list split
#+description: - Phase 1: Unify instrument variant hierarchy — introduce `Instrument` concept, `with_legs<T,Leg>`, `trade_instrument` v
#+type: task
#+level: s1
#+filetags: :import_export_cleanup:sprint_17:v0:
#+owner: marco
#+branch:
#+pr: https://github.com/OreStudio/OreStudio/pull/728
#+blocked_on: none
#+blocked_since:
#+created: 2026-05-22
#+updated: 2026-05-22
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:E62BF8DD-FD3B-4283-A1A9-3FAF1F7A1703][Import/export cleanup]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Introduce a unified instrument variant hierarchy, fix composite legs being silently dropped on ORE import, and strip UUID minting out of the parser into the planner layer.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:E62BF8DD-FD3B-4283-A1A9-3FAF1F7A1703][Import/export cleanup]] |
| Now          | Completed 2026-05-06.               |
| Waiting on   | Nothing.                               |
| Next         | None.                                  |
| Last touched | 2026-05-22                               |

* Acceptance

- Unified instrument variant hierarchy is in place.
- Composite legs are no longer silently dropped on ORE import.
- UUID minting is moved out of the parser into the planner layer.

* Plan

* Notes

** Summary

- *Phase 1*: Unify instrument variant hierarchy — introduce =Instrument= concept, =with_legs<T,Leg>=, =trade_instrument= variant, =swap_instrument_data=, =composite_instrument_data=; replace all ad-hoc instrument carrier types
- *Phase 2*: Fix composite legs gap in ORE importer — composite legs were silently dropped on import
- *Phase 3*: Pure parser — strip UUID minting out of =import_portfolio_with_context=; move it to the planner layer; delete the context-free =import_portfolio= overload
- *Phase 4*: Eliminate N+1 DB queries on bulk export — add =read_latest(ctx, ids)= batch overloads to all 30 instrument repositories, =get_TYPE_instruments(ids)= to all 30 services, and =get_swap_legs_batch= / =get_legs_batch= for the shared leg tables; replace per-trade =populate_instrument_for_trade= with =populate_instruments_for_trades= that groups by type and issues one DB call per type; fix =export_trades_to_storage= to page at 1000 instead of loading unbounded
- *Phase 6*: Split trade list from trade detail — =get_trades_response= now returns metadata-only =vector<trade>>=; new =get_trade_detail_request= fetches a single trade's instrument on demand; Qt models cache =trade= not =trade_export_item=; =TradeController= fetches detail synchronously before opening the dialog


* Result

Landed.
