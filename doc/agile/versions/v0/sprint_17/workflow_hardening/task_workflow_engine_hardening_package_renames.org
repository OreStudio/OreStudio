:PROPERTIES:
:ID: A4BC5AB1-4F4E-4BFC-A8A8-152379E6CE7E
:END:
#+title: Task: Workflow engine hardening + package renames
#+description: - Item 1: Dynamic step lists — `workflow_instance` gains `step_count` and `materialised_steps_json` so the step sequence
#+type: task
#+level: s1
#+filetags: :workflow_hardening:sprint_17:v0:
#+owner: marco
#+branch:
#+pr: https://github.com/OreStudio/OreStudio/pull/742
#+blocked_on: none
#+blocked_since:
#+created: 2026-05-22
#+updated: 2026-05-22
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:7030AB78-B8F1-425C-83EF-A6F9EBF218A8][Workflow engine hardening]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Add persisted materialised step lists, move workflow definitions to their owning API packages, and add per-step error reporting and dynamic step count to the workflow engine.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:7030AB78-B8F1-425C-83EF-A6F9EBF218A8][Workflow engine hardening]] |
| Now          | Completed 2026-05-15.               |
| Waiting on   | Nothing.                               |
| Next         | None.                                  |
| Last touched | 2026-05-22                               |

* Acceptance

- Add persisted materialised step lists, move workflow definitions to their owning API packages, and add per-step error reporting and dynamic step count to the workflow engine.
- PR #742 merged and landed on main.

* Plan

* Notes

** Summary

- *Item 1*: Dynamic step lists — =workflow_instance= gains =step_count= and =materialised_steps_json= so the step sequence is persisted at instance start and survives service restarts
- *Item 2*: Move workflow definitions to owning API packages — =workflow_step_def=, =materialised_step=, =workflow_definition=, and =workflow_registry= moved to =ores.workflow.api=; per-domain workflow definitions moved to =ores.refdata.api=, =ores.ore.api=, and =ores.reporting.api=
- *Item 3*: Idempotency guard for workflow step handlers — domain services skip duplicate commands using the =X-Workflow-Step-Id= header
- *Item 4*: Reusable =WorkflowStepsWidget= — Qt component for displaying live step progress, wired into the ORE import wizard's Done page
- *Renames*: =ores.workflow= → =ores.workflow.core=, =ores.ore= → =ores.ore.core=, =ores.telemetry= → =ores.telemetry.core= to align with the standard =ores.*.core= naming convention
- *Build*: Add sccache as compiler launcher across all CMake presets for shared compilation cache


* Result

Landed.
