:PROPERTIES:
:ID: A7BB331B-CF2E-4072-92D0-CF0C13699625
:END:
#+title: Task: Scaffold ores.orgmode component
#+description: Scaffold the ores.orgmode component (simple/flat component, per component_architecture.org — no API/service split needed for a pure parsing library) via codegen, following this project's usual component layout, ready for the parser/link-resolution implementation.
#+type: task
#+level: s1
#+filetags: :org:codegen:scaffold:ores_orgmode_component:sprint_22:v0:
#+owner: marco
#+branch: agile/qa-validation-runner-story
#+pr: 1457
#+blocked_on:
#+blocked_since:
#+created: 2026-07-06
#+updated: 2026-07-06
#+environment: bright_faraday
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:A85B16E3-5A42-4484-A570-13EE7CFA9F50][ores.orgmode: C++ org-mode parsing and org-roam link resolution]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Run =codegen-add-component= to scaffold =ores.orgmode= with this
project's standard simple-component layout (no api/core/service split — this is a pure parsing library, not a DB/service-backed domain component) and CMake wiring, so the
parser/link-resolution work in the following tasks has somewhere to
land. No parsing logic here — just a building, empty-but-wired
component.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:A85B16E3-5A42-4484-A570-13EE7CFA9F50][ores.orgmode: C++ org-mode parsing and org-roam link resolution]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-06                               |

* Acceptance

- [X] =ores.orgmode= exists as a simple/flat component (no api/core/
  service split), following the same CMake/component conventions as
  e.g. =ores.diff=.
- [X] Component builds clean (empty stub implementation) and is wired
  into the top-level build.

* Plan

Scaffolded =projects/ores.orgmode/modeling/component_overview.org= via
=compass add component=, then generated the C++/CMake scaffold via
=./projects/ores.codegen/codegen.sh generate --address ores.cpp.component=
and =--address ores.cmake.component= (the flat-component archetypes;
=--profile component= is the deprecated legacy alias and doesn't
resolve to an address in the current graph-based codegen). Registered
=ores.orgmode= in =projects/CMakeLists.txt= (dependency order: near
=ores.diff=, before anything that will consume it). Added a PlantUML
stub reflecting the *planned* shape (document/heading/link/parser/
resolver), not yet implemented. Configured and built
=ores.orgmode.lib=/=ores.orgmode.tests= clean; stub test passes.

Fixed one incidental bug in passing: =compass add component= stamps
=#+type: component=, but codegen only recognises =#+type:
ores.codegen.component= — every other =component_overview.org= in the
repo already uses the long form. Worked around by hand-editing this
one file; the scaffold template itself should be fixed separately (not
in scope here).

* Notes

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1457][#1457]] | [org] Add ores.orgmode: C++ org-mode parsing and org-roam link resolution |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Missing =#include <cctype>= for =std::toupper= | parser.cpp | Accepted | Fixed in 9197362b2. |
| 2 | Unused =logger_name=/=lg()= in orgmode_commands | orgmode_commands.hpp/.cpp | Accepted | Fixed in 9197362b2 — all output goes through the passed ostream. |
| 3 | =unquote()= escaping logic hard to read at a glance | resolver.cpp | Accepted | Added a concrete before/after example to the docstring in 9197362b2. |
| 4 | =find_keyword=/=id()= are case-sensitive; org-mode itself isn't | document.hpp | Accepted | Documented explicitly in 9197362b2; not a live bug (this repo's corpus is consistent), just clarified. |
| 5 | Unterminated =:PROPERTIES:= drawer swallows rest of file | parser.cpp | Declined | Already documented as a known simplification; corpus test confirms no real doc hits it. |
| 6 | Prepared statement not cached across =resolve()= calls | resolver.cpp | Declined | Not a hot path yet (single lookup per =doc-show= call); revisit if a future consumer resolves many links per document. |
| 7 | Bare =**= heading (no title text) falls through to body text | parser.cpp | Declined | Consistent with the documented restricted-subset scope; no real doc in the corpus relies on that form. |

* Result

=ores.orgmode= exists as a simple/flat component
(=projects/ores.orgmode/= with =include=, =src=, =tests=, =modeling=
directly inside), scaffolded via codegen's =ores.cpp.component= and
=ores.cmake.component= addresses from
=projects/ores.orgmode/modeling/component_overview.org=. Registered in
=projects/CMakeLists.txt=. Builds clean; stub test
(=ores.orgmode.tests=) passes. PlantUML stub committed reflecting the
planned (not yet implemented) document/heading/link/parser/resolver
shape. Both acceptance criteria met — ready for the parser port
(next task).
