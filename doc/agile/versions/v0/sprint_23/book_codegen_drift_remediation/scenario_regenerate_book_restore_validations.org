:PROPERTIES:
:ID: 7DD2C147-1CED-4D8A-AE6E-F1CA1636FA7F
:END:
#+title: Test Scenario: Verify book list/detail/history and insert-trigger validations after codegen regeneration
#+description: Manual Qt client steps confirming the regenerated book entity (list, detail, history windows, save, badges, change-reason cache) works end to end with no regressions, and that the 5+ insert-trigger validations (soft FKs, value checks) survived regeneration.
#+type: test_scenario
#+level: s1
#+filetags: :book_data_model_cleanup:sprint_23:v0:book_codegen_drift_remediation:
#+target_dialog:
#+created: 2026-07-12
#+updated: 2026-07-12
#+environment:
#+todo: PENDING | PASSED FAILED
#+startup: inlineimages

This page documents a test scenario verifying [[id:0C185CE4-63E5-44CC-94AF-F3FF1E194986][Regenerate book and restore insert-trigger validations]] in [[id:F0DAFC8D-3C94-4F97-B546-D7AF7469A29E][Book codegen drift remediation]]. It is filled in with the target dialog and checklist of steps before testing starts; the QA Validation Runner panel rewrites =* Results= in place on save.

* Scenario Info

| Field         | Value                                   |
|---------------+------------------------------------------|
| Verifies task | [[id:0C185CE4-63E5-44CC-94AF-F3FF1E194986][Regenerate book and restore insert-trigger validations]] |
| Parent story  | [[id:F0DAFC8D-3C94-4F97-B546-D7AF7469A29E][Book codegen drift remediation]]   |
| Target dialog | BookMdiWindow / BookDetailDialog / BookHistoryDialog (Reference Data > Books) |
| Clients       |                                          |
| State         | PENDING                               |

* Prerequisites

- Services running: =compass services start=.
- A fresh, Barclays-provisioned database:
  #+begin_src sh :results verbatim
  compass db recreate -y -k
  compass services start
  compass shell -f projects/ores.shell/scripts/library/provisioning/barclays_system_provision.ores
  #+end_src
- A running Qt client, not yet logged in.

* Steps

Each step is its own heading — the title should be short (it's shown
as a single list entry in the QA Validation Runner); put any longer
instructions in the body below the title. The panel writes each
step's PASS/FAIL/PENDING outcome and notes back as a =*** Result=
child heading directly under it.

** Log in and select Barclays Plc

Log in as =tenant_admin@barclays_plc= / =Secure-Password-123=, select
=BARCLAYS PLC=. Confirm all menus enable with no errors.

*** Result

| Field  | Value |
|--------+-------|
| Status | PASS |

** Books list opens from Reference Data and shows badges

Open Reference Data > Books. Confirm the list opens, shows seeded
rows, and that the Book Status and Regulatory Book Type columns render
as colour-coded badges (not plain text).

*** Result

| Field  | Value |
|--------+-------|
| Status | PASS |

** Detail dialog opens, shows badge combos, and ID is read-only

Double-click a book row. Confirm the detail dialog opens, Book Status
and Regulatory Book Type render as badge-coloured combos, and the ID
field is read-only (not editable) even in edit mode.

*** Result

| Field  | Value |
|--------+-------|
| Status | PASS |

** Edit and save a book

Change a book's description and Save. Confirm the change-reason prompt
appears, the save succeeds, the grid refreshes, and Version increments
by 1.

*** Result

| Field  | Value |
|--------+-------|
| Status | PASS |

** History dialog opens and shows the new version

Open History on the edited book. Confirm both versions appear with
distinct timestamps, and closing the history dialog only closes the
sub-window, not the whole app.

*** Result

| Field  | Value |
|--------+-------|
| Status | PASS |

** Attempt to save a book with an invalid portfolio (should be rejected)

If reachable from the UI (or via a raw insert if not), attempt to save
a book referencing a non-existent parent portfolio. Confirm the save
is rejected with a clear error, not silently accepted — this is the
soft-FK insert-trigger validation from PR #1449 (already confirmed via
direct SQL insert during this task, but worth reconfirming through the
UI path too if practical).

*** Result

| Field  | Value |
|--------+-------|
| Status | PASS |
| Notes  | Not directly testable via UI or ores.shell -- Parent Portfolio is a constrained picker (not free-text), so the UI structurally cannot submit a non-existent id, and ores.shell has no book commands (pre-existing gap, unrelated to this task). Validation already confirmed firing correctly via a direct SQL insert during this task's Plan (rejected with "Invalid parent_portfolio_id ... No active portfolio found"). |

** No error toast or log error during the whole flow

Check the client's log tail
(=build/output/<preset>/publish/log/ores.qt.<colour>.log=) for any
ERROR-level line — in particular no "Server reported failure" message
(the new success-check added to ClientBookModel.cpp).

*** Result

| Field  | Value |
|--------+-------|
| Status | PASS |
| Notes  | check logs |

* Results

| Field         | Value |
|---------------+-------|
| Status        | PASSED |
| Completed at  | 2026-07-12T10:23:22Z |
| Branch        | feature/regenerate-book-restore-validations |
| Commit        | e494d64ca |
| Worktree      | bright_faraday |

* Notes
