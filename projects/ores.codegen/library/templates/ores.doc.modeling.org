:PROPERTIES:
:ID: 3D46BA01-BAD7-44B2-BD70-FF30394698F0
:END:
#+title: ores.doc.modeling
#+description: Entity-modeling doc scaffolds: entity_org, field_group, table, junction, lookup_entity, service_registry, dataset_overview.
#+type: facet
#+level: cross
#+facet_group: ores.doc
#+filetags: :codegen:physical-space:doc:
#+created: 2026-06-27
#+updated: 2026-07-16

This page is the literate source for the =ores.doc.modeling= facet of the
codegen template library. Each section documents one template and
holds its source in a =mustache= block tangled to the sibling
=.mustache= file consumed by =generator.py=. The =.mustache= files are
generated artefacts — edit this document, then run the tangle
(=compass build --direct tangle_codegen_templates= or
=projects/ores.lisp/src/ores-build-codegen-templates.el= directly).
Output paths and profile membership are documented in each template's row
of the Archetypes table below.

* Summary

Codegen model-authoring scaffolds: entity_org (the literate codegen
entity model — frontmatter plus the section/table skeleton the
org_loader parses), field_group, table (SQL-only), junction,
lookup_entity (batch-loaded), service_registry, and dataset_overview.
No profile involvement.

* The ores.doc.modeling facet

Scaffolds for the source-of-truth org files that drive the rest of
codegen — these are inputs, not generated outputs, of the entity
facets in [[id:C3F9DCE5-200A-4324-8904-B56BB73FD9CB][ores.cpp]] and [[id:230D4E62-E2CC-464E-8EFD-E5CF2FA7EB0E][ores.sql]]. Each scaffold matches the section/table
shape its own loader parses (Primary key, Columns, Left/Right FK
sections for junctions, one section per service for the registry).

* Mustache incantations

Same identity-escape rendering as the rest of the group. The junction
and lookup-entity scaffolds carry frontmatter flags
(={{#has_tenant_id}}=-style) mirroring the flags their generated SQL
branches on.

* Templates

(One subheading per template: prose covering role, model inputs,
output artefact and profiles, followed by the source block. The first
line of every block is the output-neutral GENERATED FILE header:)

#+begin_example
,#+begin_src mustache :tangle <name>.mustache
{{! GENERATED FILE — tangled from <path to this doc>. Edit the org source. }}
...template source...
,#+end_src
#+end_example

* Archetypes

Per-template archetype docs for this facet have not yet been migrated
onto the physical-space graph — the templates still live as flat,
=#+type: knowledge=-tagged docs in this directory pending that
follow-up.

* See also

- (Parent template group doc: =<group>_group.org=.)
- [[id:3B6B9F75-BDC9-4AE9-B16E-5A05FFC3A4B5][Codegen template library]] — the groups overview.
