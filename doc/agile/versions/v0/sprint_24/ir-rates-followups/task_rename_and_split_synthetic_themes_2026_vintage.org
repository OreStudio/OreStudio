:PROPERTIES:
:ID: 8A6B2BFC-9E40-4AF6-BFE2-DB59922E614C
:END:
#+title: Task: Rename synthetic themes and add 2026 Realistic vintage dataset
#+description: Rename the 'basic'/'realistic' synthetic FX+IR themes to their clearer identities (Uniform Volatility Demo, 2016 ORE Samples), split out a new 2026 Realistic theme sourced from a real 2026-05-05 FX driver-rates vintage, add the QA screenshot capture fix needed to test it under WSLg, and file the follow-up tasks the manual test scenario surfaced.
#+type: task
#+level: s1
#+filetags: :ir-rates-followups:sprint_24:v0:
#+owner: marco
#+branch: feature/rename-and-split-synthetic-themes-2026-vintage
#+pr: 1683
#+blocked_on:
#+blocked_since:
#+created: 2026-07-23
#+updated: 2026-07-23
#+environment: bright_faraday
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:C29FE5A3-61F0-4D2F-BE4F-8A02223EABEF][IR Rates synthetic data: dataset seeding, index cleanup, dual-curve, quoting conventions]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Give the synthetic FX/IR data themes clearer, self-explanatory
names and add a genuinely current real-vintage theme:

- Rename the "basic" theme to "Uniform Volatility Demo" (exaggerated
  uniform volatility, for visual demo purposes) and "realistic" to
  "2016 ORE Samples" (calibrated to plausible 2016 real FX
  volatility, aligned to ORE's own bundled 2016-02-05 reference
  vintage).
- Add a new "2026 Realistic" theme sourced from a real
  2026-05-05 Federal Reserve H.10 FX driver-rates vintage
  (=marketdata_fx_driver_rates_2026_populate.sql=,
  =tools/fetch_fed_h10_rates.sh=), so there is a current-vintage
  option alongside the 2016 one.
- Add the =MarketSimulatorWindow= Start-at-Root guard
  (=promptThemeAndStart()=) so starting from the tree root prompts
  for which theme to run instead of cascading all mutually-exclusive
  vintages at once.
- Fix =QaValidationRunnerWidget::captureScreenshot()= to grab the
  app's own top-level window via =QWidget::grab()= instead of
  =QScreen::grabWindow(0)= on the X11 root window, which returned
  black frames under WSLg's compositing XWayland setup -- needed to
  actually run the manual test scenario for this work.
- Manually test the above via a new =test_scenario= doc and file the
  three follow-up bugs/gaps it surfaced (duplicate per-asset-class
  theme folders, 2026 Realistic missing its vintage FX driver rates
  on provision, and a =find_series= false-negative in vintage
  validity) as separate tasks under this story rather than fixing
  them inline.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:C29FE5A3-61F0-4D2F-BE4F-8A02223EABEF][IR Rates synthetic data: dataset seeding, index cleanup, dual-curve, quoting conventions]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-23                               |

* Acceptance

- The Market Simulator tree shows the three renamed/added themes
  (Uniform Volatility Demo, 2016 ORE Samples, 2026 Realistic).
- Starting at the tree root prompts for a theme instead of
  cascading all of them; cancelling is a no-op; picking one starts
  only that theme's feeds.
- The QA Validation Runner's screenshot button produces a real,
  non-black capture of the app window under this WSLg environment.
- The three gaps found during manual testing (duplicate theme
  folders, missing 2026 vintage data on provision, false-negative
  vintage validity) are filed as their own tasks under this story,
  not silently left for a future tester to rediscover.

* Plan

Renamed the "basic"/"realistic" synthetic FX+IR populate scripts to
=uniform_demo=/=ore_samples_2016=, added a sibling
=realistic_2026= dataset sourced from a real Fed H.10 2026-05-05
vintage (fetched via the new =tools/fetch_fed_h10_rates.sh=), wired
=promptThemeAndStart()= into =MarketSimulatorWindow='s Start-at-Root
path so cascading every theme at once is no longer possible, then
fixed the QA screenshot capture bug blocking manual verification
under WSLg and ran the scenario end to end against a freshly
provisioned Barclays Plc tenant.

* Notes

Manual testing surfaced three real gaps beyond this task's scope,
filed as separate tasks under this story rather than fixed inline:
duplicate per-asset-class theme folders
([[id:3461BD8C-B8B1-4037-9B3D-D07AF9EA4768][task]]), 2026 Realistic
missing its vintage FX driver rates on provision
([[id:094075A2-1443-4DE0-B7B4-433C5F10F572][task]]), and a
=find_series= false-negative in vintage validity
([[id:2DD250F6-7B4C-4204-B742-AC24290F7A81][task]]).

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario                                                            | State  | Notes                                              |
|----------------------------------------------------------------------+--------+-----------------------------------------------------|
| [[id:90B1018E-C019-4C75-B0EE-91284A1E1A75][Synthetic data themes and Start-at-Root guard]] | FAILED | Step 1 passed; step 2 failed (duplicate folders, red vintage badges) — both filed as follow-up tasks, not blocking this task's close. |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1683][#1683]] | [synthetic,qt,sql] Rename synthetic themes and add 2026 Realistic vintage dataset |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | ~promptThemeAndStart()~ calls ~startFolderAsync("")~ unconditionally for folder-less collections, unlike ~onStartFeedClicked()~'s guarded fallback | MarketSimulatorWindow.cpp | Fixed | Added the same empty-~folderId~ guard, falling back to ~startPairsAsync(pairsUnderIndex(...))~ + ~startIrCurvesAsync(irCurvesUnderIndex(...))~ |
| 2 | Both IR themes mark every curve ~auto_start=true~; cross-theme auto-start on the system tenant isn't server-guarded | SQL (both IR populate scripts) | Declined | Not a regression from this PR; the mutual-exclusivity invariant is intentionally client-side today (~promptThemeAndStart()~) per the existing design; flagged as a future hardening concern, not blocking |
| 3 | ~QaValidationRunnerWidget.cpp~ WSLg screenshot fix | QaValidationRunnerWidget.cpp | Accepted (no change) | Reviewer confirmed no issues |

* Result

Shipped: renamed the "basic"/"realistic" synthetic themes to
Uniform Volatility Demo / 2016 ORE Samples, added a new 2026
Realistic theme sourced from a real 2026-05-05 Fed H.10 FX vintage,
added the Start-at-Root theme picker
(=MarketSimulatorWindow::promptThemeAndStart()=) so starting from
the tree root now prompts for a theme instead of cascading every
mutually-exclusive vintage at once, and fixed
=QaValidationRunnerWidget::captureScreenshot()= to use
=QWidget::grab()= instead of the X11 root-window grab that returned
black frames under WSLg.

Acceptance met except the last bullet's "no gaps left silently for a
future tester" clause, which is satisfied by filing rather than
fixing: manual testing (via the new "Synthetic data themes and
Start-at-Root guard" scenario) found the tree still shows each
theme's FX and IR asset classes as two separate top-level nodes
(diagnosed but not fixed — root cause and proposed fix captured in
the follow-up task) and that FX vintage badges render red more
broadly than expected (two distinct causes found: 2026 Realistic's
vintage data is never published by standard party provisioning, and
a separate =find_series= false-negative affects themes with correct
data too). All three filed as their own tasks under this story
rather than blocking this one's close.

* Result
