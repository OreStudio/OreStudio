:PROPERTIES:
:ID: EDA1C2B2-C029-4BFC-8DEF-A15E8B369B4A
:END:
#+title: Task: Build the booking screen prototype
#+description: Build the throwaway clickable prototype of the booking screen: the two-level structure panel, strategy and package modes, the component picker over every product type, the four archetype widgets with a generic fallback, the version and authorisation header, the read-only initial NPV with its provenance, and forming a structure from existing trades. Mock fixtures only, from the ORE example trades.
#+type: task
#+level: s1
#+filetags: :trade-entry-prototype:sprint_25:v0:
#+owner: marco
#+branch: feature/prototype-booking-screen
#+pr: 2067
#+blocked_on:
#+blocked_since:
#+created: 2026-09-11
#+updated: 2026-09-11
#+environment: clever_dijkstra
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:D63FCA11-EE40-438F-ADF7-12C2ED5541D3][Trade entry prototype: structure-based booking screen]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Ticket type: prototype (human in the loop).

Build the throwaway clickable prototype of the booking screen. The screen is
not interactive in the pricing sense. It concentrates on the correctness and
validity of the booking.

The prototype covers:

- the structure panel, two levels deep, with named groups allowed;
- strategy mode and package mode;
- the component picker over every product type, grouped by asset class;
- four specialised widgets, plus the generic form that catches the rest;
- the version and authorisation header;
- the initial NPV, read-only, with its provenance beside it;
- a structure composed from nothing, and one formed from existing trades.

Fixtures only. No backend and no valuation.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:D63FCA11-EE40-438F-ADF7-12C2ED5541D3][Trade entry prototype: structure-based booking screen]] |
| Now          | Built and verified 2026-09-12. Both screens run, the site build deploys them, and the second feedback round is in. |
| Waiting on   | PR review. The user reacts to the prototype. |
| Next         | Review the revised prototype on the published site. |
| Last touched | 2026-09-12                               |

* Acceptance

- The prototype opens on a structure and shows its components in the panel.
- A group holds components, and components move between groups.
- The picker lists every product type from the catalogue.
- Picking a product type opens its widget, or the generic form when it has
  none.
- The version header shows the structure version and the authorisation state.
- The initial NPV is read-only, with its market data cut shown beside it.
- A component can be added, edited and removed, and the version header
  responds.
- A structure can be composed from nothing, and formed from existing trades.
- The four specialised widgets show barrier, schedule, leg and notional
  management.
- Every field the booking captures is either filled by hand or carried from
  the pricing handoff.
- The prototype is reachable from the published site.

* Plan

Build both screens as a static web app under =doc/prototypes/trade-entry/=:
plain HTML, CSS and JavaScript, with no framework and no build step. The
site build already publishes =.js=, =.css= and =doc/...org=, and already
deploys a static app directory through =ores-deploy-web-app=, so the only
build change is one call to that existing function. Nothing new is
invented for delivery.

Six files: two pages, one stylesheet, and three scripts split by concern
— =catalogue.js= holds the product types, the strategies and the
fixtures; =booking.js= and =pricing.js= hold one screen each.

The booking screen is a three-part layout:

- a header carrying the structure identity, the mode, the version and the
  authorisation state;
- a structure panel on the left, a tree two levels deep, with the
  component widget on the right;
- an initial NPV strip below, read-only, with its provenance beside it.

The version rule is the load-bearing behaviour, so it is modelled
literally: every mutating action goes through one =mutate()= function
that appends to an audit log and increments the version, and the
authorise action deliberately does not. That makes the rule visible
rather than asserted.

State lives in memory and mirrors to =localStorage=, so the Book handoff
survives the page change from the pricing screen to the booking screen.

Fixtures are transcribed from the ORE example trades, not invented:
=FX_Option_European.xml=, =FX_Barrier_Option.xml=,
=Hybrid_CompositeTrade.xml= and a leg from =Cash_BondRepo_and_Bond.xml=.

Deliberately absent: a backend, a real valuation, live market data, and
any persistence beyond the browser.

* Notes

Ticket type: prototype (human in the loop). The user reacts to the artifact.

This task was blocked on three decision tickets. It is unblocked now, and
builds on three recorded assumptions. A record repairs each of them, so
none is worth a round trip.

1. *Product types — resolved, not assumed.*
   [[id:741B9065-5ADB-4391-BB21-B6C72D979771][Research: catalogue the product types and their existing Qt form coverage]]
   is closed. The picker offers the eight =product_type= values, grouped
   by asset class, and inside =fx= the six =trade_type_code= forms that
   already have a dedicated Qt form. The generic fallback stays reachable.

2. *Strategy set — assumed; the decision ticket is still open.*
   [[id:82C8FB53-F0E2-4C5C-8415-5D3E7237C91D][Decide which strategies the strategy mode encodes and what their invariants are]]
   is a product preference, so this task does not answer it. The prototype
   carries four strategies — straddle, risk reversal, butterfly and
   strangle — because =Hybrid_CompositeTrade.xml= is already a straddle
   (long call plus short put, one strike, one expiry) and the knowledge
   page names the other three as strategy-rung examples. The screen reads
   the set from one table, so changing it is a one-line edit. The ticket
   stays open, and its answer replaces the provisional set.

3. *Version and audit display — assumed from the settled rules.*
   [[id:3EF0E6D8-72FE-4239-AB12-8670E234B5AC][Decide how audit and object versioning surface in the booking screen]]
   is open, but
   [[id:FC31FC9C-7A6E-4EFD-8A62-6DE4137F7160][Trade Structures and Deal Composition]]
   already fixes the behaviour: a change to any component increments the
   structure version, and authorisation does not. The prototype renders
   that rule and links to one history view. It invents no second rule.

The prototype lands under =doc/prototypes/=, so it needs the site build to
deploy it. That wiring is part of this diff: one call to the existing
=ores-deploy-web-app= inside =ores-deploy-web-apps=. The ticket
[[id:07E0C977-798A-4801-A623-E294169D6960][Make the site build copy and deploy doc/prototypes]]
therefore stays open. Its own acceptance is a published prototype that
loads with its assets intact, and only the live site can show that.

Fixtures come from =external/ore/examples/Products/Example_Trades=.

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/2067][#2067]] | [prototype] Add the structure-based trade entry prototype |

* Review

Two rounds. Rows 1 to 11 are the local review pass, before the PR. Rows 12
to 16 are the user's review of the published prototype. Each row is a defect
found and the decision taken.

| # | Finding | File | Decision | Notes |
|---+---------+------+----------+-------|
| 1 | The swap widget was unreachable: no type code mapped to it and the swap family named no widget, so the two-leg schedule never opened and =scheduleFields()= was dead code. | catalogue.js | Accepted | The swap family now names the widget, and the self-check covers the route. |
| 2 | The opening fixture failed its own strategy validation. =validate()= counted every component as a strategy leg, so the hedge group made the screen open on "Risk Reversal needs 2 legs". | booking.js | Accepted | A group now carries a =hedge= flag and the invariant reads the strategy legs only, which is the rule the knowledge page sets out. |
| 3 | The pricing tweak sliders were rebuilt on every tick and on every input event, so a drag ended on the first pixel. | pricing.js | Accepted | A tweak now updates its own readout and the valuation, and rebuilds nothing. This is the screen's main interaction. |
| 4 | The barrier widget carried a =+= button that incremented the version and logged "barrier level added" without adding a level. | booking.js | Accepted | Removed. Two levels are what a double barrier needs. |
| 5 | =relabel()= named a swap "Call", because it read the option type of every component. | booking.js | Accepted | The identifying field now follows the widget. |
| 6 | The premium subhead said the premium is a trade in its own right while the model stored it as a field on the option. | booking.js | Accepted | The subhead stands, and a legend records why the prototype stores it as a field and how the target model differs. |
| 7 | =formFromTrades()= wrote a stray =payOffExpiry= key beside the correct =payOffAtExpiry=. | booking.js | Accepted | Removed. |
| 8 | =bindWidget()= carried a branch that assigned a value to itself. | booking.js | Accepted | Removed. |
| 9 | The link to the pricing screen nested a =button= inside an =a=, which is invalid markup. | index.html | Accepted | The anchor carries the button styling instead. |
| 10 | The acceptance bullet "components move between groups" had no implementation. A component could be added and removed, but never reassigned, so the two-level topology was fixed at the point of creation. | booking.js | Accepted | The component editor opens with a Group select. A move reassigns the component and takes the version with it. |
| 11 | The Book handoff ran one way only. The pricing screen could price a structure that already existed, but the trader could not start a deal there. The flow the brief describes — price the deal, then book it with the economics filled in — was therefore not demonstrable. | pricing.js, pricing.html | Accepted | The pricing screen gains *New trade*: the least reference data it can price from, carried across by Book. The booking screen fills in the rest. |
| 12 | The component form mixed reference data with economics. Counterparty, book, portfolio, status and trade date sat in the same flat list as strike, notional and expiry, so nothing separated the trade header from the instrument, and each field moved when the widget changed. | booking.js | Accepted | The component panel gains an envelope block above the form, grouped under the field-group names of =ores.trading.trade=: identity, classification, parties, lifecycle and audit. The instrument form carries the instrument only, and says so. |
| 13 | The structure had no reference data of its own on screen. Counterparty, portfolio, netting set and book were reachable only through a component, so the reader could not tell what the structure was booked under. | booking.js, index.html | Accepted | The structure header gains a full-width envelope strip. The same reference data now sits in the same position at both levels: structure envelope in the header, component envelope at the head of the panel. |
| 14 | The component form ran the full height of the panel. Strike, notional and expiry sat above the schedule, the barriers and the premium, so reaching either end meant scrolling past the other. | booking.js, prototype.css | Accepted | The instrument splits into tabs, named for what the widget holds: Terms, Schedule, Premium, Barriers and Legs. Each tab is one screen long, and the trade header stays above them. |
| 15 | The premium was explained by a banner that said it is a trade in its own right, while the model stored it as a field on the option. Row 6 kept the banner and added a legend for the gap. Explaining a gap is not closing it, and the user asked for the sub-leg. | booking.js, catalogue.js | Accepted | Reverses row 6. The premium renders as a sub-leg with its own identifier, currency, amount and payment date, and its own remove and add actions. The structure references it by key. The banner is gone. |
| 16 | The audit log ran the full width below the NPV strip, and grew with every action, taking workspace away as the booking progressed. | index.html, prototype.css | Accepted | The audit folds into a drawer, closed by default, with the version rule as its summary line. A native =details= element, so it needs no script. |

* Result

Both screens are built and the site build deploys them.

The prototype is six files under =doc/prototypes/trade-entry/=:
=index.html= and =booking.js= for the booking screen, =pricing.html= and
=pricing.js= for the pricing screen, =catalogue.js= for the product types,
the strategies and the fixtures, and =prototype.css= for both screens.
There is no framework and no build step.

Every acceptance bullet is met. The picker offers the eight product types
grouped by asset class, and inside FX the six =trade_type_code= values
that have a dedicated form. Four widgets open: the FX vanilla option, the
FX barrier option, the swap, and the composite. Every other choice opens
the generic form.

The version rule is modelled rather than asserted. Every mutating action
routes through one =mutate()= function that increments the version and
writes the audit line. The authorise action deliberately does not. The
version chip animates on a change, so the rule is visible.

The topology is editable after creation. The component editor carries a
Group select, so a component moves between groups. A move counts as a
change, and the version follows it. Moving a component into a hedge group
also takes it out of the strategy invariant, which is the point of the
group being marked as a hedge.

** Reference data and economics are separated

The screen reads as two levels, and each level keeps its reference data in
a fixed place.

| Level     | Reference data                       | Economics                        |
|-----------+--------------------------------------+----------------------------------|
| Structure | The envelope strip in the header: structure ID, counterparty, portfolio, netting set, book, trade date. | The components, below it. |
| Component | The envelope block at the head of the panel, in five groups. | The instrument tabs, below it. |

The five groups are not invented. They are the field groups of
=ores.trading.trade=, which is the FpML trade header: identity,
classification, parties, lifecycle and audit. The trade is the header, and
=instrument_id= is the soft key to the instrument that carries the economics,
so the screen renders that same split rather than a new one.

The position does not move. The structure envelope is always in the header.
The component envelope is always at the head of the component panel, above
the tabs, and it stays there as the reader changes tab.

** The instrument form is tabbed

A widget splits its form into named sections, and each section is one screen
long. The tabs follow the widget: the vanilla option shows Terms, Schedule
and Premium; the barrier option adds Barriers; the swap shows Terms and
Schedule; the composite shows Terms and Legs; the generic form shows Terms
alone.

The premium is the sub-leg the knowledge page describes. It carries its own
identifier, currency, amount and payment date, it can be removed and added,
and the option references it by key. It is not a field on the option, and no
text block claims otherwise.

The audit and the version rule fold into a drawer at the foot of the screen,
closed by default. The summary line carries the rule: a component change
bumps the version, and authorisation does not.

** Delivery

One call to =ores-deploy-web-app= in =ores-build-site.el= copies the
directory and injects the site chrome, in the same way the graph and agile
apps deploy. No new publish machinery was added. The page
[[id:7B737119-099C-451D-9319-1D6BEE73A063][Trade Entry Prototype]]
describes the prototype and links to it.

** Verification

- Twenty self-check assertions pass. Open either page with =?selfcheck=1=
  to run them; the console reports =all passed=. Three are new: every
  widget splits into named sections, no economics section carries a
  trade-header field, and a premium is a sub-leg with its own identifier.
- The split check was shown to fail. Adding a counterparty field back to
  the generic instrument form reported =1 failed=, so the check catches
  the defect it names rather than passing on any input. A check that
  cannot fail is worth nothing.
- The rendered booking screen was read back from the DOM: the structure
  strip carries all six envelope fields, the component header carries all
  five field groups, the tab strip carries Terms, Schedule and Premium,
  and the audit drawer is closed on load.
- The sub-leg was driven in headless Chrome. Removing the premium took the
  version from =v7= to =v8=, adding one took it to =v9= with a fresh
  identifier, and editing a field took it to =v10=, each with the audit
  line it should write.
- The layout was read from a screenshot at 1500 by 1000. Both envelopes
  and the whole of one tab are visible without scrolling.
- Both pages render in headless Chrome with no script error, and the
  booking screen opens on a valid structure.
- The move action was driven in headless Chrome: a component row was
  clicked, its Group select was read, the value was changed to the hedge
  group, and the change event was dispatched. The component moved and the
  version chip went from =v7= to =v8=.
- The misspell check reports nothing on the changed paths. Run against the
  whole tree, it exits 1 on hits inside =projects/ores.codegen/venv/=. That
  directory is gitignored (=.gitignore= line 68) and is not in this diff,
  so the hits are pre-existing noise from a vendored tree.
- The handoff was driven end to end in headless Chrome. *New trade* was
  opened, a GBPUSD put was entered with a strike and an expiry, and *Price
  it* was pressed. The pricing screen showed the ticket and its NPV. *Book*
  was then pressed, and the browser landed on the booking screen with the
  ticket in the tree, the widget open on it, and the NPV strip carrying the
  amount with its as-of time, cut, model, run and source.
- The deploy copies all six files and patches =index.html= with the site
  chrome. The prototype source stays unpatched.

** Defects found and fixed

Sixteen. Eleven came from the local review and the verification above, and
five from the user's review of the published prototype. Each one is in the
=* Review= table with the file it touches and what was done.

The strategy set is still provisional. It belongs to
[[id:82C8FB53-F0E2-4C5C-8415-5D3E7237C91D][Decide which strategies the strategy mode encodes and what their invariants are]],
which stays open, so this task does not settle it.

The premium is no longer one of these. Row 15 moved it out of the option
and into a sub-leg of its own, which is what the knowledge page asks for.
The option still keys to it rather than owning it, which is the rule the
page sets out.

** Scope note

The pricing screen ships in this diff, although it has a task of its own.
[[id:DD436803-DCB9-4774-8A77-133AA0614219][Build the pricing screen prototype and the Book handoff]]
is blocked on a decision, and this task cannot show the initial NPV
without it. The two screens were therefore built together, and the pricing
screen is the smaller half.

That ticket stays BLOCKED and unclaimed. Its acceptance has not been
walked against it here, so this diff does not close it.
