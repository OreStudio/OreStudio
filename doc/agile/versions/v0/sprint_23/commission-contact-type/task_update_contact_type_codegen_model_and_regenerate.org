:PROPERTIES:
:ID: BD717BF3-DFD6-41C6-9A88-90B0470A37A7
:END:
#+title: Task: Update contact_type codegen model and regenerate all layers
#+description: Bring ores.refdata.contact_type.org up to the latest meta-model (checklist review, variability knobs), regenerate every facet via compass codegen entity generate, and review every diff before accepting.
#+type: task
#+level: s1
#+filetags: :commission-contact-type:sprint_23:v0:
#+owner: marco
#+branch: feature/verify-contact-type-qt-ui-post-nats
#+pr: 1542
#+blocked_on:
#+blocked_since:
#+created: 2026-07-13
#+updated: 2026-07-13
#+environment: prime_origin
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:E1F9C83E-645D-4CFC-B1C9-656651C55BCD][Commission: contact_type]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Bring =ores.refdata.contact_type.org= up to the latest codegen
meta-model — checked against the [[id:897A7156-504D-4581-AD4C-026D643C0838][Domain entity evaluation checklist]]
and a currently-correct sibling aux-type model (e.g.
=business_day_convention_type= or =day_count_fraction_type=) — then
regenerate every facet it drives and review each diff before
accepting it. This replaces drifted, hand-adjusted committed C++
(confirmed via =--address ores.cpp.qt --diff= showing the entire Qt
layer as unrecognized against current templates) with a faithful,
current regeneration. No generated file gets hand-patched as part of
this task; any real deviation from generated behaviour is expressed
as a model property or paste-block instead.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:E1F9C83E-645D-4CFC-B1C9-656651C55BCD][Commission: contact_type]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-13                               |

* Acceptance

- [X] =ores.refdata.contact_type.org= reviewed section-by-section against the Domain entity evaluation checklist.
- [X] Every variability knob the model's facets read (=has_uuid_primary_key=, =has_change_reason_cache=, =has_history=, =has_pagination=, =delete_request_id_is_plural=, =icon=, and any others the current meta-model defines) is explicitly set and deliberately chosen, not left implicit.
- [X] All facets (SQL, domain, repository, service, protocol, NATS handler/registrar/eventing, generator, Qt) regenerated via =compass codegen entity generate=.
- [X] Diff reviewed for every changed/new file before accepting; any genuinely custom behaviour ported forward via model properties, not silently dropped.
- [X] Regenerated output builds clean (affected targets).
- [X] No hand-patch to any codegen-owned file.

* Plan

1. Reviewed =ores.refdata.contact_type.org= against
   =business_day_convention_type.org= / =day_count_fraction_type.org=
   (both confirmed correct this session) and fixed: stale
   =ores.refdata/...= include paths (should be =ores.refdata.api/...=);
   missing =has_change_reason_cache=, =delete_request_id_field=/=_is_plural=,
   =history_request_id_field=/=history_response_data_field=; =name=
   column misplaced under =* Natural keys= with no matching DB unique
   constraint (moved to =* Columns=); stale unused
   =#include "sqlgen/Timestamp.hpp"= in Entity includes; missing
   =*** Detail fields= subsection (added explicit widget/placeholder
   config matching sibling convention).
2. Ran =compass codegen entity generate --diff= first to confirm scope:
   SQL/domain/repository/service diffs were small and additive
   (template header updates, a new =entity_type_of= dispatch helper,
   default-value fixes); the *protocol* diff was substantial and
   breaking (NATS subjects hyphen→underscore,
   =get_contact_types_response.contact_types= → =.types= + =success=/=message=
   fields, =delete_contact_type_request.type= (singular) →
   =.codes= (plural vector), =get_contact_type_history_request.type= →
   =.code=); the entire Qt layer was net-new relative to committed
   output.
3. Generated for real via =compass codegen entity generate=.
4. Discovered contact_type's Qt files lived in =ores.qt.party= while
   its domain lives in =ores.refdata= (component-ownership drift,
   same class of issue as the "Move book to refdata" precedent) —
   deleted the party-side files, added
   =ContactTypeController=/model construction, menu wiring (shared
   =organisation_codes_menu=), and logout cleanup to =RefdataPlugin=,
   removed the equivalent from =PartyPlugin=.
5. Reconfigured CMake (glob-based file lists don't pick up
   deletions/moves automatically) and rebuilt
   =ores.refdata.core/api/service=, =ores.qt.party/refdata=,
   =ores.qt.exe=, and the refdata test suites — all clean, no
   warnings.
6. =generators_tests.cpp='s =contact_type_generator_produces_valid_instance=
   asserted the *previous* generator template's convention
   (=version==1=, =change_reason_code=="system.new"=); the current
   template (verified against =day_count_fraction_type=) produces
   =version==0=/="system.test"=. Fixed the two assertions to match —
   scoped to =contact_type= only; the other nine tests in that file
   assert the same stale convention for entities not yet regenerated
   in this task, intentionally left alone.
7. =validate_schemas.sh= and the full =ores.refdata.api.tests= suite
   (353 assertions, 61 cases) pass.

* Notes

Full protocol/Qt regeneration was a genuinely breaking change to the
wire format and the Qt consumer — safe here because contact_type had
no other consumer (no CLI/shell/HTTP/Wt wiring existed) and the only
Qt consumer was regenerated in the same pass.

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1542][#1542]] | [refdata,qt] Commission contact_type: regenerate from model, verify Qt UI post-NATS |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Fixed six drift points in =ores.refdata.contact_type.org= (stale
include paths, missing variability knobs, misplaced natural key,
stale unused include, missing Detail fields) and regenerated every
facet the model drives. The protocol layer changed in a genuinely
breaking way (NATS subject naming, field renames, singular→plural
delete) — safe because the only consumer, the Qt layer, was
regenerated in the same pass and had no other consumer (no CLI/shell/
HTTP/Wt). Discovered and fixed a second drift class along the way:
contact_type's Qt files lived in =ores.qt.party= even though its
domain lives in =ores.refdata= — moved ownership (construction, menu,
logout) from =PartyPlugin= to =RefdataPlugin=, deleting the stale
party-side files. Fixed one stale test assertion (=contact_type=
generator test expected the previous template's =version=/
=change_reason_code= convention). Build clean across
=ores.refdata.core/api/service=, =ores.qt.party/refdata=, =ores.qt.exe=;
=validate_schemas.sh= and =ores.refdata.api.tests= (353 assertions)
pass. Manual Qt verification is the next task's job.
