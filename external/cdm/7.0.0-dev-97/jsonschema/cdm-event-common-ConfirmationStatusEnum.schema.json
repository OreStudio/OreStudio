{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "$anchor": "cdm.event.common",
  "type": "string",
  "title": "ConfirmationStatusEnum",
  "description": "Enumeration for the different types of confirmation status.",
  "enum": [
    "Confirmed",
    "Unconfirmed"
  ],
  "oneOf": [
    {
      "enum": [
        "Confirmed"
      ],
      "title": "Confirmed"
    },
    
    {
      "enum": [
        "Unconfirmed"
      ],
      "title": "Unconfirmed"
    }
  ]
}
