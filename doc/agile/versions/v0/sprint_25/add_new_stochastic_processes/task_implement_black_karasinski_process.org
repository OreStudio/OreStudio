:PROPERTIES:
:ID: CDB12DB0-B0FF-4507-B94F-17540F6792B3
:END:
#+title: Task: Implement Black-Karasinski log-normal short-rate process
#+description: Implement a log-normal short-rate engine via Euler discretization of d(ln r) = kappa(theta - ln r)dt + sigma dW, then r = exp(ln r). This ensures positivity without a tree — a simple SDE approach, data-oriented with plain struct parameters and free functions. Reference: QuantLib BlackKarasinski class.
#+type: task
#+level: s1
#+filetags: :add_new_stochastic_processes:sprint_25:v0:
#+owner: marco
#+branch: feature/implement-black-karasinski-process
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-08-07
#+updated: 2026-08-11
#+environment: eager_maxwell
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:C603D702-DE7E-4DA4-8409-6FCB0F64808F][Add new stochastic processes]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The Black-Karasinski process appears as a selectable process type in the IR curve generation config UI, with its own parameter fields (kappa, theta, sigma) stored in a strongly-typed child table. The engine itself is complete (merged via PR #1932); the system wiring is the remaining scope, following the G2 precedent.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | STARTED                              |
| Parent story | [[id:C603D702-DE7E-4DA4-8409-6FCB0F64808F][Add new stochastic processes]] |
| Now          | Engine implemented and merged (PR #1932); system wiring remaining. |
| Waiting on   | Nothing.                               |
| Next         | Wire into the system (factory, seed data, config storage, Qt UI), following the G2 precedent. |
| Last touched | 2026-08-11                               |

* Acceptance

- Seed data row makes the Black-Karasinski process appear in the process type combo box
- process_factory constructs a working Black-Karasinski engine (factory method and parameter struct)
- Black-Karasinski parameters persist to and load from the child table
- IrCurveGenerationConfigDetailDialog shows Black-Karasinski parameter fields when selected
- Existing Vasicek/CIR/Hull-White/G2 configs are unaffected

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

Moved from the IR curve follow-ups story on 2026-08-11: the engine work is complete (merged via PR #1932 on 2026-08-10), but the engine is not wired into the system. The remaining scope — system wiring (factory, seed data, config storage, Qt UI) — is tracked here, under the Add new stochastic processes story, following the G2 wiring precedent.

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1932][#1932]] | [ores.analytics.quant] Implement six stochastic IR processes |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Implemented =black_karasinski_process=: a one-factor log-normal short-rate
engine. =ln r= follows an Ornstein-Uhlenbeck process with the exact one-step
Gaussian transition, so the rate stays positive for all time. There is no
closed-form bond price under a log-normal short rate, so =discount_factor()=
builds per call a centered adaptive trinomial lattice of =ln r= and
propagates state prices through the generic =rate_tree= utility;
=sigma == 0= short-circuits to the deterministic closed form along the
mean-reversion path. 24 Catch2 tests covering Gaussian-OU moment checks
(statistical), tree-vs-Monte-Carlo agreement, dt convergence, zero-vol
closed form, deep-tree validity, seed determinism and validation.
Registered in =component_files.cmake=.

All 368 =ores.analytics.quant= tests pass on Linux. Not yet wired into
=process_factory= (deferred to PR 2: system infra wiring).

Merged via PR #1932 (merge commit 4c9b23d99) on 2026-08-10.
