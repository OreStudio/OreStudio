:PROPERTIES:
:ID: CA299D08-AC97-4A37-B3F2-A5927F379326
:END:
#+title: Spot sweep and ledger sweep
#+description: The two distinct 'sweep' mechanisms: the Spot Sweep that crystallises non-reporting-currency cash into reporting currency for Sweepable books, and the daily automated Ledger Sweep that revalues transactional currency into functional currency.
#+type: knowledge
#+level: cross
#+filetags: :domain:trading:book:finance:
#+created: 2026-07-11
#+updated: 2026-07-11

* Summary

"Sweep" names two operationally distinct mechanisms that share a
cash-centralisation goal but sit at different layers. *Spot sweep* is
a front/middle-office process that periodically crystallises profit
held in non-reporting currencies by aggregating and converting
Sweepable books' cash balances into a designated *Central Sweep Book*.
*Ledger sweep* (FX Revaluation) is a daily, automated, accounting-layer
process that revalues every ledger book's transactional-currency
balances into its functional currency — it is not opt-in, and is
unrelated to the =Is Sweepable= flag.

* Detail

** Spot sweep

*** What it is

Spot sweep crystallises profit held in any currency other than a
centre's reporting currency (see [[id:130A3261-E788-4222-9BD7-371AD92982FB][Book]] and the currency knowledge
cluster for the reporting-currency concept). As trading activity
accrues cash balances in non-reporting currencies across many books,
the firm periodically locks in an FX rate and converts those balances
back into "P&L currency."

The process is *centralised*: long and short cash positions across
many books are aggregated into a single view first, and only the net
amount is actually converted — this avoids unnecessary trades when
positions across books net each other out.

*** Mechanics

#+BEGIN_EXAMPLE
 Step 1: AGGREGATE
   Book A (+120k EUR) ─┐
   Book B (-45k EUR)  ─┼──► Central Sweep Book: net position per currency
   Book C (+30k EUR)  ─┘

 Step 2: CONVERT AT A RATE THAT CLEARS THE AVERAGE RATE TO 1
   A sweep trade is generated whose rate is chosen so the pair's
   average booked rate (tracked across all deals in that pair) is
   driven back to 1. Drift between the average rate and the current
   market rate represents crystallisable profit or loss; the sweep
   trade realises it.

 Step 3: SEGREGATE THE CRYSTALLISED PROFIT
   Once converted into reporting currency, the resulting P&L is moved
   somewhere traders cannot access or trade against.
#+END_EXAMPLE

*** Book eligibility

Only books flagged =Is Sweepable= participate (see [[id:74AA46EB-64ED-4FD7-B212-AEC164648B84][Book
classification]]). Forward positions are never swept — only spot/cash
balances. Finer-grained configuration also excludes positions older
than a threshold age (e.g. "Past Value" beyond 10 days) and excludes
specific currencies entirely.

The book balances flow *into* is designated a *Central Sweep Book* —
a distinct book purpose in its own right, not merely a flag on an
otherwise-ordinary book (see "Sweep target and remittance target are
not combinable" below).

*** Governance

Sweep runs require sign-off, alongside [[id:B0902FF2-138F-4234-A473-1371549CE219][funding runs]] and brokerage
payments. All deals generated by a single sweep run must be linked
together for traceability, exactly as for the funding process.

** Ledger sweep (FX Revaluation)

*** What it is

Distinct from spot sweep, the *ledger sweep* is a daily, automated,
*ledger-side* process. The ledger receives all positions — live and
settled cash — in *transactional currency*. Once a day, an automated
process spot-converts these into each book's *functional currency*,
capturing the resulting P&L into a dedicated *FX Revaluation* entry.
This whole daily conversion is what "ledger sweep" refers to.

#+BEGIN_EXAMPLE
  Book positions (transactional ccy) ──► daily spot conversion ──►
      Functional-currency balance + FX Revaluation P&L entry
#+END_EXAMPLE

*** How it differs from spot sweep

| Aspect | Spot sweep | Ledger sweep |
|--------+-----------+---------------|
| Layer | Front/middle office, trading | Accounting/ledger |
| Cadence | Periodic, discretionary | Daily, automated |
| Scope | Only =Is Sweepable= books | Every ledger book, unconditionally |
| P&L | Realised (real executed trade) | Unrealised (valuation only) |
| Output | Executed FX sweep trade | FX Revaluation P&L entry only |

The ledger separates realised from unrealised P&L; ledger sweep's FX
Revaluation output is unrealised, reflecting a valuation conversion
rather than an executed trade. The two "sweeps" share a name and a
conceptual goal (cleaning up cross-currency exposure) but operate at
entirely different layers, and =Is Sweepable= governs spot sweep
eligibility only — it has no bearing on ledger sweep, which applies to
every book with a functional currency regardless of that flag. See
[[id:D215AB01-8BE4-4E2B-930C-17AC80491C9D][Book and the ledger]] for the wider ledger-sweep mechanics
(functional currency, the daily automated process).

** Sweep target and remittance target are not combinable

The book that Spot Sweep centralises balances into (the *Central Sweep
Book*) and the book that [[id:DFC04627-FB0F-4FFF-973E-E3EC9E05AD87][Central Remittance]] accumulates profit into
(the *Central Remittance Book*) look superficially similar — both are
singleton "central" books that other books' balances flow into — but
they are not the same role and should not be modelled as combinable
flags on one book:

- *Different owners*: Spot Sweep is a Treasury/Money-Markets desk
  process, run frequently; Central Remittance is a Finance/Group
  Treasury process, run monthly to annually.
- *Different accounting treatment*: Spot Sweep crystallises real
  trading P&L (the average-rate mechanism generates an actual FX
  gain/loss); Central Remittance is deliberately balance-sheet-only —
  it must not touch P&L a second time, since it only relocates
  already-recognised profit.
- *Different population*: Sweep-target balances come from many
  trading books' live spot/cash positions; remittance-target balances
  come from the *global books* that already received the monthly
  Profit Sell Down mirror deal — a step removed and already
  Finance-owned.

Mixing live trading P&L postings (Sweep) with P&L-neutral relocation
postings (Remittance) on the same book balance would undermine exactly
the audit/reconciliation separation these processes exist to provide
— the same reasoning that keeps Reserve and Funding book purposes
apart (see [[id:74AA46EB-64ED-4FD7-B212-AEC164648B84][Book classification]]). Sweep target and remittance target
are therefore modelled as two further mutually-exclusive values of
=book_purpose_type=, not as independent boolean flags a book could
carry simultaneously.

* See also

- [[id:130A3261-E788-4222-9BD7-371AD92982FB][Book]] — hub note.
- [[id:74AA46EB-64ED-4FD7-B212-AEC164648B84][Book classification]] — the =Is Sweepable= flag and the wider
  =book_purpose_type= enumeration, including Sweep target and
  Remittance target.
- [[id:D215AB01-8BE4-4E2B-930C-17AC80491C9D][Book and the ledger]] — functional currency and the ledger sweep
  mechanics in the context of the wider ledger connection.
- [[id:B0902FF2-138F-4234-A473-1371549CE219][Funding process]] — a related but distinct cash-centralisation
  process.
- [[id:DFC04627-FB0F-4FFF-973E-E3EC9E05AD87][Central remittance (profit remittance)]] — the downstream process
  the Central Remittance Book (not the Central Sweep Book) feeds.
