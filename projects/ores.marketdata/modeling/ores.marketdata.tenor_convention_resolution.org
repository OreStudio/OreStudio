:PROPERTIES:
:ID: E1F5A9C3-6D2B-4E8A-B7F1-3C9D5A2E6B48
:END:
#+title: ores.marketdata.tenor_convention_resolution
#+description: Codegen junction model joining tenor_convention to tenor; presence of a row is set membership (which tenors belong to a convention), with an optional per-row anchor override for SPECIAL tenors that resolve differently under this convention than the convention's own default.
#+type: ores.codegen.junction
#+component: marketdata
#+name: tenor_convention_resolutions
#+name_singular: tenor_convention_resolution
#+name_title: Tenor Convention Resolution
#+name_singular_words: tenor convention resolution
#+brief: Joins a tenor convention to the tenors valid under it, with an optional per-tenor anchor override.
#+product: ores
#+schema: public
#+has_tenant_id: true
#+filetags: :model:junction:marketdata:tenor:
#+created: 2026-07-13
#+updated: 2026-07-13

A [[id:0AC88EB3-DB7F-4135-9DA6-0ED4583FEC29][tenor]] does not resolve the
same way under every [[id:C4D8A2E6-3B7F-4A1D-9C5E-8F2A6D3B1E90][tenor
convention]] — =O/N= resolves from horizon to tomorrow under the
spot/forward convention, but means "today" under the FX swap convention —
and not every tenor belongs to every convention's set at all (the swap
convention's tenor set stops at =S/N=; the credit/CDS convention only uses
IMM-quarter labels). This junction records both facts as one row per valid
=(convention, tenor)= pair: the row's mere presence is set membership, and
its optional =anchor_override= column carries the per-row exception to the
convention's own default anchor, needed only for the =SPECIAL= tenors
whose resolution genuinely varies by convention. A =PERIOD= tenor's
resolution never needs an override — its offset from the anchor is fixed
by its own =unit=/=multiplier=, so only the anchor itself (the
convention's =measured_from=, or this row's override) changes.

* Left
:PROPERTIES:
:column:        convention_code
:column_short:  convention
:column_title:  Convention
:type:          text
:cpp_type:      std::string
:index_comment: Tenor convention this resolution row belongs to.
:END:

References [[id:C4D8A2E6-3B7F-4A1D-9C5E-8F2A6D3B1E90][tenor_convention.code]].

* Right
:PROPERTIES:
:column:        tenor_code
:column_short:  tenor
:column_title:  Tenor
:type:          text
:cpp_type:      std::string
:index_comment: Tenor valid under this convention.
:END:

References [[id:9A2E4D6B-7C1F-4B8A-A5D3-2F6E9B1C4A87][tenor.code]].

* Columns

** anchor_override
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: true
:END:

References [[id:3F8B6C2A-1D4E-4A7F-9B3C-6E2D8F1A5C90][tenor_anchor.code]].
Null for the common case (the tenor resolves from the convention's own
=measured_from=); set only when this specific tenor resolves from a
different anchor under this specific convention.

* SQL

** Flags
:PROPERTIES:
:tablename: ores_marketdata_tenor_convention_resolutions_tbl
:END:

* Repository
:PROPERTIES:
:name_singular_short: resolution
:name_short:          resolutions
:name_singular_words: tenor convention resolution
:name_words:          tenor convention resolutions
:order_column:        convention_code
:END:

* C++

** Domain includes

#+begin_src cpp :name includes
#include <string>
#+end_src

** Entity includes

#+begin_src cpp :name includes
#include <string>
#+end_src

** Conventions
:PROPERTIES:
:iterator_var: r
:END:

** Table display

| column           | header           |
|------------------+------------------|
| convention_code  | Convention       |
| tenor_code       | Tenor            |
| anchor_override  | Anchor Override  |

* See also

- [[id:C4D8A2E6-3B7F-4A1D-9C5E-8F2A6D3B1E90][Tenor Convention]] — the left side; the convention's own default anchor this row can override.
- [[id:9A2E4D6B-7C1F-4B8A-A5D3-2F6E9B1C4A87][Tenor]] (entity model) — the right side.
- [[id:3F8B6C2A-1D4E-4A7F-9B3C-6E2D8F1A5C90][Tenor Anchor]] — the lookup =anchor_override= references.
- [[id:0AC88EB3-DB7F-4135-9DA6-0ED4583FEC29][Tenor]] — the "Tenor conventions by curve type" domain concept this junction operationalises.
