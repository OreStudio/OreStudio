:PROPERTIES:
:ID: 48D2AA4A-6253-4B09-A78F-21C9B950F68C
:END:
#+title: Task: Implement LIBOR Market Model forward-rate process
#+description: Implement an LMM/BGM engine where the state is a vector of forward LIBOR rates L_i(t) for N accrual periods. Data-oriented: the forward-rate vector, tenor structure, and correlation matrix are plain structs. The drift for each L_i under the forward measure depends on the correlation structure across all forward rates — the most complex drift calculation in the set. Reference: QuantLib LiborMarketModel classes.
#+type: task
#+level: s1
#+filetags: :add_new_stochastic_processes:sprint_25:v0:
#+owner: marco
#+branch: feature/expand-stochastic-ir-process-types
#+pr: 1932
#+blocked_on:
#+blocked_since:
#+created: 2026-08-07
#+updated: 2026-08-11
#+environment: eager_maxwell
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:C603D702-DE7E-4DA4-8409-6FCB0F64808F][Add new stochastic processes]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The LIBOR Market Model process appears as a selectable process type in the IR curve generation config UI, with its own parameter fields (initial forward rates, tenor structure, correlation matrix) stored in a strongly-typed child table. The engine itself is complete (merged via PR #1932); the system wiring is the remaining scope, following the G2 precedent.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | BACKLOG                              |
| Parent story | [[id:C603D702-DE7E-4DA4-8409-6FCB0F64808F][Add new stochastic processes]] |
| Now          | Engine implemented and merged (PR #1932); system wiring remaining. |
| Waiting on   | Nothing.                               |
| Next         | Wire into the system (factory, seed data, config storage, Qt UI), following the G2 precedent. |
| Last touched | 2026-08-11                               |

* Acceptance

- Seed data row makes the LIBOR Market Model process appear in the process type combo box
- process_factory constructs a working LIBOR Market Model engine (factory method and parameter struct)
- LIBOR Market Model parameters persist to and load from the child table
- IrCurveGenerationConfigDetailDialog shows LIBOR Market Model parameter fields when selected
- Existing Vasicek/CIR/Hull-White/G2 configs are unaffected

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

Moved from the IR curve follow-ups story on 2026-08-11: the engine work is complete (merged via PR #1932 on 2026-08-10), but the engine is not wired into the system. The remaining scope — system wiring (factory, seed data, config storage, Qt UI) — is tracked here, under the Add new stochastic processes story, following the G2 wiring precedent.

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1932][#1932]] | [ores.analytics.quant] Implement six stochastic IR processes |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Division by 1 + tau_k*L_k unguarded in the drift weight and discount_factor: a high-vol Euler step could push it negative and silently price a negative (arbitrage) discount | libor_market_model_process.cpp | Accepted | Drift throws std::invalid_argument at the pole L_k = -1/tau_k; discount_factor throws std::runtime_error; pole test added |
| 2 | Cholesky-with-eigen-fallback PSD root block duplicated verbatim across affine, QG and LMM | libor_market_model_process.cpp | Accepted | Shared math::psd_matrix_square_root helper |

* Result

Implemented =libor_market_model_process=: a forward-LIBOR engine under the
spot measure with displacements. The drift is the public
=lmm_spot_measure_drift= free function, mirroring QuantLib's
=LMMDriftCalculator= semantics (numeraire 0); correlated shocks come from
the Eigen Cholesky factor of =diag(sigma) * rho * diag(sigma)=.
=discount_factor()= is the spot-measure rollover discount. 15 Catch2 tests
covering the drift formula against hand-computed vectors, the spot-measure
martingale property and its measured Euler bias (statistical), caplet
repricing against Black's formula (statistical), drift sign and correlation
effects, positivity with displacement, zero-vol frozen rates, seed
determinism and validation. Registered in =component_files.cmake=.

All 368 =ores.analytics.quant= tests pass on Linux. Not yet wired into
=process_factory= (deferred to PR 2: system infra wiring).

Merged via PR #1932 (merge commit 4c9b23d99) on 2026-08-10.
