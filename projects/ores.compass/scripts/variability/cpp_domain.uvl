namespace OresCodegenCppDomain

// Trivial UVL model of entity_meta_model_cpp_domain.org: this facet has
// no variability knobs today (its output is fully determined by the
// Keys and columns declarations), so there is nothing to branch on --
// included for consistency across the segment pages, not because this
// facet has anything interesting to model.

features
    CppDomain {abstract}
        mandatory
            DomainStruct
