:PROPERTIES:
:ID: 166CBCA9-112E-4949-8D5A-CE0FC55E4A08
:END:
#+title: The skill catalogue
#+description: What a skill is, why the catalogue is arranged as it is, how skills are named and attributed, the vocabulary the framework uses, and where a new artefact goes.
#+type: knowledge
#+level: cross
#+filetags: :llm:skills:claude_code:architecture:catalogue:
#+created: 2026-08-26
#+updated: 2026-08-26

#+begin_note
*Status: target state.* This page describes the framework we are
building, not the catalogue as it stands today. Where this page and the
current catalogue disagree, the catalogue is what runs.
#+end_note

This page concerns the catalogue itself rather than any part of the
framework it holds: what a skill is, why the collection is arranged as
it is, what things are called, and where a new artefact goes.

For the contents of the catalogue see [[id:2003934B-ED05-D114-404B-08C0E0D844BB][Skills]], which lists every skill
grouped by domain.

* Detail

** What a skill is


A skill is *task-shaped*: the playbook a session loads to perform one
unit of work. Compare with a [[id:1A5E0079-5771-4213-B624-62FA9AC947B4][function]], which is *role-shaped* — a
description of what someone does, listing the skills that role draws on.

Every skill lives at =doc/llm/skills/<slug>/SKILL.org= and deploys to
=.claude/skills/<slug>/SKILL.md=. The slug, the directory and the
=name:= frontmatter field are always identical.


** Why the catalogue is arranged this way


A catalogue of any size presents its user with a selection problem, and
a catalogue that grows presents a worsening one. Three failures compound
in a flat list of skills.

The first is combinatorial. A catalogue that writes out one skill per
pairing of verb and artefact type grows as the product of the two, so
adding an artefact type obliges someone to write a row of new skills by
hand, and every cell nobody writes is a capability the agent cannot
find. The second is the absence of dispatch: an agent holding an
identifier, a branch name or a failing test has no way to ask what may
be done with it, and must already know a skill's name to use it. The
third is that a catalogue of actions has nowhere to put judgement, so
rules an agent should cite rather than run, and approaches to a class of
work rather than to an artefact, simply do not get written.

The three classifications answer these in order. Registering verbs
against types removes the product. Computing the applicable verbs
supplies the dispatch. Sorting artefacts by the agent's relation to them
makes room for the kinds that are cited rather than run.


** Where things go


The placement procedure. Take the first match.

1. A durable fact about the domain goes to a *knowledge document*. If it
   is the entry point to a cluster, a *structure note*.
2. An account of how one operation is performed here, with a runnable
   script, goes to a *recipe*. This is also where a method's missing
   effector goes.
3. A correction that must not be repeated goes to a *memory*. Keep these
   scarce: each is paid for by every session.
4. A rule that is cited rather than run goes to a *principle*.
5. Intended behaviour that should be checkable goes to a *specification*.
6. A sequence of phases ending in a verified state, not tied to one
   artefact type, goes to a *method*.
7. A change to a *generated* artefact goes to its model or its template,
   never to the output.
8. An operation on one artefact type goes to an *action*, at the pairing
   of type and verb. The action holds the judgement; the recipe holds
   the command.
9. A contested judgement goes to *deliberation*, and its verdict is
   recorded against the work.
10. A role a session or a delegate adopts goes to a *function*, tagged
    by level, stating what it must read first and what it may do.
11. The argument for a change, spanning many tasks, goes to a *design*
    in the story's own folder rather than to the durable tree.


** Terminology



One word per concept.

#+ATTR_HTML: :class hug-leading
| Concept                          | Word      | Not                                  |
|----------------------------------+-----------+--------------------------------------|
| A unit of work an agent executes | task      | ticket, issue, unit, slice           |
| A body of related tasks          | story     | epic, spec, map                      |
| A recorded idea, not yet work    | capture   | backlog item, inbox item             |
| The approach to a kind of work   | method    | playbook, flow                       |
| The sticky posture for a session | mode      | stance skill, style, persona         |
| A rule that is cited, not run    | principle | rule, guideline                      |
| A durable fact about the domain  | knowledge | research note, doc, ADR              |
| Intended behaviour, checkable    | spec      | never used for a story plan          |
| The role a session adopts        | function  | persona, agent type, role            |
| Any LLM process doing work       | agent     | bot, assistant                       |
| The agent a human talks to       | session   | main agent, parent, the LLM          |
| An agent spawned by another      | delegate  | subagent, worker, child agent        |
| The typed thing acted upon       | target    | object, candidate, entity            |
| The operation applied to it      | action    | command, verb, skill                 |

Three traps. *Spec* means an Allium specification and nothing else; a
story's plan is a *plan*. *Persona* means a Council member and nothing
else; a session's role is a *function*. *Agent* is the general word for
any LLM process, so it never names a role — the S1 level is called
"Agent" but it is a function, and a session or a delegate adopts it.



** Naming



Every skill we own carries the =compass-= prefix, which separates ours
from every plugin skill the harness also loads.

#+begin_example
compass-helm                                  the sticky mode
compass-principle-<rule>                      stance
compass-method-<kind>                         method
compass-<domain>-<verb>[-<object>]            action
compass-act, compass-act-all, compass-export  dispatch
#+end_example

The domain and verb registers are closed and defined in [[id:7D779BB3-BBEF-40D2-B6AC-E02BCA72AB85][skill naming
conventions]]. Every skill carries a =#+level:=.



** Provenance



Imported skills are not prefixed by their origin. The name is the
developer's interface and says what the skill does, nothing else.

Provenance lives in the org source: the upstream repository, the path
within it, the commit imported from, the date, and a divergence note. A
manifest lists every import and a check reports which have fallen behind
upstream. None of it appears in the exported skill.

Because the name carries no origin, the frontmatter and the manifest are
the *only* record that a skill was imported, which is why =skill-add=
enforces the contract at authoring time.

* See also

- [[id:AEBC2BC9-A854-4CEC-BA88-1602490AC762][Skill architecture]] — the overview this page sits under.
- [[id:2003934B-ED05-D114-404B-08C0E0D844BB][Skills]] — the inventory this page governs.
- [[id:7D779BB3-BBEF-40D2-B6AC-E02BCA72AB85][Skill naming conventions]] — the closed registers.
- [[id:47341FC6-B8FB-499E-AAE0-F4EE0ABFDA05][Glossary]] — where the terminology will be governed once it lands there.
