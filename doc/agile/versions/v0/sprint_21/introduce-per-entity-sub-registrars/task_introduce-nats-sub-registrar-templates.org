:PROPERTIES:
:ID: B1A68C26-970D-41ED-BCF9-B7187C703218
:END:
#+title: Task: Introduce nats-sub-registrar codegen templates
#+description: Write the mustache templates and org literate source for the per-entity sub-registrar pair (hpp/cpp), add a nats-sub-registrar facet entry to facet_catalogue.org, and tangle to regenerate the .mustache files.
#+type: task
#+level: s1
#+filetags: :introduce-per-entity-sub-registrars:sprint_21:v0:
#+owner: marco
#+branch: feature/introduce-nats-sub-registrar-templates
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-06-27
#+updated: 2026-06-27
#+environment: jolly_knuth
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:FA0846D8-5498-4036-8897-812EB053C4B4][Introduce per-entity NATS sub-registrars]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Add a =nats-sub-registrar= codegen profile that produces a
=<entity>_registrar.hpp/cpp= pair for each refdata entity. The hpp
declares =register_<entity>_handlers()=; the cpp includes only that
entity's handler header and wires its NATS subjects to the handler
instance. The top-level =registrar.cpp= will delegate to these in the
next task.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:FA0846D8-5498-4036-8897-812EB053C4B4][Introduce per-entity NATS sub-registrars]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-06-27                               |

* Acceptance

- =nats-sub-registrar= entry exists in =facet_catalogue.org= with two
  template/output rows (hpp and cpp).
- =cpp_nats_registrar.hpp.mustache= and =cpp_nats_registrar.cpp.mustache=
  exist in =library/templates/= and match the org literate sources.
- =cpp_nats_registrar_hpp.org= and =cpp_nats_registrar_cpp.org= exist
  with tangle blocks referencing the mustache files.
- =cpp_messaging.org= archetypes table updated to include the two new
  archetypes.
- Template flags =no_history= (skip history subject) and =list_only=
  (skip save/remove/history) documented in the org sources.

* Plan

1. Add =nats-sub-registrar= to =library/facet_catalogue.org=: two rows
   (hpp under include/, cpp under src/messaging/).
2. Write =cpp_nats_registrar.hpp.mustache= — include guard + free function
   declaration; both =domain_entity= and =entity= sections.
3. Write =cpp_nats_registrar.cpp.mustache= — includes + implementation;
   =list_only= flag controls save/remove/history; =no_history= flag skips
   history subscription; both model-type sections.
4. Write =cpp_nats_registrar_hpp.org= and =cpp_nats_registrar_cpp.org=
   as org literate sources with tangle blocks.
5. Update =cpp_messaging.org= summary and archetypes table.

* Notes

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Added the =nats-sub-registrar= codegen facet producing a per-entity
=<entity>_registrar.hpp/cpp= pair. The hpp declares
=register_<entity>_handlers()=; the cpp includes only that entity's
handler header and wires its NATS subjects to the handler instance, so a
compile failure in one entity's handler cannot block the rest of the
service.

- =facet_catalogue.org=: new =nats-sub-registrar= entry with hpp/cpp rows.
- =cpp_nats_registrar.{hpp,cpp}.mustache= and their org literate sources
  (=cpp_nats_registrar_{hpp,cpp}.org=) with tangle blocks.
- =cpp_messaging.org=: archetypes table gains both rows; summary updated
  to "Eight templates".
- =list_only= flag wires only the list subject; =no_history= skips the
  history subscription — both documented in the org sources.

Committed in =c90d0b989=. Mustache outputs match their tangle blocks
byte-for-byte (re-tangle is a no-op). Next: generate sub-registrars for
all refdata entities.
