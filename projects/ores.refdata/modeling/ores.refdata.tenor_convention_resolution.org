:PROPERTIES:
:ID: E1F5A9C3-6D2B-4E8A-B7F1-3C9D5A2E6B48
:END:
#+title: ores.refdata.tenor_convention_resolution
#+description: Codegen junction model joining tenor_convention to tenor; presence of a row is set membership (which tenors belong to a convention), with an optional per-row anchor override and, for SPECIAL tenors, the convention-specific offset that gives the tenor a duration.
#+type: ores.codegen.junction
#+component: refdata
#+name: tenor_convention_resolutions
#+name_singular: tenor_convention_resolution
#+name_title: Tenor Convention Resolution
#+name_singular_words: tenor convention resolution
#+brief: Joins a tenor convention to the tenors valid under it, with an optional per-tenor anchor override.
#+product: ores
#+schema: public
#+filetags: :model:junction:refdata:tenor:
#+created: 2026-07-13
#+updated: 2026-08-10

* Flags
:PROPERTIES:
:schema:    public
:product:   ores
:component: refdata
:profile:   tenant-scoped-junction
:END:

A [[id:0AC88EB3-DB7F-4135-9DA6-0ED4583FEC29][tenor]] does not resolve the
same way under every [[id:C4D8A2E6-3B7F-4A1D-9C5E-8F2A6D3B1E90][tenor
convention]] — =O/N= resolves from horizon to tomorrow under the
spot/forward convention, but means "today" (zero offset) under the FX
swap convention — and not every tenor belongs to every convention's set
at all (the swap convention's tenor set stops at =S/N=; the credit/CDS
convention only uses IMM-quarter labels). This junction records all of
that as one row per valid =(convention, tenor)= pair: the row's mere
presence is set membership; its optional =anchor_override= column carries
the per-row exception to the convention's own default anchor; and its
optional =offset_unit=/=offset_multiplier= columns carry the
convention-specific duration for a =SPECIAL= tenor, which — unlike a
=PERIOD= tenor — has no =unit=/=multiplier= of its own to fall back on
(see [[id:9A2E4D6B-7C1F-4B8A-A5D3-2F6E9B1C4A87][Tenor]]'s =kind= column).
A =PERIOD= tenor's resolution never needs an offset override — its
duration is fixed by its own =unit=/=multiplier= regardless of
convention, so only the anchor itself (the convention's =measured_from=,
or this row's =anchor_override=) can vary for those rows. For a
convention whose =resolution_algorithm= is =SCHEDULE_STEP= rather than
=ANCHOR_OFFSET=, the row's =schedule_code= names the
[[id:CD180696-6558-469E-8FE5-66BFBB6E3E00][tenor_schedule]] axis the
tenor walks (=ROLL_QUARTER= for the IMM rule, =FOMC_MEETING= for the
event-lookup schedule) and =schedule_step_count= is the number of steps
to the resolution date (e.g. =1Y 1RQ= for the migrated =CREDIT_CDS_IMM=
rows: one calendar year of offset from spot, then one roll-quarter) —
a tenor resolves as anchor + calendar offset + n steps along the named
schedule. The =offset_unit=/=offset_multiplier= columns stay the
calendar-axis offset: their =ROLL_QUARTER= value is gone — the roll
count lives in =schedule_step_count= now.

* Left
:PROPERTIES:
:column:        convention_code
:column_short:  convention
:column_title:  Convention
:type:          text
:cpp_type:      std::string
:index_comment: Tenor convention this resolution row belongs to.
:END:

References [[id:C4D8A2E6-3B7F-4A1D-9C5E-8F2A6D3B1E90][tenor_convention.code]].

#+begin_src cpp :name generator
std::string("RATES_SPOT_FORWARD")
#+end_src

* Right
:PROPERTIES:
:column:        tenor_code
:column_short:  tenor
:column_title:  Tenor
:type:          text
:cpp_type:      std::string
:index_comment: Tenor valid under this convention.
:END:

References [[id:9A2E4D6B-7C1F-4B8A-A5D3-2F6E9B1C4A87][tenor.code]].

#+begin_src cpp :name generator
std::string("1M")
#+end_src

* Columns

** anchor_override
:PROPERTIES:
:type:     text
:cpp_type: std::optional<std::string>
:nullable: true
:END:

References [[id:3F8B6C2A-1D4E-4A7F-9B3C-6E2D8F1A5C90][tenor_anchor.code]].
Null for the common case (the tenor resolves from the convention's own
=measured_from=); set only when this specific tenor resolves from a
different anchor under this specific convention.

#+begin_src cpp :name generator
std::nullopt
#+end_src

** offset_unit
:PROPERTIES:
:type:     text
:cpp_type: std::optional<std::string>
:nullable: true
:END:

One of =DAY=, =WEEK=, =MONTH=, =YEAR=, or =ROLL_QUARTER=. Null when the
referenced tenor is =PERIOD= (its own =unit= applies instead); required
when the referenced tenor is =SPECIAL=, since a =SPECIAL= tenor has no
duration outside a convention's say-so.

#+begin_src cpp :name generator
std::nullopt
#+end_src

** offset_multiplier
:PROPERTIES:
:type:          integer
:cpp_type:      std::optional<int>
:nullable: true
:END:

The count paired with =offset_unit= (e.g. =1= day for =O/N= under
spot/forward, =0= days for =O/N= under FX swap, =1= year for a CDS =1Y
1RQ= tenor). Same nullability rule as =offset_unit=.

#+begin_src cpp :name generator
std::nullopt
#+end_src

** schedule_code
:PROPERTIES:
:type:     text
:cpp_type: std::optional<std::string>
:nullable: true
:END:

The schedule axis the tenor walks, for
=resolution_algorithm = 'SCHEDULE_STEP'= rows.

References [[id:CD180696-6558-469E-8FE5-66BFBB6E3E00][tenor_schedule.code]].
Null for =ANCHOR_OFFSET= rows (no schedule involved).

#+begin_src cpp :name generator
std::nullopt
#+end_src

** schedule_step_count
:PROPERTIES:
:type:          integer
:cpp_type:      std::optional<int>
:nullable: true
:END:

Number of steps along =schedule_code='s= axis to the resolution date
(e.g. =1= for a CDS =1Y 1RQ= tenor, =8= for the eighth FOMC meeting).
Same nullability rule as =schedule_code=.

#+begin_src cpp :name generator
std::nullopt
#+end_src

* SQL

** Flags
:PROPERTIES:
:tablename: ores_refdata_tenor_convention_resolutions_tbl
:END:

* Repository
:PROPERTIES:
:name_singular_short: resolution
:name_short:          resolutions
:name_singular_words: tenor convention resolution
:name_words:          tenor convention resolutions
:order_column:        tenor_code
:END:

* C++

** Flags
:PROPERTIES:
:subcomponent:    api
:read_only:       true
:END:

** Domain includes

#+begin_src cpp :name includes
#include <chrono>
#include <optional>
#include <string>
#+end_src

** Entity includes

#+begin_src cpp :name includes
#include <optional>
#include <string>
#+end_src

** Conventions
:PROPERTIES:
:iterator_var: r
:END:

** Table display

| column              | header              |
|---------------------+---------------------|
| convention_code     | Convention          |
| tenor_code          | Tenor               |
| anchor_override     | Anchor Override     |
| offset_unit         | Offset Unit         |
| offset_multiplier   | Offset Multiplier   |
| schedule_code       | Schedule            |
| schedule_step_count | Schedule Step Count |

* See also

- [[id:C4D8A2E6-3B7F-4A1D-9C5E-8F2A6D3B1E90][Tenor Convention]] — the left side; the convention's own default anchor and resolution algorithm this row can override or supply values for.
- [[id:9A2E4D6B-7C1F-4B8A-A5D3-2F6E9B1C4A87][Tenor]] (entity model) — the right side.
- [[id:3F8B6C2A-1D4E-4A7F-9B3C-6E2D8F1A5C90][Tenor Anchor]] — the lookup =anchor_override= references.
- [[id:0AC88EB3-DB7F-4135-9DA6-0ED4583FEC29][Tenor]] — the "Tenor conventions by curve type" domain concept this junction operationalises.
- [[id:013BC5B0-9461-4AD4-A55E-374BCF34D8A4][IMM Dates]] — the roll schedule =ROLL_QUARTER= offsets are counted against.
