:PROPERTIES:
:ID: D07E1889-FD0C-4F0B-AB3F-60D1519D08BA
:END:
#+title: Task: Lift libpq Windows workaround to PostgreSQL imported target
#+description: PRs #708 and #709 patched the Windows libpq link workaround (\`pgport\`, \`pgcommon\`, \`OpenSSL::SSL\`/\`Crypto\`, \`Se
#+type: task
#+level: s1
#+filetags: :windows_portability:sprint_17:v0:
#+owner: marco
#+branch:
#+pr: https://github.com/OreStudio/OreStudio/pull/712
#+blocked_on: none
#+blocked_since:
#+created: 2026-05-22
#+updated: 2026-05-22
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:7D84E735-7DFE-4215-AF00-A1F4C9323BDA][Windows portability fixes]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Consolidate the scattered Windows libpq link workaround into a single PostgreSQL CMake imported target so all downstream consumers pick it up automatically.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:7D84E735-7DFE-4215-AF00-A1F4C9323BDA][Windows portability fixes]] |
| Now          | Completed 2026-04-20.               |
| Waiting on   | Nothing.                               |
| Next         | None.                                  |
| Last touched | 2026-05-22                               |

* Acceptance

- libpq Windows workaround is lifted to a reusable CMake helper at the =postgresql= level.
- All consumers pick up the fix without per-component duplication.
- PR #710 landed.

* Plan

* Notes

** Summary
PRs #708 and #709 patched the Windows libpq link workaround (=pgport=, =pgcommon=, =OpenSSL::SSL=/=Crypto=, =Secur32=, =Wldap32=) onto =ores.database=. That cleared the symptom there, but every other DLL that transitively pulls libpq in via =sqlgen= now reports the same undefined references — the latest failing build had =ores.refdata.core.dll= tripping over =pg_tolower=, =pg_strong_random=, =gettimeofday=, =strlcpy=, =RAND_bytes=, =BIO_new=, ... The per-target patch does not scale.

*Fix*: lift the workaround to the root =CMakeLists.txt= and append the libraries directly onto the =PostgreSQL::PostgreSQL= imported target. =sqlgen='s exported config already declares =PostgreSQL::PostgreSQL= in its =INTERFACE_LINK_LIBRARIES=, so every consumer — =ores.database=, =ores.refdata.core=, and any future one — picks the fix up through normal CMake transitivity with zero per-target edits. The per-target block in =ores.database/src/CMakeLists.txt= is removed as now-redundant.


* Result

Landed.
