:PROPERTIES:
:ID: F5426198-ACAF-41DA-A043-FF15785E4A1D
:END:
#+title: Sprint planning
#+description: The first sprint phase: define the mission, choose stories from the backlog, scaffold the sprint, promote captures into stories, and decompose them into tasks.
#+type: knowledge
#+level: cross
#+filetags: :agile:sprint:planning:knowledge:
#+created: 2026-06-05
#+updated: 2026-06-05

* Summary

In agile practice, /sprint planning/ is the ceremony that opens an
iteration: the team agrees what the sprint is for, selects the work
that serves that goal from the backlog, and breaks it down far enough
that execution can start. Good planning is goal-led — the sprint goal
filters the backlog, not the other way around.

In ORE Studio, sprint planning is the moment a new
[[id:0820B7FD-147C-4832-AC25-C043D38D5B61][sprint]] acquires its identity. The [[id:198DA730-D9FB-4BDE-B1C0-B930CD019917][sprint mission]] is written
/first/ and acts as the filter for everything else: [[id:671F18E4-E09C-4B3B-BD24-D33DF8AE38A6][captures]] are
pulled from the [[id:9A17E8DF-7E94-4F55-8A14-BCB7D9A16D6B][next]] bucket only if they fit the mission and the
sprint's size budget, promoted into [[id:4FAC3AE2-04A8-4C35-B72E-78C7A862EED9][stories]], and decomposed into
one-PR [[id:A7CF5191-6786-408F-A4A6-3A508DCADAC1][tasks]]. Planning ends when the sprint is ready: mission
filled and at least one story BACKLOG-ready.

* Detail

** Ownership

*Owned by* [[id:8F13BA75-EDAA-46CC-9BE2-E356D60BF4F4][System 3]]. [[id:6DF00A64-7CC3-40A7-950B-55D2B05A31C7][System 5]] vets each candidate against the
[[id:2F71292F-CDB0-4E2E-B50F-4F02E10597C4][product vision]]; [[id:6CA8B181-BD81-4EF4-AF27-64F5491D62F0][System 4]] sanity-checks the sprint mission
against the version mission; [[id:BF4F183F-BB33-44F2-A53F-D9BD602E9BE8][System 2]] decomposes each promoted
story into tasks.

** Opening a sprint

A sprint exists when its folder exists. To open sprint N+1:

1. *Define the sprint mission.* One sentence: what this sprint exists
   to achieve. The mission is the /filter/ that decides whether
   borderline captures make it in. Write it before looking at the
   backlog so the choice of stories is led by the mission, not the
   other way round. See [[id:198DA730-D9FB-4BDE-B1C0-B930CD019917][sprint mission]].
2. *Choose stories.* Look at the [[id:9A17E8DF-7E94-4F55-8A14-BCB7D9A16D6B][next]] bucket; pick captures whose
   combined size fits one sprint's budget /and/ whose theme matches
   the mission. Captures that don't fit the mission stay in the
   bucket. Sprints are time-boxed at roughly one mission's worth of
   work; the budget is empirical (past sprints' task counts and
   elapsed days under =doc/agile/versions/v0/=), not a fixed
   hour-count.
3. *Create the sprint folder.* Use compass: =compass add sprint
   --parent-dir doc/agile/versions/<version>=. Slug is =sprint_NN=.
4. *Promote each chosen capture.* For each:
   - =git mv= the capture file from its bucket into
     =doc/agile/versions/<version>/<sprint_NN>/<slug>/story.org=.
   - Flip =#+type:= from =capture= to =story=. The =:ID:= is preserved.
   - Apply the v2 story shape: blurb, =* Goal=, =* Status= (table,
     state =BACKLOG=), =* Acceptance=, =* Tasks= (empty), =* Decisions=,
     =* Out of scope=.
   - Re-run =regenerate_backlog_indexes.py=; the entry disappears from
     the bucket index automatically.
5. *Decompose stories into tasks.* Each task is one PR; see the
   sizing rules in [[id:A7CF5191-6786-408F-A4A6-3A508DCADAC1][Tasks]]. Generate task files with =compass add
   task --parent-dir <story-dir>=. Order them in the story's
   =* Tasks= table by execution order. Tasks start =BACKLOG=.
6. *Regenerate the version's sprint table* on the version page.

** When planning is done

The sprint is /ready/ when its Mission is filled and at least one
story is BACKLOG-ready (see [[id:4FAC3AE2-04A8-4C35-B72E-78C7A862EED9][Stories]] for story readiness). Execution
can begin; planning does not need every story fully decomposed up
front.

* See also

- [[id:46DFE47F-967B-4C24-95BF-3E21F97CFD20][Agile process]] — the loop around the phases.
- [[id:227958A5-0F78-4CC6-A116-B83988592B68][Sprint execution]] — the next phase.
- [[id:47032D2E-4922-4A43-B594-AD76F0213E10][Open a new sprint]] — the runbook that drives this phase
  end-to-end, including closing the previous sprint.
- [[id:D4219199-7B17-4A89-B4BC-6019738642DA][Product backlog]] — where candidates come from.
- [[id:A064D838-F127-4DD6-BB42-9A7902039AEE][Sprint themes]] — how chosen stories are grouped in the sprint
  backlog.
