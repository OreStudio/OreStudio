:PROPERTIES:
:ID: C86FFED4-AD4E-4330-B542-95B36760ED2F
:END:
#+title: Task: Enforce uppercase UUID invariant in codegen and audit docs
#+description: Patch v2 doc templates and Python scripts so every generated UUID and link is uppercase; sweep org docs for stragglers.
#+type: task
#+version: 2
#+level: s1
#+filetags: :documentation:codegen:audit:uppercase_uuid_invariant:sprint_18:v0:
#+owner: marco
#+branch: feature/minor-doc-fixes
#+pr:
#+blocked_on: none
#+blocked_since:
#+created: 2026-05-24
#+updated: 2026-05-24
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:BAF1DEAE-59DF-49AB-BFCD-E6D09496DA43][Lock down uppercase UUID invariant]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Close every code path that can still emit a lowercase UUID into the documentation graph, and run one final sweep to confirm the existing on-disk corpus is clean.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                                   |
| Parent story | [[id:BAF1DEAE-59DF-49AB-BFCD-E6D09496DA43][Lock down uppercase UUID invariant]] |
| Now          | Completed 2026-05-24.                  |
| Waiting on   | Nothing.                               |
| Next         | None.                                  |
| Last touched | 2026-05-24                             |

* Acceptance

- Every =v2_doc_*.org.mustache= template under =projects/ores.codegen/library/templates/= renders only uppercase UUIDs (verified by running each =generate_v2_doc.sh --type ...= variant against a scratch dir and grepping for =[a-f]= inside =[[id:...]]= and =:ID:=).
- =v2_doc_generate.py= calls =.upper()= on =parent_id= and =predecessor_id= before they reach the renderer.
- =build/scripts/generate_release_notes.py= calls =.upper()= on the generated UUID.
- =projects/ores.codegen/scripts/regenerate_backlog_indexes.py= holds its UUID string constants in uppercase.
- A full-tree grep (=grep -rEn '[a-f]' --include='*.org' [...]=) for lowercase hex inside =:ID:= properties and =[[id:...]]= links across =doc/= and =projects/*/modeling/= returns zero real matches (template placeholders such as =[[id:UUID]]= and =[[id:<parent_id>]]= are excluded).
- A note is added to =doc/recipes/codegen/how_do_i_create_a_new_v2_doc.org= or =doc/meta/document_types.org= documenting the invariant, so future contributors know not to undo it.

* Plan

Plan distilled into the parent story's =* Decisions= section on close — see [[id:BAF1DEAE-59DF-49AB-BFCD-E6D09496DA43][Lock down uppercase UUID invariant]].

* Notes

- The audit that fed this plan was performed on 2026-05-24 on =feature/minor-doc-fixes=. It found zero lowercase IDs in the on-disk org corpus (confirming prior normalisation commits held) and four code-side leak sources (templates, =parent_id= / =predecessor_id= pass-through, release-notes generator, backlog-indexes constants).
- The newly generated =story.org= and =task.org= for /this/ story themselves shipped with lowercase concept-link UUIDs from the templates — fixed by hand in the same commit that introduced them. They are Exhibit A for why the template fix was needed.

* Result

All eight plan steps executed. On-disk org corpus and codegen-generated documents now use uppercase UUIDs by construction; the contract is documented in [[id:8B29A8D8-E004-4E87-B81D-ABEAF68F2B98][document types]].

Verification:

- Regression sweep: =grep -rEn '^:ID:\s+[A-Za-z0-9-]*[a-f]' --include='*.org' doc/ projects/= → 0 matches.
- Regression sweep: =grep -rEn '\[\[id:[0-9a-f-]{20,}[a-f][0-9a-f-]*\]' --include='*.org' doc/ projects/= → 0 matches.
- Codegen smoke test: generated one of each v2 doc type under a scratch dir; every =:ID:= and every concept-link UUID was uppercase. Defensive upper on =--parent-id= verified by passing a lowercase value and observing the rendered link as uppercase.
