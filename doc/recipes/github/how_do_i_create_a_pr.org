:PROPERTIES:
:ID: CC928252-E0C5-4C3C-A7A8-D613593E4E37
:END:
#+title: How do I create a PR?
#+description: Open a pull request via the gh CLI with a structured title and HEREDOC body — and the conventions ORE Studio follows on title format.
#+type: recipe
#+version: 2
#+level: cross
#+filetags: :github:pr:gh:recipe:
#+created: 2026-05-21
#+updated: 2026-05-21

The body content should be assembled via [[id:1882D6DF-4012-4C6E-BF62-0D943EF805B0][How do I generate a PR
summary?]] before running the =gh= command here.

* Question

How do I open a pull request from my current feature branch, with a
correctly-formatted title and a structured body?

* Answer

1. /Push the branch/ first if it isn't on the remote yet:

   #+begin_src sh :results verbatim
   git push -u origin "$(git branch --show-current)"
   #+end_src

2. /Create the PR/ with =gh pr create=. Use a HEREDOC for the body
   so newlines are preserved:

   #+begin_src sh :results verbatim
   gh pr create --title "[component] One-line description" \
     --body "$(cat <<'EOF'
   ## Summary

   Brief paragraph.

   ## Changes

   - First change
   - Second change
   EOF
   )"
   #+end_src

3. /Confirm the PR URL/ that =gh= prints; verify the title format
   matches =[COMPONENT] Description=.

* Conventions

- /No "Test plan" section/ in the body.
- /Title format/ is =[COMPONENT] Description=. Multi-component PRs
  use =[a,b,c]=.
- Body sections may be expanded (Notes, Decisions, Follow-ups), but
  Summary and Changes are the load-bearing pair.

* Script

Bare =gh= CLI; no wrapper. The CLI must be authenticated (=gh auth
login=) once per checkout.

* Tested by

Manual. Reviewers verify the PR shape; =gh pr checks= reports CI.

* See also

- [[id:1882D6DF-4012-4C6E-BF62-0D943EF805B0][How do I generate a PR summary?]] — assemble the body.
- [[id:F6E88DCC-9240-4632-A9A3-9F36F79C9262][How do I monitor a PR until green?]] — what to do once it's open.
- [[id:95D560C6-3551-4810-9D11-55F9F82B483C][How do I merge a PR?]] — close it out.
- [[id:545D6B43-17FC-0234-FF2B-AB6565695616][PR Manager]] — full lifecycle.
