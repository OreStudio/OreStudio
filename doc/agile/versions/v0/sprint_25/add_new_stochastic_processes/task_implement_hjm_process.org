:PROPERTIES:
:ID: 65233C15-4ABD-4457-B238-0FD994028978
:END:
#+title: Task: Implement Heath-Jarrow-Morton forward-curve process
#+description: Implement an HJM forward-rate engine where the state is a discretized forward curve f(t,T_i) at N tenor points. Data-oriented: the forward curve is a vector of doubles, evolved by a free function that applies volatility structure sigma(t,T_i) and the HJM no-arbitrage drift condition. discount_factor() integrates the forward curve from the current discretization. Reference: QuantLib HJM class.
#+type: task
#+level: s1
#+filetags: :add_new_stochastic_processes:sprint_25:v0:
#+owner: marco
#+branch:
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-08-07
#+updated: 2026-08-11
#+environment: eager_maxwell
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:C603D702-DE7E-4DA4-8409-6FCB0F64808F][Add new stochastic processes]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The Heath-Jarrow-Morton process appears as a selectable process type in the IR curve generation config UI, with its own parameter fields (initial forward curve, volatility structure) stored in a strongly-typed child table. The engine itself is complete (merged via PR #1932); the system wiring is the remaining scope, following the G2 precedent.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | BACKLOG                              |
| Parent story | [[id:C603D702-DE7E-4DA4-8409-6FCB0F64808F][Add new stochastic processes]] |
| Now          | Engine implemented and merged (PR #1932); system wiring remaining. |
| Waiting on   | Nothing.                               |
| Next         | Wire into the system (factory, seed data, config storage, Qt UI), following the G2 precedent. |
| Last touched | 2026-08-11                               |

* Acceptance

- Seed data row makes the Heath-Jarrow-Morton process appear in the process type combo box
- process_factory constructs a working Heath-Jarrow-Morton engine (factory method and parameter struct)
- Heath-Jarrow-Morton parameters persist to and load from the child table
- IrCurveGenerationConfigDetailDialog shows Heath-Jarrow-Morton parameter fields when selected
- Existing Vasicek/CIR/Hull-White/G2 configs are unaffected

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

Moved from the IR curve follow-ups story on 2026-08-11: the engine work is complete (merged via PR #1932 on 2026-08-10), but the engine is not wired into the system. The remaining scope — system wiring (factory, seed data, config storage, Qt UI) — is tracked here, under the Add new stochastic processes story, following the G2 wiring precedent.

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1932][#1932]] | [ores.analytics.quant] Implement six stochastic IR processes |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Drift documented as "exactly the no-arbitrage drift of the piecewise-linear interpolation", but the code implements a right-endpoint sum (piecewise-constant vol convention) while discount_factor() integrates with the trapezoid rule | heath_jarrow_morton_process.hpp | Accepted | Softened: the discrete analogue of the continuum drift, first-order consistent with the continuum model |

* Result

Implemented =heath_jarrow_morton_process=: a forward-curve engine whose state
is a discretized forward-rate curve at fixed relative tenors. The
no-arbitrage drift is the public =hjm_no_arbitrage_drift= free function (the
trapezoid of the cumulative volatility); =next()= advances every tenor,
=current()= is the front (short) rate and =discount_factor()= integrates the
current curve by trapezoid with flat extension past the last tenor. 10
Catch2 tests covering frozen rates at =sigma == 0= reproducing the initial
curve exactly, the drift formula against hand-computed vectors, the
discounted-bond martingale property (statistical), Hull-White equivalence
under exponential volatility (statistical), discount monotonicity, seed
determinism and validation. Registered in =component_files.cmake=.

All 368 =ores.analytics.quant= tests pass on Linux. Not yet wired into
=process_factory= (deferred to PR 2: system infra wiring).

Merged via PR #1932 (merge commit 4c9b23d99) on 2026-08-10.
