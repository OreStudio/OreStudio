:PROPERTIES:
:ID: fea0593a-8ecb-43fb-83f9-6eff120492eb
:END:
#+title: Add serialisation tests for analytics domain types
#+description: No tests exist for analytics domain types (pricing engine type, pricing model config, etc.). Add basic round-trip serialisation tests for each.
#+type: capture
#+version: 2
#+level: cross
#+filetags: :code:testing:analytics:
#+created: 2026-05-22
#+updated: 2026-05-22

This page is a [[id:671f18e4-e09c-4b3b-bd24-d33df8ae38a6][capture]] in the *far* bucket of the product backlog — a pre-sprint idea, not yet pulled into a sprint as a story.

* What

(One paragraph: the idea.)

* Why

(Motivation, problem being solved, related context.)

* References

-

* See also

-
No automated tests exist for the analytics domain types (=PricingEngineType=, =PricingModelConfig=, etc.). At minimum, add basic round-trip tests:

- Construct a domain object with known field values.
- Serialise to the wire format (JSON / proto / SQL round-trip via the service).
- Deserialise and assert field equality.

These are unit-level tests and do not require a running database.
