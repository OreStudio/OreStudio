:PROPERTIES:
:ID: CA9CB414-9F61-4395-8183-A134BE9A2B9B
:END:
#+title: Task: Fix ACME provisioning recipe to be deterministic, curated, and self-contained
#+description: The acme_system_provision.ores recipe uses --source synthetic which creates random UK financial firms and picks one at random to activate. It must instead produce the carefully curated ACME holding group exactly: Acme Corporation Plc plus its three offices (UK, US, HK), each with their defined staff, book structure, business units, CRM topology, and market data — identically every run.
#+type: task
#+level: s1
#+filetags: :acme:sprint_25:v0:acme_corporation_followups:
#+owner: marco
#+branch: feature/fix-provisioning-failure-reporting
#+pr: 1983
#+blocked_on:
#+blocked_since:
#+created: 2026-08-09
#+updated: 2026-08-09
#+environment: eager_maxwell
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:BC5582FB-1648-42F2-9D6A-B6DB6EEBC676][Acme Corporation follow-ups: manual chapter, logo fixes, synthetic feeds]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

One recipe, one command, deterministic ACME group every time: holding company + 3 offices (UK, US, HK) with curated staff, books, BUs, CRM, and market data.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:BC5582FB-1648-42F2-9D6A-B6DB6EEBC676][Acme Corporation follow-ups: manual chapter, logo fixes, synthetic feeds]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-08-09                               |

* Acceptance

- acme_system_provision.ores produces exactly the same 4-party ACME hierarchy every run
- All ACME staff accounts are present with correct per-office roles
- Book and business-unit structure matches the curated ACME definition
- CRM topology and market data are active for all 3 offices
- No random/synthetic firm names appear (only ACME + GLEIF counterparties)

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
| [[https://github.com/OreStudio/OreStudio/pull/1983][#1983]] | [ores.iam] Surface exact workflow failure reasons on provisioning |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
| 1 | Log separator missing between instance id and error message | internal_request_client.cpp | Accepted | Fixed in 5e3065a435 |
| 2 | fail_with doc comment still cites the replaced "workflow did not complete" fallback | tenant_handler.hpp | Accepted | Fixed in de8422bf5b |
| 3 | Timeout-budget comment claims per-party 10-minute waits; actual waits are 1500s base, 600s LEI, 120s default per-party | provision_commands.cpp | Accepted | Fixed in 8b58315bdd |
| 4 | Orphaned step-0 row left in_progress when start aborts after step persistence | workflow_engine.cpp | Accepted | Fixed in c13538bd7c |
| 5 | No-steps recovery branch never publishes a status event | workflow_engine.cpp | Accepted | Fixed in c47cc88f8d |

* Result

Both PRs delivered and merged:

- [[https://github.com/OreStudio/OreStudio/pull/1983][#1983]]: honest provisioning failure reporting. The workflow engine records start failures on the instance and recovers step-less orphans; the query protocol carries instance status/error; the wait client returns the exact reason (failed step, failed instance, timeout with progress); =publish_bundle= propagates it into the step action so the shell prints the real error; wait windows raised to 1500 s (base), 600 s (acme_lei_import), 45 min (shell).
- [[https://github.com/OreStudio/OreStudio/pull/1984][#1984]]: FOMC-dated tenor allowed for OIS IR curve configs, scoped to =index_family = 'sofr'= (model, generated SQL constraint, regenerated C++ header).

Verified end-to-end against [[id:D28E67F7-0F2B-4184-B338-57D0F8A29DEC][scenario_acme_readiness]] on a fresh database with the official provisioning script: script exits 0 with no failed steps; login lands on Acme Corporation Plc with no setup wizard; exactly 4 parties all Active (9,695 LEIs); =tenant_admin= default_party_id = Acme Corporation Plc; onboarding suppressed; 43,834 GLEIF counterparties.

Findings recorded, not regressions:

- Provisioning does not create market-data feed bindings (scenario step 7 anticipated gap); rates render static until bindings are created via a client.
- BU/portfolio/book/account structure is richer than the scenario's minimal targets (15 BUs / 59 accounts in UK vs 3 / 9) — pre-existing, user-validated.
