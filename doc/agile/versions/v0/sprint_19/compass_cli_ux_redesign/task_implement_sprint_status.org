:PROPERTIES:
:ID: A5B4156C-B75A-42D9-9E08-2AB8F5504A8D
:END:
#+title: Task: Implement compass sprint status
#+description: Add compass sprint status subcommand: all stories in current sprint grouped by DONE/STARTED/BACKLOG with task counts.
#+type: task
#+level: s1
#+filetags: :compass_cli_ux_redesign:sprint_19:v0:
#+owner: marco
#+branch: feature/implement-sprint-status
#+pr:
#+blocked_on: none
#+blocked_since:
#+created: 2026-06-01
#+updated: 2026-06-01
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:A596D1E7-6A74-4E55-996D-6F1F2E48F261][Compass CLI UX redesign]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Add =compass sprint status= — a new subcommand that prints all stories in
the current sprint grouped by state (DONE / STARTED / BACKLOG) with task
counts per story. Addresses the gap where =compass where= only shows
STARTED items.

* Status

| Field        | Value                                       |
|--------------+---------------------------------------------|
| State        | DONE                                        |
| Parent story | [[id:A596D1E7-6A74-4E55-996D-6F1F2E48F261][Compass CLI UX redesign]]                    |
| Now          | Nothing.                                    |
| Waiting on   | Nothing.                                    |
| Next         | Done. T4: implement compass story status.   |
| Last touched | 2026-06-01                                  |

* Acceptance

- =compass sprint status= prints all stories in the current sprint grouped
  by state: DONE first, then STARTED, then BACKLOG.
- Each story row shows: title, state, start/end dates, and task count
  (done/total, e.g. =3/5=).
- =compass sprint status --format json= emits machine-readable output.
- =compass sprint= is registered in =--help= under the Orient pillar.
- =compass sprint status= reads story state from each =story.org= directly
  (not from =sprint.org=) to avoid the drift problem identified in the
  sprint status audit.
- CI passes.

* Result

- =compass sprint status= implemented in =compass.py=.
- Stories read from =story.org= files directly (not =sprint.org=) to avoid drift.
- Grouped by state: DONE / STARTED / BLOCKED / BACKLOG; alphabetical within each group.
- Task counts (done/total) shown per story from =task_*.org= glob.
- =--format json= emits machine-readable output.
- Wired into =main()= short-circuit and argparse registry.
- =_STATE_RE= and =_TITLE_RE2= regexes shared with upcoming =compass story status=.

* Plan

From [[id:ACBF9164-1806-4472-8C96-C57821AD5F29][UX analysis]]:

- =compass sprint= is a sub-subcommand group (like =compass journal=).
- =compass sprint status= is the only sub-subcommand in sprint 19 scope.
- Add =cmd_sprint(argv)= following the pattern of =cmd_journal(argv)=:
  argparse sub-subcommand dispatcher.
- Implementation: find current sprint dir → glob =*/story.org= → read
  =#+title:= and =| State |= from each → group by state → print.
- Task count per story: glob =task_*.org= in same dir → count DONE vs total.
- Wire into =main()= short-circuit and argparse registry.

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |
