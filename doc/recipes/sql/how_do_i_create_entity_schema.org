:PROPERTIES:
:ID: 479F858E-AA31-4DB4-ABE9-A0ED7CEBF098
:END:
#+title: How do I create a new entity SQL schema?
#+description: Walk through tables, indexes, grants, and history trigger for a new ORE Studio entity.
#+type: recipe
#+level: cross
#+filetags: :recipe:sql:entity:schema:
#+created: 2026-05-21
#+updated: 2026-05-27

Naming conventions and idempotency rules are in
[[id:5CB31A0E-4E38-4521-B0EE-E4396C3E2936][SQL entity schema patterns]]. The [[id:A08FC2A2-4E42-41EC-AC92-D1AF50DC61CB][codegen-add-sql-schema]] skill drives this
process; codegen is mandatory.

* Question

How do I create the SQL schema for a new entity in ORE Studio?

* Answer

1. Create a JSON model under =projects/ores.codegen/models/{component}/=
   using =*_domain_entity.json= (UUID PK) or =*_junction.json=
   (composite text PK) naming.

2. Generate the SQL schema file (written directly to [[proj:projects/ores.sql][=projects/ores.sql/=]]):

#+begin_src sh
./compass.sh codegen entity generate <entity> --address ores.sql.schema
#+end_src

3. Register the new schema file in the orchestration script for the component
   (typically =projects/ores.sql/scripts/{component}_schema.sh=).

4. Add RLS policies for the new entity.  Every entity table with =tenant_id=
   needs a tenant isolation policy; tables that also have =party_id= need a
   party isolation policy on top.  Add them to
   =projects/ores.sql/create/{component}/{component}_rls_policies_create.sql=
   (create the file if the component has none) and register it in
   [[proj:projects/ores.sql/create/rls/rls_create.sql][=projects/ores.sql/create/rls/rls_create.sql=]].  See
   [[id:5CB31A0E-4E38-4521-B0EE-E4396C3E2936][SQL entity schema patterns]] §Row-Level Security for the exact policy
   templates.

5. Run schema validation:

#+begin_src sh
./projects/ores.sql/utility/validate_schemas.sh
#+end_src

6. Raise a PR with title =[sql] Add {entity} schema=.

* Tested by

[[proj:projects/ores.sql/utility/validate_schemas.sh][=./projects/ores.sql/utility/validate_schemas.sh=]] — verifies
idempotency and naming conventions for all schema files.

* See also

- [[id:5CB31A0E-4E38-4521-B0EE-E4396C3E2936][SQL entity schema patterns]] — naming conventions and idempotency rules.
- [[id:A08FC2A2-4E42-41EC-AC92-D1AF50DC61CB][codegen-add-sql-schema]] — skill that drives this workflow.
- [[id:608675E1-4284-4902-99AC-4DA565390AFB][Entity lifecycle]] — layer ordering overview.
