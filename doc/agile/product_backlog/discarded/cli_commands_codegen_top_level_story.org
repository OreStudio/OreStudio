:PROPERTIES:
:ID: 469958E3-B249-4D4C-95A9-E7CDB703F0C0
:END:
#+title: CLI commands — top-level commissioning story
#+description: Discarded. ores.cli retired on 2026-09-23, so the CLI command layer this would have templatized no longer exists. The shell's command codegen is the surviving path.
#+type: capture
#+level: cross
#+filetags: :codegen:refdata:discarded:
#+created: 2026-07-03
#+updated: 2026-09-26

This page is a [[id:671F18E4-E09C-4B3B-BD24-D33DF8AE38A6][capture]] in the *discarded* bucket of the product backlog. The idea is ruled out rather than deferred, and the file stays as the record of why.

* What

Make *CLI entity commands* a top-level story rather than an "implement CLI
commands" task buried in each =commission_*= story. =ores.cli= has *no codegen
profile* (confirmed in the =compass-codegen-add-surface-entity= skill's "Codegen gap"
section) — entity commands are hand-written. Identical situation to the shell
layer: establish the per-entity pattern on a reference entity, add a generated
aggregator for registration, templatize into a codegen CLI facet, then roll out
per entity. Per-commission "implement CLI commands" tasks are superseded by this
and should be closed in favour of it. *Pick up after entities are commissioned* —
CLI is a horizontal layer, templatized once.

* Why discarded

=ores.cli= was retired on 2026-09-23 (story
[[id:32606E40-CCC2-4FFF-9CE4-2516CCC086B2][Decommission ores.cli]]), and the
65 CLI recipes went with it. There is no CLI command layer left to
templatize, so this capture cannot be done. The shell's command codegen
is the surviving path for the entity-facing console surface.

* Why

Like shell/HTTP/Wt, CLI is an entity-facing layer with no codegen and no home,
producing an identical deferred task in every commission story. One top-level
story removes the per-entity toil and gives every entity a consistent, generated
CLI surface.

* References

- Codegen gap: =.claude/skills/compass-codegen-add-surface-entity/SKILL.md= ("Codegen gap").
- The shell sibling that survives: [[id:8BCD0D1C-AAFB-4FAA-A0E3-022FC6EF93C2][Shell entity commands — top-level commissioning story]].

* See also

- [[id:8BCD0D1C-AAFB-4FAA-A0E3-022FC6EF93C2][Shell entity commands — top-level commissioning story]] (sibling; same approach).
- [[id:03FC50B3-622B-4456-AE66-2F180B526723][HTTP entity endpoints — top-level commissioning story]] (sibling).
- [[id:EEE4880F-525A-4805-8082-B8638CB54ABA][Wt entity UI — top-level commissioning story]] (sibling).
