:PROPERTIES:
:ID: 1C0EF2C8-E952-4807-92EF-A217C8DFF49A
:END:
#+title: Task: Wire cross-reference tables in story new / task new
#+description: compass story new and task new scaffold the story/task docs but leave their cross-references unwired and their Goal/Acceptance as placeholders. Observed: the new task is not added to the story's * Tasks table; story new does not add the new story to the sprint's * Stories table (the tool prints both as manual 'next steps'); and neither accepts --goal/--acceptance, so docs are born without content. Make scaffolding wire the story<->task and sprint<->story table rows automatically, and accept --goal/--acceptance (matching the documented expectation that scaffolding wires the tables and is born with content).
#+type: task
#+level: s1
#+filetags: :compass_quality_of_life:sprint_20:v0:
#+owner: marco
#+branch: feature/wire-crossrefs-in-story-task-new
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-06-08
#+updated: 2026-06-08
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:EE8D63CB-D468-4D91-A1D8-CDE5A70D3B38][Compass quality of life]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The Goto pillar (=compass story new= / =compass task new=) wires its
cross-references and is born with content, matching the Scaffold pillar
(=compass add=).

Observed gap (2026-06-08, while scaffolding the =suggest_next_work_item=
story and the =reuse_built_site_in_deploy= task):

- =compass add task= *does* wire the new row into the parent story's
  =* Tasks= table (prints "🔗 Wired into … (* Tasks, …)").
- =compass story new= and =compass task new= do *not*: the new task is left
  out of the story's =* Tasks= table, and =story new= leaves the new story
  out of the sprint's =* Stories= table — both are printed only as manual
  "next steps".
- None of =story new= / =task new= accept =--goal= / =--acceptance=, so the
  docs are born with placeholder Goal/Acceptance and must be filled by hand.

The inconsistency is the bug: the Goto pillar builds on the Scaffold pillar
but skips the wiring the Scaffold pillar already performs.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                                   |
| Parent story | [[id:EE8D63CB-D468-4D91-A1D8-CDE5A70D3B38][Compass quality of life]]                |
| Now          | Complete.                              |
| Waiting on   | Nothing.                               |
| Next         | None.                                  |
| Last touched | 2026-06-10                             |

* Acceptance

- =compass story new= adds the new story's row to the sprint =* Stories=
  table and the first task's row to the story =* Tasks= table.
- =compass task new= adds the new task's row to the target story's =* Tasks=
  table.
- =compass story new= and =compass task new= accept =--goal= and
  =--acceptance= and write them into the scaffolded doc (no manual fill-in).
- The "next steps" hint no longer lists table wiring as a manual step.

* Plan

Modified =projects/ores.compass/src/compass.py=:

1. Added =_wire_story_into_sprint(sprint_path, story_id, title, description)= — finds the
   last =| Story | State |= table in sprint.org and appends a BACKLOG row, matching
   the same guard (UUID already present → skip) used by =_wire_task_into_story=.

2. Updated =_scaffold_and_branch= signature to accept =goal=""= and =acceptance=[]= and
   pass them as =--goal= / =--acceptance=<val>= to =gen.main()= calls. Both story and
   first real task receive the flags; the scaffold task does not (it has a fixed goal).

3. In =_scaffold_and_branch= step 3 (new auto-wire block): after codegen, read the new
   UUIDs and call =_wire_story_into_sprint= + =_wire_task_into_story= for all created
   docs. Removed the manual "wire the story into sprint.org" from Next steps.

4. Added =--goal= / =--acceptance= (append) to both =story new= and =task new= parsers;
   pass through to =_scaffold_and_branch= via =goal== / =acceptance== keyword args.

* Notes

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

All four acceptance criteria met:

- =compass story new= now calls =_wire_story_into_sprint= (new helper) to append the
  story's BACKLOG row to the last =| Story | State |= table in =sprint.org=, and calls
  =_wire_task_into_story= for both the scaffold task and the first real task.
- =compass task new= now calls =_wire_task_into_story= for the new task, printing
  the same "🔗 Wired into …" confirmation that =compass add task= prints.
- Both commands now accept =--goal= and =--acceptance= (append), passed through to
  =gen.main()= so the scaffolded docs are born with filled content.
- The "next steps" list no longer mentions wiring as a manual step.
