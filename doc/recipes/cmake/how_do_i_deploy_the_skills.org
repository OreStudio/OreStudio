:PROPERTIES:
:ID: B5F231A9-5403-482C-8F2A-12D6917C58CD
:END:
#+title: How do I deploy the skills?
#+description: Build the Claude Code skills bundle locally with compass build --direct skills (no cmake/vcpkg needed).
#+type: recipe
#+level: cross
#+filetags: :cmake:build:skills:claude_code:emacs:recipe:
#+created: 2026-05-18
#+updated: 2026-05-18

For where the output lands and how the build script works, see
[[id:5AAE6900-8488-4E22-9F31-A1B9D5320EFB][CMake setup]].

* Question

How do I build the Claude Code skills bundle locally?

* Answer

=compass build --direct skills= invokes Emacs in batch mode against
[[proj:projects/ores.lisp/src/ores-build-skills.el][=projects/ores.lisp/src/ores-build-skills.el=]], which exports each skill under [[proj:doc/llm/skills][=doc/llm/skills/=]] to
the format Claude Code expects. It calls Emacs directly (no cmake, no
vcpkg), so it works in a light environment:

#+begin_src sh :results verbatim
./compass.sh build --direct skills
#+end_src

Output lands under =build/output/= in the skills directory configured
by =CMakeLists.txt= (=ORES_SKILLS_DIRECTORY=).

In a full environment the equivalent =deploy_skills= CMake target
(=cmake --build --preset <preset> --target deploy_skills=) runs the same
script; prefer the compass command above.

** Prerequisites

- Emacs on PATH. (No cmake configure step is required for =--direct=.)

* Script

[[proj:projects/ores.lisp/src/ores-build-skills.el][=projects/ores.lisp/src/ores-build-skills.el=]] is the Emacs script that =compass build --direct
skills= runs. The equivalent =deploy_skills= CMake target definition
lives in =CMakeLists.txt= near =add_custom_target(deploy_skills ...)= and
invokes the same script.

* Tested by

No dedicated CI workflow yet — the target is exercised by manual
deployment when a skill is updated.

* See also

- [[id:5AAE6900-8488-4E22-9F31-A1B9D5320EFB][CMake setup]]
- [[id:6930472B-222C-4621-AB37-CFEE3070A8BD][How do I deploy the site?]] — parallel target for the website.
- [[id:3DDB2AFA-9CAA-44A0-A91A-645A95320A5F][How do I deploy the settings?]] — parallel target for Claude Code permissions.
