:PROPERTIES:
:ID: 1c778ce6-25d6-429b-920e-d08a2ce9a0a6
:END:
#+title: Sprint 16
#+description: Final v0 sprint. ORE import end-to-end; workflow engine + saga orchestration; market data subsystem; service lifecycle controller; Qt plugin architecture; trade detail dialog unification; per-type instrument tables; reporting + analytics; asset class unification; massive Windows portability sweep.
#+type: sprint
#+version: 2
#+level: s3
#+filetags: :ore:workflow:marketdata:controller:plugins:trading:reporting:windows:sprint_16:v0:
#+created: 2026-05-20
#+updated: 2026-05-20
#+todo: STARTED | DONE

Parent version: [[id:e6fd30ed-963e-4705-b414-91bf471c23d0][Version 0]].

* DONE Status
:PROPERTIES:
:CLOSED: [2026-04-17]
:END:

** Now
Sprint closed 2026-04-17. Final v0 sprint — closes v0.

** Waiting on
None.

** Next
v1 work begins on the basis laid by v0.

** Last touched
2026-04-17

* Mission

/Work on ORE import./ Delivered ORE import end-to-end (dispatch
pipeline + mappers for every instrument family + server-side
=ores.ore.service= + supporting market + fixings + calendar +
conventions import + golden roundtrip tests + portfolio export).
Plus: workflow engine + saga orchestration (party provisioning + ORE
import + report execution); market data subsystem from scratch
(parser + schema + repository + service + UI); =ores.controller=
service lifecycle component; Qt plugin architecture (=ores.qt.api= +
7 domain plugins); trade detail dialog unification (six instrument
families merged behind =IInstrumentForm= registry); per-type
instrument tables (rates / FX / equity); reporting subsystem +
analytics pricing engine; asset class unification (asset_class as
risk taxonomy / product_type as structural discriminator); UTC-
everywhere timestamp policy; ~20 Windows portability fixes; party-
isolation RLS gaps closed.

The largest v0 sprint by clock (157h) and by task count (89). It
closes v0.

* Stories

In execution order (loose grouping by theme):

- [[id:c1db4c7a-16d7-4289-a457-a6c6ce5c8b5c][Sprint 16 housekeeping]] — backlog refinement.
- [[id:ffc60db1-b82a-4c8a-961e-2c6720379508][Asset class and product_type unification]] — rename
  + analysis + implementation.
- [[id:2faf5366-2fed-4fbe-99f2-8cb46ca84aeb][Party isolation RLS policies]] — close the gaps.
  Continues from sprint 13 party_isolation_books_portfolios_trades.
- [[id:4fcd03ac-51ad-47ad-8203-faefaa4211b9][Workflow engine]] — scaffold + FSM state UUIDs +
  generalised event-driven engine.
- [[id:c0ed556b-57a4-4bce-9196-70bcf4fd0d63][Party provisioning saga]] — first concrete saga + UX.
- [[id:87599263-bd70-42cf-b653-0803e12f4d0b][Workflow monitor]] — Qt plugin + late fixes.
- [[id:8361bc43-bd96-433d-889e-367072bad44e][ORE import pipeline]] — the sprint's mission.
  Continues from sprint 15 fix_ore_import_bugs.
- [[id:44ecad9c-332a-48f5-acb4-7c30f9eb8ddf][ORE import supporting files]] — market.txt + fixings.txt
  + calendar + conventions.
- [[id:43f68b11-1870-46db-bfc3-c5fbc83fe6cf][ORE golden roundtrip tests]] — three test PRs.
- [[id:fb0471f1-6bd2-4955-ac39-15d7800bb973][ORE portfolio export]] — exporter + Qt actions.
- [[id:47ce4c06-2c59-4b8b-89b3-232d04b2da23][Market data subsystem]] — parser + schema + repo +
  service + Qt UI.
- [[id:7399693e-f61b-4e3d-b37c-f53f3ce5a197][Generic object storage API]] — ores.storage + compute
  grid migration.
- [[id:08d2b499-53ce-4b1c-92cf-a7c26d036ec9][Reporting and analytics]] — DQ-sourced report defs +
  execution workflow + analytics pricing engine. Continues from
  sprint 14 reporting_subsystem.
- [[id:1490e130-08f9-4d43-8f80-d7e0f5022fbd][Trade detail dialog unification]] — six instrument
  family merges + IInstrumentForm registry.
- [[id:ce26e56b-944f-4c89-b5a9-e0dabf521908][Per-type instrument tables]] — rates / FX / equity.
  Continues from sprint 15 cdm_instruments.
- [[id:014352d4-aaa5-48e7-903d-c06b5b4f7d62][Service lifecycle controller]] — ores.controller +
  unified NATS heartbeat. Continues from sprint 15 compute_grid_
  observability.
- [[id:38c972e1-2dd2-41ae-a770-800fee90d1e6][Qt plugin architecture]] — ores.qt.api + 7 domain plugins.
- [[id:2d496a77-5238-4b5b-8cc6-c61fc59fa9c1][Scheduler UI complete]] — Job Instances + Monitor.
- [[id:ba5268af-df0e-4338-a6ce-1d5f84e1166d][Nats-Session-Id]] — session-level correlation.
- [[id:1997ccef-3e2f-44c9-ab1c-9435a646bd61][UTC-everywhere timestamp policy]] — canonical UTC API.
- [[id:cef64a10-082e-447f-8a39-f8f60d984f11][Windows portability fixes]] — ~20 fixes surfaced by
  Windows builds.
- [[id:2c3a9e28-68dc-4bf1-8eb7-5c4379e5c248][Engineering hygiene]] — clock_cast, nodiscard, codegen
  profiles, menu repositioning.

* Retrospective

** What went well

- ORE import landed end-to-end inside the sprint — pipeline +
  mappers for all six instrument families + supporting files +
  server-side service.
- Workflow engine generalised mid-sprint and immediately consumed
  two sagas (party provisioning + ORE import); the engine becomes
  load-bearing infrastructure rather than party-specific machinery.
- Market data subsystem from scratch in one sprint: parser + schema
  + domain + repository + service + UI.
- Trade detail dialog unification (six merge PRs + IInstrumentForm
  registry) closes a long-running UX inconsistency.
- Qt plugin architecture (ores.qt.api + 7 domain plugins) prepares
  the codebase for v1's modularity needs.
- Windows portability sweep — 20+ fixes — gets Windows CI back to
  green and keeps it there.
- Asset class unification finally separates risk-taxonomy
  (asset_class) from structural-routing (product_type).
- Sprint hit 89 tasks across 22 stories; everything either DONE or
  documented as next-version work.

** What hurt

- 157h sprint — by far the longest. The breadth was hard to keep
  coherent + the review queue was substantial.
- Per-type instrument table refactor (rates / FX / equity) repeats
  parts of the sprint-15 CDM modelling work; that we needed both
  passes is evidence the first design wasn't quite right.
- Trade detail dialog merge surfaced several regressions in
  intermediate PRs (CDO zero values, new-trade tab visibility, etc.)
  that lingered across PRs before being cleaned up.
- Workflow engine generalisation came after the first concrete saga
  rather than before — saga-2 (ORE import) had to wait while the
  engine grew up.
- Windows portability fixes were largely reactive — surfaced by CI
  rather than prevented by deliberate cross-platform discipline.

** What changed

- =ores.controller= owns service lifecycle; the dashboard is the
  visible window into it.
- =ores.qt= is now =ores.qt.api= + 7 domain plugins; the monolith
  is gone.
- TradeDetailDialog drives every instrument family via the
  =IInstrumentForm= registry.
- ORE import is end-to-end with mappers for every family + server-
  side service + supporting market data / fixings / calendar /
  conventions.
- Workflow engine is general-purpose; party provisioning and ORE
  import are both event-driven sagas on top of it.
- Market data is a real component with its own subsystem.
- =asset_class= and =product_type= are recognised as distinct
  concepts with shared refdata source of truth.
- Per-type instrument tables (rates / FX / equity) replace the
  generic instruments table from sprint 15.
- Party isolation RLS gaps closed; trading instrument subtables
  have proper RLS.
- UTC-everywhere timestamp policy enforced from DB to wire format.
- Windows is a first-class build target with the ~20 portability
  fixes that landed this sprint.

** Closing v0

This is the final v0 sprint. v0's mission was to /establish the
main architectural tiers (database, services, UI), model all ORE
inputs and outputs, and add GUI support for every data operation/.
That mission is delivered: every ORE entity type is modelled +
importable + manageable through the GUI; the platform has multi-
tenant + multi-party RLS, JWT-based auth, NATS-based microservices,
a compute grid, a scheduler, a workflow engine, an analytics +
reporting stack, and a Qt plugin architecture. v1 work picks up
from here.
