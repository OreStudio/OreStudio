:PROPERTIES:
:ID: 49f7da81-adb7-44ed-be7e-06dbfbb32a84
:END:
#+title: How do I set up a development environment?
#+description: Install all required packages on Ubuntu/Debian/WSL with one script, then initialise the database before the first build.
#+type: recipe
#+version: 2
#+level: cross
#+filetags: :cmake:build:environment:setup:recipe:
#+created: 2026-05-22
#+updated: 2026-05-22

Run once on a fresh checkout — including WSL. For the preset menu and
build commands see [[id:5aae6900-8488-4e22-9f31-a1b9d5320efb][CMake setup]].

* Question

How do I set up a development environment on Ubuntu, Debian, or WSL?

* Answer

1. /Install all required system packages/ with the bootstrap script:

   #+begin_src sh :results verbatim
   ./build/scripts/install_debian_packages.sh --full-install
   #+end_src

   Installs from distro repos: GCC, Clang, CMake, Ninja, PostgreSQL, Qt6,
   Valgrind, and X11/GL headers.

2. /Initialise the database/ before the first build:

   #+begin_src sh :results verbatim
   ./projects/ores.sql/recreate_database.sh
   #+end_src

3. /Configure and build/ (see [[id:7c44751e-54c0-4cb7-b489-1d3ec609a4e3][How do I configure the project?]] and
   [[id:f176fca1-68d5-420d-b076-75a65fee5ebb][How do I build the system?]]):

   #+begin_src sh :results verbatim
   cmake --preset linux-clang-debug-ninja
   cmake --build --preset linux-clang-debug-ninja
   #+end_src

* Script

=build/scripts/install_debian_packages.sh= — one-shot package installer.
=projects/ores.sql/recreate_database.sh= — drops and recreates all ORE Studio
databases with the current schema.

* Tested by

Manual, once per fresh environment. CI uses pre-configured runners and
does not exercise this path.

* See also

- [[id:5aae6900-8488-4e22-9f31-a1b9d5320efb][CMake setup]] — preset menu, output directories, build types.
- [[id:7c44751e-54c0-4cb7-b489-1d3ec609a4e3][How do I configure the project?]] — once-per-checkout configure step.
- [[id:f176fca1-68d5-420d-b076-75a65fee5ebb][How do I build the system?]] — day-to-day build command.
