:PROPERTIES:
:ID: 17CAD54D-0C46-427F-9117-E2B82346BAE5
:END:
#+title: Task: Add ores.refdata.client with a currency_pair_convention cache
#+description: New thin, DB-free NATS-only component (mirroring ores.marketdata.client's role) housing a currency_pair_convention_cache built on the nats-event-cache codegen facet, so every UI surface (Qt now, shell/Wt later) that needs FX pair conventions shares one cache instead of each hand-rolling its own.
#+type: task
#+level: s1
#+filetags: :crm_implementation:sprint_23:v0:
#+owner: marco
#+branch: feature/add-refdata-client-currency-pair-convention-cache
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-07-15
#+updated: 2026-07-15
#+environment: clever_dijkstra
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:B38B3869-02FD-4CC7-99BD-9A77904ACA19][Cross-rates matrix (CRM)]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The convention-aware CRM rate display formatter
([[id:4F2FC58A-D715-4159-A93F-E372AD720D0D][a separate task]]) needs
read access to =currency_pair_convention= rows
(pip_factor/tick_size/decimal_places) and =currency.base_precedence=
without a refdata round-trip on every CRM request/reload. The
project now has a generic caching primitive for exactly this shape
of problem -- =ores.eventing.core::service::cache::partitioned_cache=
plus the =nats-event-cache= codegen facet (see =party_cache= in
=ores.iam.core= for the worked exemplar: generated cache class +
registrar wiring, populated via a =read_*_for_cache= NATS request at
startup, refreshed on the entity's changed-event).

Where the cache should live is the interesting question this task
resolves: =party_cache= lives in =ores.iam.core=, the specific
*consumer* of party data for its own authorization logic -- not in
=ores.refdata= itself (refdata has direct DB access to its own data;
it has no need to cache itself via NATS). But unlike party's
single-consumer (IAM authorization) shape, FX pair conventions are a
cross-UI concern: the CRM matrix needs them in Qt today, and a shell
CRM view ([[id:F4D45153-D81E-464D-8D9B-2DBFEE7CB670][Add CRM support to ores.shell]], filed as a
sibling task) will need the same conventions tomorrow. Putting the
cache inside =ores.qt= would just relocate the duplication problem
the sibling formatter task's own "presentation lives in
=ores.marketdata.client=, not =ores.qt=" decision was designed to
avoid.

=client= is an existing, documented specialist sub-component role
(see =projects/modeling/component_architecture.org=: "provides the
consumer-facing API without coupling consumers to core internals",
currently only instantiated as =ores.iam.client=), so
=ores.refdata.client= is the regular, catalogue-consistent name for
this new piece -- not a bespoke ad hoc suffix. Add it, mirroring
=ores.marketdata.client='s existing shape (thin, DB-free, NATS-only),
and put
=currency_pair_convention_cache= there, generated via the
=nats-event-cache= facet exactly as =party_cache= was, so any UI
component (Qt now, shell/Wt later) links one shared cache instead of
each hand-rolling its own. See
[[id:723B0112-4DDC-4C03-924E-2C90C2F19B5C][Where should an
entity-mirror cache live?]] for the general placement rule this task
established.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:B38B3869-02FD-4CC7-99BD-9A77904ACA19][Cross-rates matrix (CRM)]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-15                               |

* Acceptance

- The =nats-event-cache= facet's currently-hardcoded
  =projects/ores.{cached_by}/core/include/ores.{cached_by}.core/...=
  output path is generalised to accept a configurable subcomponent
  segment (not a literal =core=), so =cached_by: refdata=,
  =cache_subcomponent: client= correctly targets
  =ores.refdata.client= (=projects/ores.refdata/client/include/ores.refdata.client/...=),
  not a nonsensical =ores.refdata.core=.
- New =ores.refdata.client= component: thin, DB-free, links only NATS
  + refdata's own API/protocol types, following
  =ores.marketdata.client='s existing shape (its CMakeLists, export
  header, and directory layout are the template).
- =currency_pair_convention_cache= (+ registrar wiring) generated via
  the =nats-event-cache= codegen facet, mirroring =party_cache=:
  populated via a =read_currency_pair_conventions_for_cache= NATS
  request at startup, refreshed on
  =currency_pair_convention_changed_event=, partitioned by tenant,
  keyed by =pair_code=.
- No refdata round-trip on every lookup -- reads are wait-free
  =partitioned_cache::get()= calls against the in-process snapshot.
- Real Catch2 coverage: cache load from a fake/mock NATS reply,
  lookup hit/miss, refresh-on-changed-event replacing only the
  affected tenant's partition.
- =ores.qt= (the CRM matrix's own module) links and instantiates this
  cache -- actual convention-aware *formatting* using it is the
  sibling formatter task's job, not this one; this task only needs to
  prove the cache loads, refreshes, and is queryable end-to-end.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
| [[id:B5632854-1CE5-4839-9D56-53A8CA0AD112][Verify currency_pair_convention_cache loads end-to-end from the CRM matrix]] | PASSED | All 4 steps PASSED; re-verified live post-auth-fix: 179 conventions loaded successfully, no error dialog |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Shipped in three parts:

1. *Generalised the =nats-event-cache= facet* past its hardcoded
   =.core= output path. =cached_by= now accepts the same
   ="component.subcomponent"= dotted form =component_include= already
   uses (e.g. =cached_by: refdata.client=) -- no new model-facing
   flag; =cache_component=/=cache_subcomponent= are purely derived by
   splitting =cached_by= on its optional dot, exactly like every other
   =_upper=/=_dir= variable in the same block. Verified byte-identical
   regeneration of =party_cache= (the one existing consumer) before
   and after.
2. *New =ores.refdata.client= component* -- thin, DB-free, the
   catalogue-consistent =client= specialist role (not a bespoke
   =.cache= suffix; corrected =component_architecture.org='s own
   =client= row to list =ores.marketdata= alongside =ores.iam= while
   here). Houses =currency_pair_convention_cache= (+ registrar
   wiring), generated end-to-end via =read_for_cache= +
   =cached_by: refdata.client= with zero hand-written protocol/handler
   code -- unlike =party=, which predates this and is still stuck on
   a hand-written =read_parties_for_cache= due to unrelated codegen
   drift.
3. *Wired and verified live* from =CrmCrossRatesMatrixMdiWindow=
   (construct + one-shot warm-up on open) -- surfaced two real,
   pre-existing gaps along the way rather than working around them:
   - =load()= returned =void= and only logged failures, invisible to
     any caller that isn't tailing a log. Changed to =[[nodiscard]]
     std::string= (empty on success); every existing fire-and-forget
     caller (generated registrar warm-up/refresh, IAM's hand-written
     auth/bootstrap handlers) now explicitly discards it with
     =(void)=.
   - The =nats-event-cache= cache_header template never included
     =ores.utility/rfl/reflectors.hpp=, needed for
     =rfl::json::read/write= on any =tenant_id=/=uuid=-bearing entity
     (every =read_for_cache= entity qualifies). Only ever worked for
     =party_cache= by accident, via a transitively-included handler
     header in every real consumer TU. Fixed at the template.
   - =ClientManager= had no bearer-token accessor for a UI-side
     consumer authenticating as a logged-in user (as opposed to
     =party_cache='s service-account =token_provider=). Added
     =currentAuthToken()=, snapshotted synchronously on the GUI thread
     at construction (not re-read live from a background-thread
     token_provider, which would race the proactive token-refresh
     timer -- safe here since =load()= only ever fires once).
   - A one-shot load failure routed through =errorOccurred=/
     =statusChanged= was found to be invisible in practice: this
     window's own 5s auto-refresh success message immediately
     overwrote it in the shared status-bar channel. Switched to
     =MessageBoxHelper::critical= (user-readable message + expandable,
     copyable low-level-error details section), matching the app's
     standard critical-dialog convention used elsewhere in this file.

Verified live end-to-end (post-fix): server-side JWT validation
succeeds, 179 currency pair conventions loaded and mapped, cache logs
"Loaded 179 currency_pair_conventions for tenant ..." -- no error
dialog. =ores.refdata.client.tests= (compile/instantiate smoke test;
=load()='s NATS round trip isn't unit-testable without a live service,
matching =party_cache='s own precedent) and every touched component
build clean.
