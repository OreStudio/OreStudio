:PROPERTIES:
:ID: E3647ADD-B5F6-4D12-82E4-57A93DA4C8F8
:END:
#+title: Task: Consolidate duplicated common/logging/database/nats parser boilerplate across domain services
#+description: The ~17 domain-service config/parser.cpp files (iam, refdata, dq, assets, marketdata, ore, reporting, scheduler, synthetic, telemetry, trading, variability, workflow, analytics, compute.service, workspace, and similar) are one-time codegen scaffolds from cpp_service_config_parser.cpp.mustache that were then hand-maintained -- they are ~95% identical, differing only in service name/namespace/app-specific options. Add a shared ores.service::config::standard_service_options helper that composes common_configuration + logging_configuration + database_configuration + nats_configuration into one options_description and does command-line + environment parsing + help/version handling in one call, returning the four already-read sub-options plus the raw variables_map for app-specific extras. Migrate the domain-service parsers to call it, and update cpp_service_config_parser.cpp.mustache so future scaffolds use it too. This also becomes the single call site for nats_configuration::register_shared_domain(), replacing the one-line-per-file calls added by the mapper fallback-tier task.
#+type: task
#+level: s1
#+filetags: :controller:process-supervisor:config:tech-debt:codegen:process-supervisor-config-passthrough-gap:sprint_25:v0:
#+owner: marco
#+branch:
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-08-04
#+updated: 2026-08-04
#+environment:
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:9E3EC9E2-742F-4830-839D-99EF2C2BC626][Collapse NATS config passthrough to a single environment-mapper fallback tier]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

New process-wide config knobs (and new services) get the shared parsing behaviour for free instead of requiring N near-identical hand edits; the codegen scaffold for new services stays small.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | BACKLOG                              |
| Parent story | [[id:9E3EC9E2-742F-4830-839D-99EF2C2BC626][Collapse NATS config passthrough to a single environment-mapper fallback tier]] |
| Now          | Not yet started.                       |
| Waiting on   | Nothing.                               |
| Next         | Begin implementation.                  |
| Last touched | 2026-08-04                               |

* Acceptance

- A shared standard_service_options builder+parse helper exists in ores.service; at least the domain-service parsers (excluding http.server/wt.service/shell/compute.wrapper/cli, which have enough genuine divergence to be evaluated separately) are migrated to use it and drop their duplicated composition/parsing code; nats_configuration::register_shared_domain() is called from exactly one place; cpp_service_config_parser.cpp.mustache is updated to scaffold against the new helper; all migrated services still parse CLI args and .env identically (existing behaviour preserved, verified by tests/manual smoke).

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result
