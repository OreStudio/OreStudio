:PROPERTIES:
:ID: 25167ED8-6CCC-43AC-9978-026E6AD581FC
:END:
#+title: How do I create Qt UI for a new entity?
#+description: Walk through client model, list window, detail dialog, and history dialog for a new entity in ores.qt.
#+type: recipe
#+level: cross
#+filetags: :recipe:qt:entity:ui:
#+created: 2026-05-21
#+updated: 2026-05-21

ClientModel layout, async fetch pattern, and file locations are in
[[id:2FDDC2F6-C510-4A86-8C50-D79C83EDDFFF][Qt entity patterns]]. The [[id:C6ADAC80-F4E6-44E8-9525-3760CB98C41D][codegen-add-qt-entity]] skill drives this process;
codegen (=--address ores.cpp.qt=) is the primary path.

* Question

How do I add Qt UI components for a new entity in =ores.qt=?

* Answer

*Codegen path (preferred)*:

#+begin_src sh
./compass.sh codegen entity generate <entity> --address ores.cpp.qt
#+end_src

Preview changes before writing:

#+begin_src sh
./compass.sh codegen entity generate <entity> --address ores.cpp.qt --diff
#+end_src

Then proceed to Phase 4 (controller wiring) below.

*Manual path (phases)*:

*Phase 1 — ClientModel + list window*
(PR: =[qt] Add {Entity} model and list window=):

1. Create =Client{Entity}Model.hpp= / =.cpp=: inherit
   =QAbstractTableModel=, define =Column= enum, implement async fetch
   using =QtConcurrent::run= + =QFutureWatcher=, add pagination.
2. Create ={Entity}ListWindow.hpp= / =.cpp=: toolbar, sort/filter proxy,
   table view; connect =dataLoaded= / =loadError= signals.
3. Build (via =compass build=, not raw =cmake --build=): =./compass.sh build ores.qt.lib=.

*Phase 2 — detail dialog* (PR: =[qt] Add {Entity} detail dialog=):

1. Create ={Entity}Dialog.hpp= / =.cpp= with one editor per field,
   async save, =accepted= / =rejected= signals.
2. Connect from list window: double-click / Edit button → open dialog.

*Phase 3 — history dialog* (PR: =[qt] Add {Entity} history dialog=):

1. Create ={Entity}HistoryDialog.hpp= / =.cpp= with
   =Client{Entity}HistoryModel= loading all versions by UUID.
2. Connect from list window: History toolbar button → open dialog.

*Phase 4 — controller and integration*
(PR: =[qt] Integrate {Entity} controller and update diagrams=):

1. Register the list window in =MainWindow.cpp= (menu + plugin slot).
2. Update class diagrams with [[id:D6D86317-4961-6754-A6DB-9C12B5FAC997][doc-add-class-diagram]].
3. Add recipe docs under [[proj:doc/recipes/qt][=doc/recipes/qt/=]].

* Tested by

Launch =ores.qt=, navigate to the entity list; verify async loading,
pagination, add/edit/delete via dialogs, history view, and recency
highlighting.

* See also

- [[id:2FDDC2F6-C510-4A86-8C50-D79C83EDDFFF][Qt entity patterns]] — ClientModel, dialog, history dialog patterns.
- [[id:C6ADAC80-F4E6-44E8-9525-3760CB98C41D][codegen-add-qt-entity]] — skill that drives this workflow.
- [[id:E81C7FEA-33E4-400A-839A-9D1618BED211][Qt plugin architecture]] — plugin lifecycle and menu registration.
- [[id:608675E1-4284-4902-99AC-4DA565390AFB][Entity lifecycle]] — layer ordering overview.
