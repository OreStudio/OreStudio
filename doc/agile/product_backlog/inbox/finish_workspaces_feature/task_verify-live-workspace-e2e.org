:PROPERTIES:
:ID: B0A57246-FFA4-4F7E-96D4-3C7B6474464B
:END:
#+title: Task: Verify the Live workspace end-to-end across workspace-aware entities
#+description: Run the final acceptance verification for the Workspaces: finish the feature story: confirm every workspace-aware entity behaves correctly under the Live workspace, and confirm the stand-alone workspace import flow still isolates imported data from Live.
#+type: task
#+level: s1
#+filetags: :finish_workspaces_feature:inbox:product_backlog:
#+owner: marco
#+branch:
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-08-25
#+updated: 2026-08-25
#+environment:
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:603F4845-B4A0-41EB-ACF9-B0FC1387E1C4][Workspaces: finish the feature]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

Live workspace confirmed correct end-to-end for every workspace-aware entity -- no silent fallback bugs, no entity missing the workspace_id column or query filter -- plus evidence that stand-alone ORE sample imports still land in an isolated workspace.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | BACKLOG                              |
| Parent story | [[id:603F4845-B4A0-41EB-ACF9-B0FC1387E1C4][Workspaces: finish the feature]] |
| Now          | Not yet started.                       |
| Waiting on   | Nothing.                               |
| Next         | Begin implementation.                  |
| Last touched | 2026-08-25                               |

* Acceptance

- A QA test scenario passes in the QA Validation Runner covering one representative entity per coverage mechanism under the Live workspace: a workspace-scoped-lookup refdata convention (e.g. cds_convention), an fk-scoped-child book or portfolio, a newly converted trading instrument (e.g. fx_forward), and a raw-flag trade.
- SQL sweep over every workspace-aware table: every table carries a populated workspace_id column and all provisioned Acme rows live under the Live sentinel UUID aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa; no row sits in an unexpected workspace.
- Stand-alone workspace import: creating a temporary workspace via the ORE sample import flow and importing a sample into it leaves Live data untouched -- the imported rows are visible only under the temporary workspace, and Live data does not leak into it.

* Plan

(Implementation strategy. Written when work starts; key decisions
are distilled into the parent story's =* Decisions= at close, but the
plan itself stays — it is the historical record of what we did.)

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
| [[id:FC10CB2F-9237-457F-BA54-C9DFE86FC1AE][Verify the Live workspace across workspace-aware entities]] | PENDING | One entity per coverage mechanism under Live, then temp-workspace import isolation. |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result
