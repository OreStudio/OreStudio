:PROPERTIES:
:ID: 01304B6F-C431-4BD3-885A-CE713114B5A4
:END:
#+title: Task: Migrate Portfolio/Book to composite-entity versioning
#+description: Rescoped after design review: book_status/regulatory_book_type are enum-like FK attributes (many books reference one lookup row), not composite children -- applying :bump_parent_version: there would be wrong (editing a status definition would bump every book referencing it). The real 1-to-many composite relationship is Portfolio (parent) -> Book (child, via parent_portfolio_id): a portfolio genuinely has many books, matching the proven party/party_identifier shape. Add :bump_parent_version: true and :list_by_as_of: true to book.org's parent_portfolio_id FK entry, and :generate_touch_function: true to portfolio.org's Flags, per the Temporal Composite Entity Versioning target-state doc (id:BA159E88-74EB-4401-91CC-1DE7DF5AE50A) and the counterparty/counterparty_identifier pilot.
#+type: task
#+level: s1
#+filetags: :book_data_model_cleanup:sprint_23:v0:
#+owner: marco
#+branch: feature/migrate-book-to-composite-entity-versioning
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-07-11
#+updated: 2026-07-11
#+environment: bright_faraday
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:F0DAFC8D-3C94-4F97-B546-D7AF7469A29E][Book codegen drift remediation]] story. It captures the goal, current status, acceptance, and any notes or results. Runs after book, book_status, and regulatory_book_type are regenerated and end-to-end tested against current templates.

* Goal

Register Book as a composite child of Portfolio (not of book_status/
regulatory_book_type) so that adding/editing/deleting a book bumps its
parent portfolio's version, and "portfolio as of version N" can
correctly compose the books it contained at that point in time.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                                  |
| Parent story | [[id:F0DAFC8D-3C94-4F97-B546-D7AF7469A29E][Book codegen drift remediation]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-07-12                               |

* Acceptance

-

* Plan

Added =:bump_parent_version: true= and =:list_by_as_of: true= to
book.org's =parent_portfolio_id= FK entry, and
=:generate_touch_function: true= to portfolio.org's Flags (the same
shape as the counterparty/counterparty_identifier pilot).
Diff-reviewed before generating: portfolio's regen was restricted to
=--address ores.sql.schema= only (its Qt layer is still fully stale
and out of scope -- a full regen would have pulled in a 3700+ line
unrelated diff). book's full regen only touched
C++/SQL (handler/registrar/protocol/repository/service/SQL) -- its Qt
files came back byte-identical (already current from the earlier
regen task).

Built clean, =db recreate= applied both modified SQL files with no
errors. Verified the mechanism directly via SQL, not just trusting the
generated code:

- Created a portfolio (v1). Inserted a book referencing it -- portfolio
  correctly bumped to v2, v1's window closed at the exact insert
  instant.
- Deleted the book -- portfolio correctly bumped to v3, symmetric with
  the insert-side bump.
- Composed "portfolio as of v2" via the interval-overlap join
  (=book.valid_from < v2.valid_to and book.valid_to > v2.valid_from=)
  -- correctly returned exactly the book that existed during that
  window.

=ores.refdata.api/core/service.tests= 3/3 passed after the change.

** Recursive (multi-level) bump

Manual QA correctly identified a real gap: Portfolio has its own
self-referencing =parent_portfolio_id= (a portfolio tree), and the
initial implementation only bumped the *immediate* parent -- a
grandparent portfolio would never see a grandchild book's change,
exactly the multi-level composition case the target-state doc flagged
as a deferred follow-up ("out of scope for the first cut... a natural
extension").

Fixed by declaring Portfolio as composite parent *and* child of
itself: added a =* Foreign keys= section to portfolio.org for its own
=parent_portfolio_id= (previously undeclared -- a pre-existing gap,
this column had *no* existence validation at all) with
=:bump_parent_version: true=. Since every portfolio write already
re-fires Portfolio's own insert trigger (including the touch
function's re-insert when a *child* bumps it), that trigger now also
recursively bumps *its own* parent. A root portfolio
(=parent_portfolio_id IS NULL=) safely terminates the recursion: the
touch function's row lookup on a NULL id matches nothing and no-ops.

This surfaced a genuine template bug, not just a missing model flag:
=CREATE RULE= (the soft-delete rule) is parsed eagerly by PostgreSQL,
unlike a PL/pgSQL function body -- so a self-referencing entity's
delete rule, which calls its own touch function, failed with
"function ... does not exist" because the touch function was
generated *after* the trigger/rule in the same file (fine for
cross-entity parent/child, since the child's file always runs after
the *entire* parent file, but broken for self-reference). Fixed the
shared template (both the tangled =.mustache= and its literate =.org=
source, re-tangled via =compass build --direct
tangle_codegen_templates=) to generate the touch function *before* the
insert trigger/delete rule, not after. Confirmed safe for the existing
party/counterparty pilot too (diffed both -- content unchanged, only
relocated).

Verified via SQL with a 3-level tree (grandparent -> parent -> book):
adding a book to =parent= correctly bumped =parent= (v1->v2) *and*
=grandparent= (v2->v3) in the same transaction.

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
| [[id:1DCA1F0C-C993-4EE4-9097-6ABAF07D041F][Verify Portfolio/Book composite version bump and history]] | PASSED | UI initially showed a FAIL on the delete-side bump step, but direct SQL cross-check against the exact same real portfolio/tenant confirmed the backend mechanism fired correctly every time (add->v6, delete->v7, matching the earlier insert->edit sequence). Not a backend defect -- Portfolio's own Qt layer is unmigrated/out of scope for this task, so it not reacting to NATS events from a sibling entity's write is expected, not a regression (captured separately as the Portfolio window live-refresh gap). Also surfaced a real gap (see Plan): bumps don't propagate recursively up a nested portfolio tree -- fixed. |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Book registered as composite child of Portfolio (=:bump_parent_version:
true=, =:list_by_as_of: true= on book.org's =parent_portfolio_id= FK;
=:generate_touch_function: true= on portfolio.org). Recursive
multi-level bump implemented by declaring Portfolio composite parent
*and* child of itself, uncovering and fixing a real codegen-template
bug (touch-function generation order vs. eager =CREATE RULE=
resolution) along the way -- confirmed safe for the existing
party/counterparty pilot. Mechanism verified directly via SQL (not
just generated code) at 2 and 3 levels deep, and =ores.refdata.api/core/
service.tests= pass 3/3.

Manual QA scenario PASSED. Its one reported FAIL (delete-side bump not
visible in the Portfolio History dialog) was run down to ground: a
direct SQL reproduction against the exact same real portfolio/tenant
showed the bump firing correctly (v5->v6 on insert, v6->v7 on delete),
proving the backend is correct and the UI symptom is the already-known
Portfolio-window live-refresh gap (Portfolio's Qt layer doesn't react
to NATS events from a sibling entity's write -- out of scope here,
captured separately).
