:PROPERTIES:
:ID: F58CA9E7-C65F-4D17-A9A7-95FDE25B9EBD
:END:
#+title: Execution Model
#+description: How LLMs and agents operate the information architecture during a working session.
#+type: meta
#+version: 2
#+level: cross
#+filetags: :meta:execution:llm:agents:
#+created: 2026-05-18
#+updated: 2026-05-21

This page describes /how/ the [[id:926B916E-A57E-498E-99AB-720B583C0362][cybernetic information architecture]] is
actually operated in a session — who reads what, who writes what, how
LLMs fork subprocesses, how blocking and task-switching work. It is
the runtime view of the architecture; the static contract lives in
[[id:8B29A8D8-E004-4E87-B81D-ABEAF68F2B98][document types]] and the role definitions in [[id:1A5E0079-5771-4213-B624-62FA9AC947B4][functions]]. Read this when
you want to know what concretely happens when a sprint is in flight.

* Roles

The actors involved in a working session. Each role maps onto a
cybernetic system — load the linked [[id:1A5E0079-5771-4213-B624-62FA9AC947B4][function]] doc for the priming
context that role uses.

#+ATTR_HTML: :class hug-leading
| Role             | Maps to                                                    | What it does                                                                                                                            |
|------------------+------------------------------------------------------------+-----------------------------------------------------------------------------------------------------------------------------------------|
| User             | —                                                          | Assigns stories to orchestrators; invokes [[id:8F13BA75-EDAA-46CC-9BE2-E356D60BF4F4][System 3]] / [[id:6CA8B181-BD81-4EF4-AF27-64F5491D62F0][System 4]] / [[id:6DF00A64-7CC3-40A7-950B-55D2B05A31C7][System 5]] sessions on demand; reviews story-level outcomes. |
| Agent            | [[id:DBFD530B-4B56-48D1-9F03-3A0D0DA16552][System 1]] ([[id:B544713A-BA23-4D5A-9CEE-084212D7C8BA][S1 Agent]] function)                          | A Claude subagent that picks up a single task, implements it, returns when the task is =DONE= or =BLOCKED=.                            |
| Orchestrator LLM | [[id:BF4F183F-BB33-44F2-A53F-D9BD602E9BE8][System 2]] ([[id:EECA6981-3240-44D9-ADA7-4B8C64A76B44][S2 Orchestrator]] function)                  | Coordinates one assigned story. Forks agents, reviews their output, triages discovered work.                                            |
| System 3 session | [[id:8F13BA75-EDAA-46CC-9BE2-E356D60BF4F4][System 3]] ([[id:F1D99662-C6B1-4839-92E1-D061CCF6B83F][S3 Sprint Planner]] function)                | LLM session invoked on demand that reasons about sprint coherence — opening, running, and closing a sprint.                             |
| System 4 session | [[id:6CA8B181-BD81-4EF4-AF27-64F5491D62F0][System 4]] ([[id:66EB6824-52B8-4603-B302-F805DA9EC443][S4 Version Planner]] function)               | LLM session invoked on demand that reasons about version coherence — sprint sequencing, release scope.                                  |
| System 5 session | [[id:6DF00A64-7CC3-40A7-950B-55D2B05A31C7][System 5]] ([[id:5CAD21DA-21FB-460B-9286-8998B4C6EF52][S5 Identity Steward]] function)              | LLM session, usually with the user collaborating, that reasons about identity — what is in or out of scope.                             |
| Audit jobs       | [[id:ED29C99B-1507-49C9-9937-D296FFFE428E][System 3*]] ([[id:049B086E-F131-4D7A-8244-C1B2FF297AB6][S3* Auditor]] function)                     | Scripts run periodically (e.g. nightly) plus on-demand sessions; enforce invariants from outside the operational hierarchy.             |

* Story execution loop (the [[id:BF4F183F-BB33-44F2-A53F-D9BD602E9BE8][System 2]] orchestrator)

1. User assigns a story. Orchestrator reads the story document, the
   sprint mission, and any linked knowledge. It moves the story's
   Status TODO from =BACKLOG= to =STARTED=.
2. Orchestrator breaks the story into tasks if not already broken.
   Each task is created as =BACKLOG= under the story folder.
3. Orchestrator picks the next task. Sets the task's TODO to
   =STARTED=, fills =#+owner=, =#+branch=, =#+blocked_on: none=,
   updates the =** Now= / =** Next= / =** Last touched= subheadings.
   Spawns an agent with the task as input.
4. Orchestrator monitors the agent. When the agent reports done:
   1. Reads the agent's task notes and the produced PR.
   2. Reviews the change against the task's =* Acceptance= criteria.
   3. If satisfied: moves the task TODO to =DONE=, fills =* Result=.
   4. If not: TODO stays at =STARTED= (or returns there from =BLOCKED=)
      with review feedback in =* Notes=; or the task is split into
      smaller tasks.
5. Orchestrator processes any tasks the agent filed as =DISCOVERED=:
   triages each one (see [[id:8810E0F9-C5B9-4CFA-A710-774AD6C771DD][lifecycle]]).
6. Loops to step 3 until every task in the story is =DONE= or
   =ABANDONED=.
7. Orchestrator distils the plan(s) into the story's =* Decisions=,
   archives or deletes the plan files, and moves the story TODO to
   =DONE=.

* Concurrency rules

- One orchestrator per story at a time.
- An orchestrator may run multiple agents in parallel /only if/ their
  tasks touch disjoint files. The orchestrator is responsible for
  proving disjointness before spawning.
- Discovered work is /never/ implemented opportunistically by an
  agent. It is filed as =DISCOVERED= for orchestrator triage.

* Blocking and task switching

Wall-clock waste comes from blocking, not active work: PR review, CI
runs, local builds. The orchestrator should keep multiple of its
story's tasks in flight so that when one blocks, another can advance.

** When an orchestrator blocks on a task

1. Move the task TODO from =STARTED= to =BLOCKED=.
2. Set =#+blocked_on= to the cause (=review=, =ci=, =local_build=,
   =external=). Set =#+blocked_since= to now. Update =** Waiting on=
   in the Status subheadings, leave a one-sentence =** Now= describing
   what was just done, set =** Next= to the action that will resume
   once unblocked.
3. Update =#+updated= and =** Last touched=.
4. Move attention to another =STARTED=-eligible task in the same
   story. If none exist, pick the next =BACKLOG= task and start it.
5. If every task in the story is =BLOCKED=, report to the user and
   stop.

** When an orchestrator returns to a blocked task

1. Check whether the blocker has cleared (review posted, CI green,
   build passed).
2. If cleared: move TODO back to =STARTED=, clear =#+blocked_on=
   (=none=) and =#+blocked_since=, update Status, resume the planned
   =** Next= action.
3. If not cleared: refresh =** Last touched=, leave the task, switch
   away.

** The active-branches view

Audit ([[id:ED29C99B-1507-49C9-9937-D296FFFE428E][System 3*]]) generates =audit/reports/active_branches.org= by
grepping every task document for =^\* STARTED Status= and
=^\* BLOCKED Status=, then reading the task's frontmatter. The output
is a table: =task | owner | branch | pr | todo | blocked_on |
blocked_since=. This is what a user opens to see what is in flight and
where the time is going.

* The task document as live status

Opening a single task document must answer four questions: /what is
this task, what is the LLM doing right now, what is it waiting for,
what is next/. The =* Status= headline carries the TODO state and its
subheadings carry the operational detail. See =task= in
[[id:8B29A8D8-E004-4E87-B81D-ABEAF68F2B98][document types]] for the structure.

The implication for the orchestrator and agent: /every meaningful
action updates Status and =#+updated= before control yields/. This is
a hard discipline. Tasks that fall out of sync with reality are how
the v0 system rotted; audit enforces freshness here so it cannot
recur.

* When [[id:8F13BA75-EDAA-46CC-9BE2-E356D60BF4F4][System 3]], [[id:6CA8B181-BD81-4EF4-AF27-64F5491D62F0][4]], [[id:6DF00A64-7CC3-40A7-950B-55D2B05A31C7][5]] run

On demand, in their own sessions:
- /[[id:8F13BA75-EDAA-46CC-9BE2-E356D60BF4F4][System 3]]/ — when planning or closing a sprint, or when the user
  senses the sprint is drifting from its mission.
- /[[id:6CA8B181-BD81-4EF4-AF27-64F5491D62F0][System 4]]/ — when planning a version, sequencing sprints, or
  considering release scope.
- /[[id:6DF00A64-7CC3-40A7-950B-55D2B05A31C7][System 5]]/ — when a far story arrives, when the product identity
  needs refinement, or when a candidate version is being shaped.

These sessions read and write the relevant documents but do not spawn
agents. They produce structural changes (mission rewrites, story
re-prioritisation, scope decisions) that subsequent [[id:BF4F183F-BB33-44F2-A53F-D9BD602E9BE8][System 2]] sessions
inherit.

* What this model does not yet have

- Automated orchestrator dispatch — the user manually starts
  orchestrators today.
- A queue or lock — we rely on the user to avoid assigning conflicting
  stories to parallel orchestrators.
- Multi-user coordination — single-user assumption.
