:PROPERTIES:
:ID: 3F3559A2-5E98-44EA-A87A-E5DA7CF5BA23
:END:
#+title: Task: Update history field mapper codegen template to use provenance_fields constants
#+description: cpp_history_field_mapper.cpp.mustache hardcodes the five provenance labels ("Modified By", "Recorded At", etc.) as string literals, repeated in every generated mapper, while the shared ores::history::domain::provenance_fields constants struct is the single source of truth the Qt history dialog renders from. Emit provenance_fields::* from the template's audit blocks with the include, then regenerate every entity's mapper so all 122 files are template-consistent.
#+type: task
#+level: s1
#+filetags: :fix-codegen-template-drift:sprint_25:v0:
#+owner: marco
#+branch: feature/history-field-mapper-template-provenance-fields
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-08-06
#+updated: 2026-09-04
#+environment: brave_hopper
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:F5532C5C-7A8D-461D-8E0A-CACB602AB3DF][Fix codegen template drift]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

The =*.history_field_mapper.cpp= template (the =ores.codegen.entity=
template that produces every entity's =render_{entity}_fields()=
history/diff mapper, tangled from
=ores.cpp.presentation.history_field_mapper_impl.org=) hardcodes the
five provenance field labels as string literals in its audit blocks:
="Modified By"=, ="Performed By"=, ="Change Reason Code"=, ="Change
Commentary"=, and ="Recorded At"=. The shared
=ores::history::domain::provenance_fields= constants struct
(=ores.history.api/domain/provenance_fields.hpp=) is the single
source of truth for exactly these five labels, and it is what the Qt
history dialog already renders from. At capture (2026-07-16),
=currency='s mapper on =main= used the constants, but a later codegen
regeneration (198e1222c1) reverted it to the template's literal
output. That revert is the proof of where the drift lives: the
template emits literals, so any regeneration of any entity
reproduces literals, and no generated mapper can converge on the
constants until the template changes. Update the template's audit
blocks (both the =has_audit_group= shape rendering =v.audit.*= and
the flat legacy shape rendering =v.*=) to emit
=provenance_fields::*= for all five labels, with the matching
=#include "ores.history.api/domain/provenance_fields.hpp"= and a
function-scope =using= declaration, then regenerate every entity's
=*_history_field_mapper.cpp= so all 122 files across the 8
components (refdata 64, trading 18, dq 14, synthetic 9, marketdata 5,
analytics 4, iam 4, reporting 4) are template-consistent again.

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | DONE                              |
| Parent story | [[id:F5532C5C-7A8D-461D-8E0A-CACB602AB3DF][Fix codegen template drift]] |
| Now          | Nothing. |
| Waiting on   | Nothing.                               |
| Next         | Nothing. |
| Last touched | 2026-09-04                               |

* Acceptance

- =ores.cpp.presentation.history_field_mapper_impl.org='s src block
  emits all five labels as =provenance_fields::*= constants in both
  audit shapes (=has_audit_group= nested =v.audit.*= and flat =v.*=);
  no label literal remains in its audit blocks.
- The block emits the =#include
  "ores.history.api/domain/provenance_fields.hpp"= and a
  function-scope =using ores::history::domain::provenance_fields;=.
- Re-tangling (=compass build --direct tangle_codegen_templates=)
  changes only =cpp_history_field_mapper.cpp.mustache=, and
  regenerating the 8 components is idempotent: a second regeneration
  is byte-identical to the first.
- Every regenerated =*_history_field_mapper.cpp= renders its audit
  labels from the constants, and the combined diff is confined to the
  audit-block lines and the include.
- All 8 components' core builds compile the regenerated mappers; the
  =ores.history= link is added to a component core CMake where the
  build proves the include path is missing.

* Plan

- 2026-09-04: task started; surveyed the blast radius. 122 generated
  =*_history_field_mapper.cpp= across 8 components (refdata 64,
  trading 18, dq 14, synthetic 9, marketdata 5, analytics 4, iam 4,
  reporting 4) all render the literal labels today, =currency=
  included: its constants usage was reverted by regen 198e1222c1, so
  the capture's premise ("currency is the one entity matching the
  desired end state") is corrected in the Goal above. The label
  literals live in exactly one place, the audit blocks of the
  =cpp_history_field_mapper.cpp.mustache= src block inside
  =ores.cpp.presentation.history_field_mapper_impl.org=, in two
  shapes: nested =v.audit.*= for =has_audit_group= entities and flat
  =v.*= for the rest. The =Recorded At= line renders in both shapes
  and outside the =has_audit_columns= guard, so the include and
  =using= are emitted unconditionally.
- The fix mirrors currency's captured constants state (f58ab8078f):
  all five labels as =provenance_fields::*=, one function-scope
  =using=, include after the entity hpp. Five components (iam,
  marketdata, reporting, synthetic, trading) do not link =ores.history=
  in their core CMake today; the link is added where the build proves
  the include path missing.
- Baseline regen before any edit (codegen-fix-drift step 1), org
  edit, re-tangle, per-component regen, idempotency check, then the
  union build of all affected cores, then close out.

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
|          |       |       |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result

Delivered 2026-09-04 on branch
=feature/history-field-mapper-template-provenance-fields=, commits
373ef857ea (org source fix + regeneration) and cfcfafd123 (cmake
links).

- The org source
  (=ores.cpp.presentation.history_field_mapper_impl.org=) emits all
  five labels as =provenance_fields::*= constants in both audit
  shapes, the nested =v.audit.*= (=has_audit_group=) and the flat
  =v.*= legacy shape; no label literal remains in its audit blocks.
  The =#include "ores.history.api/domain/provenance_fields.hpp"= and
  the function-scope =using ores::history::domain::provenance_fields;=
  are unconditional: =Recorded At= renders outside the
  =has_audit_columns= guard in both shapes, so neither shape could
  carry the include alone.
- Re-tangling (=compass build --direct tangle_codegen_templates=)
  changed only =cpp_history_field_mapper.cpp.mustache=. Regenerating
  the 8 components is idempotent: the second regeneration is
  byte-identical to the first.
- 120 of the 122 tracked =*_history_field_mapper.cpp= regenerated in
  373ef857ea, each diff confined to the audit-block lines and the
  include. The two trading mappers (=party_role_type=,
  =trade_type=) did not regenerate: full-component regeneration leaves
  them byte-identical with the literal labels, so the template fix
  cannot converge them. The drift survey records them as the
  trading-cpp stale pair, owned by the trading per-component drift
  story and excluded from this PR per program scope.
- Compile gate: all 8 core libraries build the regenerated mappers.
  The unconditional include needs =ores.history= on the include path;
  the five cores that did not link it (iam, marketdata, reporting,
  synthetic, trading) failed the first build and received
  =ores.history.core.lib= in their CMakeLists (cfcfafd123), matching
  the refdata/dq/analytics idiom.
- Verification (change class: code + ci + docs): site build clean;
  full system build clean (linux-clang-debug-make); ctest 71/71
  passed against a recreated database — the CI-equivalent state,
  because the repository write tests are not repeatable against a
  persistent database (the first clean-slate suite run passed
  =ores.iam.core.tests=, which had failed a prior run on duplicate
  keys left by an interrupted earlier execution); tangle roundtrip
  clean; component drift checks (refdata, reporting, marketdata)
  clean except the three recorded pre-existing files; ore domain
  roundtrip check red before and after (pre-existing, untouched by
  this diff).

Acceptance: all five bullets met.

* Promoted from capture

Captured 2026-07-16 in the product backlog; promoted preserving the UUID.

** What

=cpp_history_field_mapper.cpp.mustache= (the =ores.codegen.entity=
template that produces every entity's =render_{entity}_fields()=
history/diff mapper) hardcodes the four audit-column field labels as
string literals inside its =has_audit_columns= block: ="Modified
By"=, ="Performed By"=, ="Change Reason Code"=, ="Change Commentary"=.
On =main=, =projects/ores.refdata/core/src/presentation/currency_history_field_mapper.cpp=
is currently committed in a state the template cannot produce: it uses
=ores::history::domain::provenance_fields::modified_by= etc. (a
shared constants struct in =ores.history.api/domain/provenance_fields.hpp=)
instead of the literals, with the matching =#include=. Every other
entity's generated mapper (=country=, and everything else) still has
the hardcoded literals, since regenerating any of them via =compass
codegen entity generate <entity>= reproduces the literal-string
version, not the constants version. Update the template's
=has_audit_columns= block to emit =provenance_fields::modified_by=
etc. plus the necessary =#include "ores.history.api/domain/provenance_fields.hpp"=,
then regenerate every entity's =*_history_field_mapper.cpp= so they
are all consistent with the (now-corrected) template.

** Why

Discovered while rebasing
[[id:30563983-1E0E-44D2-8D9D-9BBE7086BF68][the currency-calendar FK
task]]'s PR onto =main=: the rebase hit a real merge conflict in
=currency_history_field_mapper.cpp= because =main='s committed content
had drifted from what the template currently produces. Using shared
=provenance_fields= constants rather than repeating the same four
label strings across every entity's generated mapper is the direction
we want — it is a single source of truth for these labels and avoids
typo-drift between entities (e.g. "Change Reason Code" spelled two
different ways somewhere down the line). The template needs to catch
up to how =currency='s mapper already looks on =main=, and every other
entity's mapper needs to be regenerated to match once the template is
fixed, so the whole codebase converges on one pattern instead of one
entity being silently exempt from regeneration.

** References

- =projects/ores.codegen/library/templates/cpp_history_field_mapper.cpp.mustache= — the template to update.
- =projects/ores.history.api/include/ores.history.api/domain/provenance_fields.hpp= — the shared constants struct.
- =projects/ores.refdata/core/src/presentation/currency_history_field_mapper.cpp= — the one entity currently (accidentally) matching the desired end state.

** See also

- [[id:30563983-1E0E-44D2-8D9D-9BBE7086BF68][Migrate currency.holiday_calendar to a real FK relationship]] — task where the drift was discovered.
