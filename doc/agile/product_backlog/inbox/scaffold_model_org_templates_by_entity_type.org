:PROPERTIES:
:ID: EE237306-7782-4625-8880-47E8975530EA
:END:
#+title: Scaffold model .org files per entity type (regular/enum/junction)
#+description: Generate model .org skeletons from mustache templates keyed by entity type (regular entity, enum entity, junction), pre-populated with the correct variability knobs already enabled.
#+type: capture
#+level: cross
#+filetags: :codegen:modeling:refdata:
#+created: 2026-07-15
#+updated: 2026-07-15

This page is a [[id:671F18E4-E09C-4B3B-BD24-D33DF8AE38A6][capture]] in the *inbox* bucket of the product backlog — a pre-sprint idea, not yet pulled into a sprint as a story.

* What

Today every entity's model =.org= file (e.g.
=projects/ores.refdata/modeling/ores.refdata.<entity>.org=) is
hand-authored from scratch or copy-pasted from a similar entity, so
variability knobs (=has_change_reason_cache=,
=history_response_data_field=, =delete_request_id_field=,
=has_uuid_primary_key=, badge wiring for enum-like lookups, junction
wiring for many-to-many association entities, etc.) get set
inconsistently or omitted entirely depending on which entity someone
copied from. In practice there are really only a handful of *entity
kinds* driving these knobs: a plain domain entity, an enum/lookup
entity (small closed set of codes, usually badge-rendered in list
views), and a junction/association entity (links two entities,
often read-only or list-only). Add a mustache template per kind
under =projects/ores.codegen/library/templates/= that scaffolds a new
model =.org= with the correct knobs for that kind already present and
set to sensible defaults, instead of leaving every author to
rediscover them by trial and error (or, worse, never discover the gap
until a runtime bug surfaces).

* Why

This session (task [[id:AA97E310-F293-46AD-9BE7-C13AD376346C][AA97E310]], PRs #1571/#1590) found exactly this class of
bug repeatedly: 8 =*_convention= entities were missing
=has_change_reason_cache: true= despite their schema requiring
=change_reason_code= (saves silently failed with an empty
change-reason dropdown), and the same 8 were missing
=history_response_data_field=/=delete_request_id_field= overrides
because their protocol field names don't follow the codegen
defaults. All three gaps trace back to the model =.org= file being
authored without a checklist for "what does an entity of this kind
need". A kind-aware scaffold would make these knobs impossible to
silently omit — the generator would ask for/default them at model-
creation time instead of relying on someone noticing a save failure
or a QA scenario catching it months later.

* References

- [[id:AA97E310-F293-46AD-9BE7-C13AD376346C][Task: Delete per-entity history-dialog codegen archetypes and reconcile dynamic_combo drift]] — where the has_change_reason_cache/history_response_data_field/delete_request_id_field gaps were found and fixed for 8 entities, with the same class of bug suspected to exist more broadly.
- =projects/ores.codegen/library/templates/= — where the new per-kind mustache template(s) would live, alongside the existing facet archetypes.

* See also

- [[id:DBDFF0B1-E358-4300-B361-644A89FBD169][Story: Migrate all entities onto the generic HistoryDialog]]
