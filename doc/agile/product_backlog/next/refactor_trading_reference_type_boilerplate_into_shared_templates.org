:PROPERTIES:
:ID: 63DD4AD6-4F3B-4DCF-8C66-9CB399E970FF
:END:
#+title: Refactor trading reference type boilerplate into shared templates
#+description: Refactor trading reference type boilerplate into shared templates
#+type: capture
#+level: cross
#+filetags: :code:capture:next:
#+created: 2026-05-21
#+updated: 2026-05-21

This page is a [[id:671F18E4-E09C-4B3B-BD24-D33DF8AE38A6][capture]] in the *next* bucket of the product backlog — a pre-sprint idea, not yet pulled into a sprint as a story.

The five new trading instrument reference data types (DayCountFractionType,
BusinessDayConventionType, FloatingIndexType, PaymentFrequencyType, LegType)
introduced in Phase 0 of the CDM instrument domain model follow the established
per-type file convention used throughout the codebase. However, because all five
types share the same structure (code + description + provenance), there is an
opportunity to reduce boilerplate in the repository layer without changing the
established pattern elsewhere.

Acceptance criteria:

- Repository entity structs: Replace the five identical =*_entity= structs in
  =ores.trading.core= with a single =trading_ref_type_entity= base struct, and
  type-alias or inherit per type.

The two CLI acceptance items this capture once carried died with
=ores.cli= on 2026-09-23, so they were removed here.

Scope: =ores.trading.core= only. The Qt UI layer (Controllers,
MdiWindows, DetailDialogs, HistoryDialogs, ClientModels) follows the same
per-type pattern as all other entity types in =ores.qt= and is out of scope for
this story.
