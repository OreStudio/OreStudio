:PROPERTIES:
:ID: 2AF9873F-5285-4FE8-BB44-122FEFA8E424
:END:
#+title: Replace parent combo boxes with server-side type-ahead search
#+description: Replace parent combo boxes with server-side type-ahead search
#+type: capture
#+level: cross
#+filetags: :code:capture:next:
#+created: 2026-05-21
#+updated: 2026-05-21

This page is a [[id:671F18E4-E09C-4B3B-BD24-D33DF8AE38A6][capture]] in the *next* bucket of the product backlog — a pre-sprint idea, not yet pulled into a sprint as a story.

The entity detail dialogs (party, counterparty) load all entities into a combo
box for parent selection using a single request with =limit=1000= (via
=LookupFetcher::fetch_parties=/=fetch_counterparties=). If the system has more
than 1,000 entities, the dropdown silently truncates results, meaning users
cannot select the correct parent and the hierarchy tree is incomplete. The
same pattern exists for country image maps and lookup fetchers.

The fix is to replace the plain =QComboBox= with a searchable type-ahead widget
backed by =QCompleter= that issues server-side search requests as the user
types. This is the scalable solution and avoids loading the full dataset into
memory.

Affected locations (updated 2026-07-18 -- the codebase has since moved off the
hand-written =EntityDetailDialog=/=PartyDetailOperations=/
=CounterpartyDetailOperations= pattern these originally named onto
codegen-generated dialogs; the underlying concern is unchanged, just relocated):

- =projects/ores.qt/api/src/LookupFetcher.cpp:373= - =fetch_parties()=
- =projects/ores.qt/api/src/LookupFetcher.cpp:386= - =fetch_counterparties()=
- =projects/ores.qt/refdata/src/PartyDetailDialog.cpp:264= - =populateParentPartyCombo()=
- =projects/ores.qt/refdata/src/CounterpartyDetailDialog.cpp:269= - =populateParentCounterpartyCombo()=
- =LookupFetcher.cpp= - country and lookup reference data loading

Note: the =MdiWindow= list views already have proper server-side pagination
via =ClientCounterpartyModel= / =ClientPartyModel=; this issue only affects
detail dialog combo boxes and related data loading.

- Introduce a reusable =SearchableEntityComboBox= widget with =QCompleter=
- Add server-side search endpoints for parties and counterparties
- Replace the =LookupFetcher= all-in-one fetches with on-demand search
- Consider the same approach for country and lookup fetcher if volumes warrant it

See also: [[id:43C835A9-8F80-428F-9690-7021B46CADEA][Replace parent entity combo box with server-side searchable widget]]
-- likely a duplicate of this capture, filed six days apart; worth merging
during backlog refinement.
