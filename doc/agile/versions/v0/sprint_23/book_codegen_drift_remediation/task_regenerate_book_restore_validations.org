:PROPERTIES:
:ID: 0C185CE4-63E5-44CC-94AF-F3FF1E194986
:END:
#+title: Task: Regenerate book and restore insert-trigger validations
#+description: Regenerate book (currently almost entirely stale). Diff the resulting SQL against ores_refdata_books_create.sql on disk specifically for the 5 insert-trigger validations from PR #1449 (soft FKs on parent_portfolio_id/party_id/owner_unit_id; value checks on ledger_ccy/book_status) plus any others -- confirm book.org's Foreign keys/Insert trigger sections still produce them. Fix via paste blocks or variability knobs in the .org model if anything is missing, not hand-patched SQL.
#+type: task
#+level: s1
#+filetags: :book_data_model_cleanup:sprint_23:v0:book_codegen_drift_remediation:
#+owner: marco
#+branch: feature/regenerate-book-restore-validations
#+pr:
#+blocked_on:
#+blocked_since:
#+created: 2026-07-11
#+updated: 2026-07-11
#+environment:
#+todo: DISCOVERED BACKLOG STARTED BLOCKED | DONE ABANDONED

This page documents a [[id:31C868B9-306E-4282-BA8C-07E647021B34][task]] in the [[id:F0DAFC8D-3C94-4F97-B546-D7AF7469A29E][Book codegen drift remediation]] story. It captures the goal, current status, acceptance, and any notes or results.

* Goal

(Describe what user-visible-or-internal change this task produces.)

* Status

| Field        | Value                                  |
|--------------+----------------------------------------|
| State        | STARTED                              |
| Parent story | [[id:F0DAFC8D-3C94-4F97-B546-D7AF7469A29E][Book codegen drift remediation]] |
| Now          | Regenerated, built, DB-verified (validations confirmed via direct SQL insert); awaiting manual QA. |
| Waiting on   | Nothing.                               |
| Next         | Run the manual scenario, then close out. |
| Last touched | 2026-07-12                               |

* Acceptance

-

* Plan

Confirmed book.org's =* Foreign keys=/=* Insert trigger= sections
(added in PR #1449 after that regen dropped them) were still present
before touching anything: 3 soft FKs (parent_portfolio_id, party_id,
owner_unit_id) + 3 value checks (ledger_ccy, book_status,
regulatory_book_type — the third added since PR #1449). Diff-reviewed
=compass codegen entity generate book --diff= next: the insert-trigger
validation block didn't appear in the diff at all (confirming it would
be untouched), only the same safe shape of changes seen in
book_status/regulatory_book_type (real bug fix in ClientBookModel.cpp
missing =result->success=, =clock_timestamp()= trigger fix, composite
=read_at_version=, plus book-specific badge/icon polish in
BookMdiWindow.cpp and an =idEdit->setReadOnly()= fix in
BookDetailDialog.cpp).

Ran the real generation (all 40 files ✅ in =compass codegen entity
show book=). Re-grepped the generated SQL for all 6 validations --
present, byte-identical to before. =BookController='s constructor
signature was already correct in RefdataPlugin.cpp (fixed during the
"Move book to refdata" story), so no call-site fix needed this time.
Built clean across api/core/service/qt.refdata/qt.trading/qt.exe.

=db recreate= applied the modified SQL with no errors. Verified the
soft-FK validation actually fires at the DB level: a direct insert
with a non-existent =parent_portfolio_id= was correctly rejected with
=Invalid parent_portfolio_id: ... No active portfolio found=.

* Notes

* Test Scenarios

Manual QA scenarios (scaffolded via =compass add test_scenario=, run
through the QA Validation Runner panel) that verify this task. Link
new ones here as they're created; the scenario doc itself links back
via its "Verifies task" field.

| Scenario | State | Notes |
|----------+-------+-------|
| [[id:7DD2C147-1CED-4D8A-AE6E-F1CA1636FA7F][Verify book list/detail/history and insert-trigger validations after codegen regeneration]] | PENDING | |

* PRs

| PR | Title |
|----+-------|
|    |       |

* Review

| # | Comment summary | File | Decision | Notes |
|---+-----------------+------+----------+-------|
|   |                 |      |          |       |

* Result
