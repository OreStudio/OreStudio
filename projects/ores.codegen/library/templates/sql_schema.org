:PROPERTIES:
:ID: 4EF84682-0895-46D2-95B3-B6D8D8047A7A
:END:
#+title: SQL schema templates
#+description: DDL artefacts: entity and junction table creation, drop scripts, notify triggers, and the artefact-tracking table.
#+type: facet
#+level: cross
#+facet_group: sql
#+filetags: :codegen:templates:literate:sql:
#+created: 2026-06-06
#+updated: 2026-06-06

This page is the literate source for the =sql_schema= facet of the
codegen template library. Each section documents one template and
holds its source in a =mustache= block tangled to the sibling
=.mustache= file consumed by =generator.py=. The =.mustache= files are
generated artefacts — edit this document, then run the tangle
(=compass build --direct tangle_codegen_templates= or
=projects/ores.lisp/src/ores-build-codegen-templates.el= directly).
Output paths and profile membership come from
[[id:A8145FD7-0651-40FD-857E-BC37C9320020][facet_catalogue.org]].

* Summary

Eight DDL templates owning the table layer: bi-temporal entity tables
in the =domain_entity= and =table= model formats, junction tables,
drop scripts, the NOTIFY trigger pair, and the artefact/staging table.
Most are wired to the =sql= profile (=projects/ores.sql/create/= and
=drop/= outputs); =sql_schema_artefact_create= is invoked directly by
=generator.py= rather than via a profile.

* The sql_schema facet

The SQL projection of the /entity/ model ([[id:608675E1-4284-4902-99AC-4DA565390AFB][entity lifecycle]]): each
entity org-model's natural keys, columns, and temporal flags become a
PostgreSQL table with the house conventions — UUID primary key,
=valid_from= / =valid_to= bi-temporality where applicable,
=tenant_id=, and a =_notify= trigger announcing changes on a NATS
channel. The near-identical create templates (=create=,
=domain_entity_create=) reflect the model formats codegen has
accreted, and converge as the single-model unification proceeds.

* Mustache incantations

Beyond the group's standard constructs: every template opens with a
={{! purpose }}= comment (kept from the pre-literate files — they
render to nothing) and injects ={{{sql_license}}}= as triple-stache.
Column loops use ={{^last}},{{/last}}= inverted sections for
comma placement, and ={{#default}}…{{/default}}= /
={{#nullable}}/{{^nullable}}= for per-column DDL fragments. The
entity-create templates carry book/cross-check validation fragments
gated by ={{#declare_vars}}=, ={{#coalesce}}= and ={{#copy_empty}}=.

* Archetypes

| Archetype | Description |
|-----------|-------------|
| [[id:9C11F389-EB60-4BE2-A0D7-DBF3CEB86A26][sql_schema_create.mustache]] | Bi-temporal entity table from the /table/ model format (={{#table.columns}}=, =table.component=, =table.entity_plural=): the unified successor to the per-format create templates. =sql= profile; output =projects/ores.sql/create/{component}/{component}_{entity_plural}_create.sql= (=current_only= gates the current-rows view). |
| [[id:AE9E09E9-15FD-4A7E-9EC2-F1A6748BA812][sql_schema_domain_entity_create.mustache]] | The original bi-temporal create, driven by the /domain_entity/ model (={{#domain_entity.columns}}=, natural keys, primary key). Carries the richest validation machinery: book/cross-check error fragments, ={{#declare_vars}}= function-local declarations, ={{#coalesce}}= / ={{#copy_empty}}= column handling. =sql= profile; same output path as the unified variant — the two coexist while models migrate formats. |
| [[id:3D9951F8-C68B-4EA1-93FE-2229769997E8][sql_schema_domain_entity_drop.mustache]] | Drop script for an entity's table and dependents: a thin ={{#domain_entity}}= section over =component= / =entity_plural= / =product=. =sql= profile; output =projects/ores.sql/drop/{component}/{component}_{entity_plural}_drop.sql=. The =<<paste:5E47F108-1350-4540-B3C2-E83DD5379B2D>>= marker is replaced with any entity-model-defined extra drop statements (e.g. the validate function generated by the companion =_table.org= model). If no entity implements the kind the marker expands to an empty string. |
| [[id:1AB594D5-0BDF-4E79-B698-3877B9B6041D][sql_schema_junction_create.mustache]] | Many-to-many junction table from the /junction/ model: composite text primary key over =junction.left= / =junction.right= columns, optional =tenant_id= (={{#junction.has_tenant_id}}=), per-side indexes with comments. =sql= profile; output =projects/ores.sql/create/{component}/{component}_{entity}_create.sql=. |
| [[id:3EAC73DC-6ECE-4DB3-8172-790A2C709AAC][sql_schema_notify_trigger.mustache]] | =pg_notify= trigger announcing entity changes (insert/update/delete with the primary-key value) on the entity's channel — the database end of the eventing pipeline. ={{#domain_entity}}= section. =sql= profile; output =…/{component}_{entity_plural}_notify_trigger_create.sql=. |
| [[id:730B3A9A-5532-4A9C-8098-D152F616AAAA][sql_schema_notify_trigger_drop.mustache]] | Drop counterpart of the notify trigger. =sql= profile; output =projects/ores.sql/drop/{component}/{component}_{entity_plural}_notify_trigger_drop.sql=. |
| [[id:1025E09D-7E2D-4366-AC51-8651C95B3ADC][sql_schema_table_create.mustache]] | The earlier bi-temporal table create, sharing its output path and most of its body with =sql_schema_create= — the duplication the unified template exists to retire. =sql= profile. |
| [[id:23A3DE1F-0C2F-46B4-9D52-36261F209C6C][sql_schema_artefact_create.mustache]] | Staging table for an entity's imported artefacts: entity columns plus artefact bookkeeping (={{#entity.has_coding_scheme}}=, ={{#entity.has_image_id}}=, artefact indexes, optional insert function via ={{#entity.has_artefact_insert_fn}}=). *No profile* — invoked directly by =generator.py= on the dataset path. |

* See also

- (Parent template group doc: =<group>_group.org=.)
- [[id:3B6B9F75-BDC9-4AE9-B16E-5A05FFC3A4B5][Codegen template library]] — the groups overview.
- [[id:B079DA67-8408-4BD0-A93C-D108AC780872][Facet]] — the MASD concept these templates project.
- [[id:5CB31A0E-4E38-4521-B0EE-E4396C3E2936][SQL facet]] — the facet's modeling reference.
- [[id:0BD6F9EC-34DD-44D9-8AA1-917EE5966396][Variability Model]] — to understand the configuration knobs these
  templates branch on (the =masd.sql.*= feature bundle: =system_scope=,
  =nullable_tenant_id=, =extra_checks=, =fk_copy_validations=, …), see
  Part 2. Structural predicates (=has_tenant_id=, =is_uuid=, …) are
  derived from the model, not configured.
- [[id:43FF3591-AD18-4CE3-8239-A220862C9B5A][Applied MASD]] — the facet catalogue and codegen routes.
- [[id:A8145FD7-0651-40FD-857E-BC37C9320020][facet_catalogue.org]] — profile → template/output mapping.
