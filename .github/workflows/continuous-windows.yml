name: Continuous Windows

on:
  push:
    branches:
      - master
    tags:
      - '*'
  pull_request:
    branches:
      - master

jobs:
  windows:
    name: ${{matrix.family}}-${{matrix.compiler}}-${{matrix.buildtype}}
    runs-on: windows-latest
    environment: CI
    strategy:
      fail-fast: false
      matrix:
        name: [windows-msvc] # , windows-msvc-clang-cl
        buildtype: [debug, release]
        include:
          - name: windows-msvc
            family: windows
            compiler: msvc
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Enable Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1.13.0

      - name: get-cmake
        uses: lukka/get-cmake@v3.29.6

      - name: run-vcpkg
        uses: lukka/run-vcpkg@v11.5
        with:
          appendedCacheKey: ${{matrix.name}}${{matrix.buildtype}}

      - name: Run CTest workflow
        run: |
          $env:VORE_BUILD_PROVIDER="github"
          $env:VORE_BUILD_COMMIT="$env:GITHUB_SHA"
          $env:VORE_BUILD_NUMBER="$env:GITHUB_RUN_NUMBER"
          $env:VORE_BUILD_TIMESTAMP=Get-Date -format "yyyy/MM/dd HH:mm:ss"
          $preset="${{matrix.family}}-${{matrix.compiler}}-${{matrix.buildtype}}"
          $cmake_args="build_group=Continuous,preset=$preset"
          ctest -VV --preset $preset --script "CTest.cmake,$cmake_args"

      - name: Upload artifact
        uses: actions/upload-artifact@v1
        with:
          name: ${{matrix.family}}-${{matrix.compiler}}-${{matrix.buildtype}}
          path: ./build/output/${{matrix.family}}-${{matrix.compiler}}-${{matrix.buildtype}}/stage/pkg/VORE-1.0.33-Windows-AMD64.msi
