:PROPERTIES:
:ID: A7A25644-CBF0-4E4A-A768-4636CCF91E05
:END:
#+title: ores.trading.callable_swap_instrument
#+description: Codegen entity model for the trading =callable_swap_instrument= table. Drives the C++ domain class, mapper, repository, service, generator, Qt UI, and SQL schema generation from a single literate source.
#+type: ores.codegen.entity
#+component: trading
#+filetags: :model:entity:trading:
#+brief: Callable interest rate swap instrument.
#+entity_singular: callable_swap_instrument
#+entity_plural: callable_swap_instruments
#+entity_title: Callable Swap Instrument
#+created: 2026-06-01
#+updated: 2026-06-01

Represents a callable interest rate swap where one party has the right
to terminate the swap early on specified call dates.

* Flags
:PROPERTIES:
:schema:           public
:product:          ores
:component:        trading
:has_tenant_id:    true
:has_workspace_id: true
:END:

* Primary key
:PROPERTIES:
:column:   instrument_id
:type:     uuid
:cpp_type: boost::uuids::uuid
:END:

UUID uniquely identifying this callable swap instrument.

Surrogate key for the instrument record.

* Natural keys

* Columns

** trade_type_code
:PROPERTIES:
:group:    identity
:type:     text
:cpp_type: std::string
:nullable: false
:END:

Trade type code (soft FK to ores_trading_trade_types_tbl).

#+begin_src cpp :name generator
std::string("CallableSwap")
#+end_src

** party_id
:PROPERTIES:
:group:    identity
:type:     uuid
:cpp_type: boost::uuids::uuid
:nullable: false
:END:

Party that owns this instrument.

Set from session variable app.current_party_id.

** trade_id
:PROPERTIES:
:group:    identity
:type:     uuid
:cpp_type: std::optional<boost::uuids::uuid>
:nullable: true
:END:

Optional soft FK to the parent trade.

Links instrument to a trade if applicable.

** start_date
:PROPERTIES:
:type:     date
:cpp_type: std::string
:nullable: false
:END:

Swap effective start date.

ISO 8601 date string (YYYY-MM-DD).

#+begin_src cpp :name generator
std::string("2024-01-15")
#+end_src

** maturity_date
:PROPERTIES:
:type:     date
:cpp_type: std::string
:nullable: false
:END:

Swap maturity date.

Must be after start_date.

#+begin_src cpp :name generator
std::string("2029-01-15")
#+end_src

** call_dates_json
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: true
:END:

Optional JSON array of call dates.

ISO 8601 date strings when the party may exercise the call option.

** call_type
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: true
:END:

Optional call type: Bermudan or One-Time.

Bermudan allows multiple call dates; One-Time allows exactly one.

** description
:PROPERTIES:
:type:     text
:cpp_type: std::string
:nullable: true
:END:

Optional free-text description.

Human-readable notes about this instrument.

* SQL

** Flags
:PROPERTIES:
:tablename: ores_trading_callable_swap_instruments_tbl
:END:

* C++

The C++ domain class is decomposed into nested sub-structs (the C1202
pattern from PRs #1047/#1071/#1075/#1083/#1085): the implicit scaffolding
columns (version, tenant_id, workspace_id, the primary key) and the
columns marked =:group: identity= below fold into an
=instrument_identity identity;= member (see the
ores.trading.instrument_identity field-group model), the implicit audit
columns fold into an =ores::dq::domain::audit_record audit;= member (see
the ores.dq.audit_record field-group model), and the remaining columns
stay flat in between. The SQL schema, DB entity and column lists are
unaffected — only the domain class and the mapper's domain-side accesses
nest. The entity templates consume these annotations; domain and
repository profiles regenerate correctly.

** Flags
:PROPERTIES:
:subcomponent:    api
:domain_identity_group: ores.trading.instrument_identity
:domain_audit_group:    ores.dq.audit_record
:has_batch_read:       true
:generator_facet_name: generators
:END:

** Repository
:PROPERTIES:
:entity_singular_short: callable_swap_instrument
:entity_plural_short:   callable_swap_instruments
:entity_singular_words: callable swap instrument
:entity_plural_words:   callable swap instruments
:END:

** Domain includes

#+begin_src cpp :name includes
#include <chrono>
#include <string>
#+end_src

** Conventions
:PROPERTIES:
:iterator_var: cs
:END:

** Custom repository methods

* See also

- [[id:5C7A961E-D834-4B2F-AE15-F049B27361D8][ores.trading]] — component group overview.
