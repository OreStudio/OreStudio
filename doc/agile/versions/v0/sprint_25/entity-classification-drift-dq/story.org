:PROPERTIES:
:ID: A452F2A7-D9CB-48E6-8B23-4509AFFC5699
:END:
#+title: Story: Entity classification and drift baseline: ores.dq
#+description: Apply the ores.refdata pilot's proven pattern to ores.dq: bind every eligible entity (SQL-side and C++-side) to a variability profile, resolve every drift the zero-diff regen check surfaces, and extend the refdata-codegen-drift CI job to cover dq and dq-cpp. Supersedes the ad hoc 'Apply safe drift to dq-cpp' task.
#+type: story
#+level: s2
#+filetags: :codegen:drift:tooling:sprint_25:v0:
#+created: 2026-08-04
#+updated: 2026-08-04
#+environment: jolly_knuth
#+todo: BACKLOG STARTED | DONE ABANDONED

This page documents a [[id:699A8D45-B67E-4D3E-9206-24FA17C51ADA][story]] in [[id:0E7321A7-A66D-4CE6-B3E7-89F2E35CA48B][Sprint 25]]. It captures the goal, current status, acceptance criteria, and the tasks that compose it.

* Goal

Bring ores.dq to the same proven state as the ores.refdata pilot: every codegen-eligible entity across both the SQL-side (dq) and C++-side (dq-cpp) catalogue entries bound to a profile or recorded as a genuine no-match, zero-diff regeneration verified, and any real gap the check surfaces fixed rather than skipped. Once trusted, dq and dq-cpp are added to the CI regeneration-drift check so this component can never silently drift again.

* Status

| Field         | Value                                  |
|---------------+----------------------------------------|
| State         | STARTED                              |
| Parent sprint | [[id:0E7321A7-A66D-4CE6-B3E7-89F2E35CA48B][Sprint 25]] |
| Now           | Not yet started.                       |
| Waiting on    | Nothing.                               |
| Next          | Break the story into tasks.            |
| Last touched  | 2026-08-04                               |

* Acceptance

- Every ores.dq entity (both catalogue entries: dq and dq-cpp) is bound to a matching Variability Profile, or recorded as a genuine no-match with a documented reason (per the pilot's Notes precedent).
- Every rebound entity is regenerated and diffed against its pre-rebind checked-in output; zero diff is the pass condition, and any non-zero diff is fixed (profile catalogue, matching logic, or a real pre-existing template/model bug), not worked around.
- Full local build clean, validate_schemas.sh green, full ctest suite green after regeneration.
- check_component_drift.py --components is extended to include dq and dq-cpp, and the refdata-codegen-drift CI job (.github/workflows/pr.yml) is updated so future PRs touching ores.dq fail on any regeneration drift.
- The superseded ad hoc task 'Apply safe drift to dq-cpp' (doc/agile/versions/v0/sprint_25/refactor_codegen_cpp/task_apply_safe_drift_dq.org) is marked ABANDONED, pointing here.
- The known starting-point wrinkles already recorded for dq-cpp (service_find_by_uuid/service_find_by_code on dataset_bundle; HexPrefix model typo) are resolved as part of reaching zero drift, not left outstanding.

* Tasks

| Task | State | Start | End | Description |
|------+-------+-------+-----+-------------|
| [[id:163AC07E-19E4-4486-A503-8170D9B46108][Bind ores.dq (SQL) entities to profiles; verify zero-diff regen]] | STARTED | 2026-08-04 | | Implement profile binding for the dq catalogue entry (SQL-side model discovery), rebind every matching entity, regenerate, and confirm byte-identical output, following the ores.refdata pilot method exactly. |
| [[id:C4D47809-FC4F-4D89-BB15-B0A88C3FCACB][Bind ores.dq (C++) entities to profiles; verify zero-diff regen]] | BACKLOG | | | Implement profile binding for the dq-cpp catalogue entry, rebind every matching entity, regenerate, and confirm byte-identical output. Known starting point (from the superseded ad hoc task): set service_find_by_uuid and service_find_by_code on dataset_bundle; fix a HexPrefix model typo. |

* Decisions

* Out of scope

-
